# 第 3 轮 · 复验报告（U1/X1 复验 + 全量回归 + 三轮回环终验）

> 项目：金丝雀蜜罐 CanaryGuard AntiFraud v1.0.0 ｜ 执行：qa-tester ｜ 依据：`docs/第3轮_实战测试报告.md` §5 终验门槛 + t14（U1 前端）/t15（X1 熔断）修复
> 生成时间：2026-09-13 12:27:45 ｜ 主控：http://127.0.0.1:9200（重启加载修复代码）

## 1. U1 复验（P1，webui-dev 修复）→ **PASS**

【修复方案确认】webui/src/views/CasesView.vue：发布弹窗新增 confirm 复选框（『我确认要发布该脱敏案例（confirm=true）』）与 reason 输入（≥20 字，提示写入 GateLog）；`canPublish` 需 det 已选 + confirm + reason≥20，否则『发布』按钮禁用；client.js publishCase 增加 confirm/reason 参数；后端错误映射友好提示（confirm_required/reason_too_short/sensitive_data）。

【复验操作・实测结果】
- 真实浏览器 /cases → 打开发布弹窗：确认复选框与『发布理由（≥20 字，写入审计日志 GateLog）』输入框**均存在** ✓
- 未选检测/未勾选 confirm/reason<20 时『发布』按钮**禁用**（实测 disabled=true）✓
- 选检测 #3035 + 勾选 confirm + reason≥20 → 按钮**启用** → 点击发布 → **『案例 #1654 已发布』（HTTP 200 入库）** ✓
- 含手机号『13812345678』载荷 → **被拒且显示友好提示**：『脱敏未通过：载荷仍含敏感信息（手机号/身份证/微信等），请先脱敏再发布』（无原始 pydantic 文本）✓

## 2. X1 复验（P1，backend-core 修复）→ **PASS**

【修复方案确认】app/services/llm_provider.py：连续失败熔断（degraded）+ **半开（half-open）探活**——冷却期（llm.cooldown_seconds 可热更新，默认见实现）到期后放行 1 次单飞探测：成功 → 解除熔断（state=close），失败 → 重新熔断并重置冷却；熔断/探活/恢复写日志并写 events（kind=llm_circuit_breaker，payload.state=open|half_open|close）。

【复验操作・实测结果】
- **进程内复验**（`var/qa_round3/verify_r3_breaker.py`，临时库 + mock LLM 先 500 后 200）：3 次失败 → degraded=True；熔断中 llm_used=False（纯规则 200 不瘫）；events `llm_circuit_breaker open` ≥1；冷却 0.4s 后 **自动恢复 llm_used=True / degraded=False（无需重启进程）**；events `close` ≥1 → **ALL_PASS** ✓
- **实机复验**（真实主控 + AF_LLM_* 指向 mock，3 次 500 后 200）：3 扫描 llm_used=False ×3 → events open=3 → 熔断中 llm_used=False → mock 恢复 + 冷却 0.2s → **scan llm_used=True（自动恢复）** → events close=1 → **ALL_PASS** ✓
- 复验后主控已恢复默认配置（llm=not_configured），mock 进程已停 ✓

## 3. 第 3 轮其余项回归抽查 → 全部通过

- **C 并发 publish**：30 并发全 200（无 500/误 404）✓
- **S 大库分页**：/events page=2 ×50 → 200/50 条；/cases?tag=fake_investment → 200 total=65 ✓
- **D deep-link**：/ui/dashboard、/ui/supply-chain、/ui/sessions?tab=hit&id=3035 全 200 ✓
- **X 强杀 WAL**：预写 2 条 pending → 强杀 → 重启 → integrity=ok、health=200、**2 条 pending 零丢失** ✓
- **L 长稳短采样**：3 分钟 18 采样点 health 全 True（终验抽测；35 分钟长稳已在第 3 轮报告 L1 完成）✓

## 4. 终验门槛（回归）

| 项 | 命令 | 结果 |
|---|---|---|
| pytest 全量 | `.venv\Scripts\python -m pytest -q` | **240 passed**（36.63s，修复新增用例含在内）|
| 前端构建 | `npm run build`（webui/） | **成功**（✓ built in 29.21s，含 U1 修复产物）|
| e2e 63 断言 | `tests/e2e_acceptance.py` | **63 PASS / 0 FAIL**（脚本退出码 1 为输出通道噪音，汇总明确 63/63）|
| 主控健康 | `GET /system/health` | **200 status=ok**（默认配置，key 未轮换）|

## 5. 三轮回环终验结论

- **U1/X1 复验通过** → 第 3 轮 39/39 全部闭合（37 原生通过 + U1/X1 修复后复验通过），**P0/P1 清零**
- **回归全绿**：pytest 240 passed + npm build + e2e 63/63 → 终验门槛达成
- 遗留 P2 观察项（留档，不阻断）：重复案例发布提示（B9）、disabled 蜜饵可 monitor（C5）、LLM 熔断无日志（已随 X1 修复补日志/events，实际已解决）、证据链外部锚点、当前 key 熵属历史轮次已升级 128bit 等
- **总体结论：达标**（三轮回环 120 用例终态通过率见 `docs/三轮回环总报告.md`）
