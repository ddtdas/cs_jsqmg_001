# 严格验收 R4 · 代码级验收（调用关系）复验报告

- 轮次：严格验收第 4 轮 · 代码级验收（R3 修复复验 + 收尾建议）
- 审计对象：192.168.10.110 `C:\Users\Administrator\Desktop\知乎黑客松\金丝雀蜜罐`（只读审计，未改任何文件，未重启；主控已加载 R1-R3 代码）
- 审计员：code-auditor（调用关系检查）
- 基线：pytest 462 passed（R3 修复后）；9200 health 200
- 方法：R4 全量镜像（99 py）+ R3→R4 逐文件 diff（9 文件）+ 远程只读 DB 实测（UNIQUE 索引/80 行/pending 收敛）+ 线上 openapi 94 复测 + AST 调用图/静态检查复跑

---

## 0. 结论摘要

| 级别 | 数量 | 一句话说明 |
|------|------|-----------|
| P0   | 0    | 无阻断级缺陷 |
| P1   | 0    | R3 修复全部正确落地，无回归 |
| P2   | 1    | R1-R3 遗留项（死代码/未引用 import/注释漂移）仍未处理，建议第 5 轮收尾清理（任务定向要求） |
| P3   | 3    | 遗留项明细（7 处死代码/5 处未引用 import/6 处注释漂移）、evt 审计更新型终态语义说明、CasesView 文案为纯前端 |

**R3 修复验证：6/6 通过（pending resolved/terminal 收敛、UNIQUE(det_id) 索引上线无损、is_trap_escalated 共享、start.bat 精确匹配 + 启动后复核、CasesView 文案、test_r8 覆盖）。**

---

## 1. R3 修复逐项复核

### 1.1 ✅ _escalate_pending 终态收敛（resolved/terminal）—— 正确，P2-1 闭环

- **落地**（scheduler.py:131-199）：
  - 查询仅取**未终态** pending 行：`json_valid(payload)=1 AND json_extract('$.resolved') IS NULL AND json_extract('$.terminal') IS NULL`；
  - **resolved 收敛**：升级成功/已升级 → payload 追加 `resolved=true, recovered=<bool>, resolved_at`，UPDATE 原行（不删除事件——保留审计轨迹，符合 events append-only 约定）；
  - **terminal 收敛**：检测记录不存在（已删除）→ payload 追加 `terminal=true, reason='detection_deleted', resolved_at`，**停止重试**（彻底消除 R3 报告的"已删 det 每轮 detection_not_found"问题）；
  - 仅瞬时异常（DB 繁忙等）保持原状留待下轮；
  - 返回 `(done, terminal)` 元组，job_trap_recheck 双值消费并在日志输出。
- **test_r8 覆盖**：`test_pending_marked_resolved_after_replay`（重放→L5+resolved+recovered=True+第二轮跳过）、`test_pending_terminal_on_deleted_det`（999999→terminal+第二轮不再抛/不再重试）断言完整。
- **线上（只读）**：pending 行 0 → 生产无积压；收敛逻辑为纯代码路径（测试验证足够）。
- 影响评估：events 不再无界增长；每轮仅扫未终态行；审计轨迹保留（UPDATE 而非 DELETE 是正确选择）。

### 1.2 ✅ cases UNIQUE(det_id) 索引（P2-2）—— 线上无损落地

- **落地**（models/__init__.py INDEXES）：`"idx_cases_det_unique": "CREATE UNIQUE INDEX IF NOT EXISTS idx_cases_det_unique ON cases(det_id)"` 加入 INDEXES → ensure_schema 启动建出（幂等）。
- **线上实测（只读）**：
  - `PRAGMA index_list('cases')` → `idx_cases_det_unique, unique=1` ✓ 已存在；
  - `SELECT sql ... idx_cases_det_unique` → `CREATE UNIQUE INDEX idx_cases_det_unique ON cases(det_id)` ✓；
  - cases 仍 **80 行**、0 重复组 → **索引加装零数据丢失** ✓。
- **语义核查**：det_id 允许 NULL（cases.det_id 无 NOT NULL），SQLite UNIQUE 允许多个 NULL → UNIQUE(det_id) 不误伤无 det 的案例（当前 0 NULL，语义安全）；与 `_publish_tx` 的 IntegrityError 兜底（retry 到 503）配合——应用层幂等返回在查重先行，撞索引的概率仅剩跨进程竞态，落 503 而非重复（可接受）。
- test_r8 `test_cases_unique_index_created_and_enforced`（索引创建 + 直插重复 IntegrityError + 应用层幂等返回不受影响）断言正确。

### 1.3 ✅ is_trap_escalated 抽取共享（P3-1）—— 复用正确无漂移

- **落地**（grading.py:41-58）：`GradingService.is_trap_escalated(conn, detection_id)` 静态方法，口径同原 `_det_already_trap_escalated`（grade=L5 且 grade_reason 含 trap_hit 信号），docstring 注明供 scheduler/traps 共用；
- scheduler.py 删除私有 `_det_already_trap_escalated`，`_escalate_detection` 改调 `GradingService.is_trap_escalated(conn, det_id)`（grading.py:119）；
- api/traps.py `_escalate_manual_hit` 删除内联重复实现，改调同一方法（traps.py:216-222）→ **两处调用点单一口径**，无漂移风险；
- **调用关系复查**：scheduler→grading、traps→grading 均为函数内延迟 import，单向无环；分析器死代码列表不再出现 `_det_already_trap_escalated`（已删除）✓；
- test_r8 `test_is_trap_escalated_shared_check`（L5+trap_hit→True / L5+llm_fraud→False / L3→False / 不存在→False）覆盖四分支。

### 1.4 ✅ start.bat --port 9200 精确匹配 + 启动后复核（P3-5）—— 双保险落实

- **start.bat**：zombie 清理匹配从 `-match '9200'` 改为 `-match '--port 9200'`（精确，不再误匹配 `19200` 等含 9200 子串的命令行）；**新增启动后复核段**：START_MASTER 后 ping 2s → 重新 netstat 取 LISTENING PID → 再清一次僵尸（持有端口者及其 venv 父进程受保护，同秒双启动落败方被清理）→ **双启动互杀窗口关闭**；
- **start.ps1**：同步加 `Start-Sleep 2s` + 启动后复核清理（`--port 9200` 精确匹配 + safePids 保护），catch 兜底不阻塞启动；
- 语义核查：复核在 health probe 之前，正常启动路径多 2s 延迟可接受；PID 解析与异常分支与既有逻辑一致。

### 1.5 ✅ CasesView case_exists 文案（P3-4）—— 前端补全

- CasesView.vue publishErrorText 新增 `case_exists → '该检测记录已发布过案例（同一检测仅一个案例），请选择其他检测记录'`——语义准确、位置正确（sensitive_data 之后、422 之前），纯前端改动不涉及 API。

### 1.6 ✅ test_r7 同步更新（UNIQUE 前置兼容）

- test_r7 R4 版在构造遗留重复脏数据前 `DROP INDEX IF EXISTS idx_cases_det_unique`（模拟索引加装前 legacy 库），清洗后由 ensure_schema 恢复——与线上"先清洗后加索引"时序一致，**无测试-实现错位**。

---

## 2. 调用关系复查（R4 最终图）

- **变更面**：R3→R4 仅 9 个文件（6 个 app/脚本 + CasesView + test_r7 更新 + test_r8 新增），无结构级变化；
- API→服务新增边：`traps → grading`（R3 已有，本轮回调共享方法）→ 合法单向；scheduler→grading 同样合法；
- **循环依赖：0**；**幽灵调用：0**（4 个常量 + 1 个别名误报沿用排除）；
- **新断点：0**；**新死代码**：is_trap_escalated 被 2 处消费（非死代码）；`_det_already_trap_escalated` 已删除（不再出现在死代码列表）✓；
- 死代码遗留（R1-R3 未动）：cfg_int/cfg_bool/make_reader/on_degraded/strip_fence/get_scheduler/job_snapshot —— 仍为测试专用或 0 引用（详见 §4）。

---

## 3. 前端契约（client.js ↔ openapi）R4 复查

- client.js 与 docs/openapi.json 哈希与 R3 完全一致（R3==R4: True）→ 前端 API 层零改动；
- 线上 /openapi.json = 94 paths，client.js 91 调用逐一命中（contract_check4 无 MISS）；
- 反向 15 个未调用端点不变（测试覆盖面，P3 设计内）；
- 本轮唯一前端改动为 CasesView 错误文案（非 API 契约）→ 无契约影响。

---

## 4. 静态健康与 R1-R3 遗留项（任务定向检查）

### 4.1 py_compile
- 镜像 99 个 .py（app/mcp/scripts/tests/var，含 test_r8）**全部通过**，0 语法错误。

### 4.2 R1-R3 遗留项——仍存在，建议第 5 轮收尾清理（P2-1）

| 类别 | 项 | 位置 | 建议 |
|------|----|------|------|
| 死代码 | `cfg_int` / `cfg_bool` | config_reader.py:39/47 | 全库 0 调用，删除或由调用方接线消费 |
| 死代码 | `make_reader` | config_reader.py:59 | 仅测试用（test_r3），删除或标注测试专用 |
| 死代码 | `on_degraded` | llm_provider.py:47 | 仅测试用，删除或标注 |
| 死代码 | `LLMProvider.strip_fence` | llm_provider.py:142 | 全库 0 调用（fence() 为实际路径），删除 |
| 死代码 | `get_scheduler` / `job_snapshot` | scheduler.py:479/483 | 仅测试用；若运维面板需要建议接入 /system/stats |
| 未引用 import | `ApiError` | api/collector.py:18 | 删除（仅 ok 在用） |
| 未引用 import | `ChannelUnavailableError` | api/zhihu.py:19 | 删除（仅 CHANNELS/ZhihuBridge 在用） |
| 未引用 import | `Any` | speech_engine.py:23、desensitize.py:17、trap_engine.py:33 | 删除 |
| 未引用 import | `FENCE_OPEN/FENCE_CLOSE` | speech_engine.py:30-31 | 删除（围栏由 llm_provider 内部处理） |
| 注释漂移 | "14 张表"（实为 22） | db.py:6/:80、models/__init__.py:1/:4 | 改 22 |
| 注释漂移 | "8 类 job"（实为 9） | main.py:5/:58、scheduler.py:57 | 改 9 |

> 注：R4 本轮 fix-engineer 聚焦 R3 验收点（P2-1/P2-2/P3-1/P3-4/P3-5），遗留项连续 4 轮未动——均为**低风险清理项**（无运行时影响、无契约影响），建议第 5 轮集中收尾后再做终验。

### 4.3 重复函数
- 0（zhihu_bridge 多类同名 to_dict 误报排除）。

---

## 5. 问题清单（R4 结论）

| # | 级别 | 位置 | 类型 | 结论 |
|---|------|------|------|------|
| P2-1 | P2 | 全项目（见 §4.2） | 遗留清理项（任务定向） | R1-R3 死代码/未引用 import/注释漂移未处理；建议第 5 轮收尾 |
| P3-1 | P3 | scheduler.py:152-199 | 语义说明 | pending 终态用 UPDATE payload 标记而非删除——保留了审计轨迹（append-only 约定），resolved/terminal 行永久留存 events（量级小，正常）；如需瘦身可加定期归档 job（非必须） |
| P3-2 | P3 | CasesView.vue / client.js | 纯前端 | case_exists 文案已补；其余未调用端点 15 个为设计内（测试覆盖），无需处理 |
| P3-3 | P3 | evidence frozen 冗余包 | 设计内确认 | 15 个多包 det 全为 frozen=1（冻结存证保留），R4 无变化 |

---

## 6. 修复建议清单（供 fix-engineer R5 收尾）

1. **P2-1**：按 §4.2 清 7 处死代码 + 5 处未引用 import + 6 处注释漂移（14→22 张表、8→9 类 job）
2. **P3-1**：（可选）pending 终态行归档策略——若 events 表量级增长可加归档，非必须
3. 无需其他修复——R3 验收点全部闭环，P0/P1 为 0

*报告完*