# 严格验收 R3 · 文档幻觉核实与修复报告

> 任务：t11 ｜ 角色：doc-verifier（文档幻觉核实员：核实+修复）
> 日期：2026-09-17 ｜ 项目：金丝雀蜜罐 CanaryGuard AntiFraud v1.0.0（远程 192.168.10.110，`C:\Users\Administrator\Desktop\知乎黑客松\金丝雀蜜罐`）
> 输入：R2 修复已合入（fix-engineer t8，458 passed）、R2 文档修复（t7）已落盘
> 方法：读 docs/ + 读代码（R2 改动 5 文件：scheduler/evidence/traps/cleanup_duplicates/test_r7_r2fixes）+ 调线上 API（openapi/stats/audit-verify，admin key）+ 远程实跑 pytest + 线上 DB 只读核查；只改文档（UTF-8 无 BOM），不修代码、不重启主控。

---

## 0. 结论摘要

R2 修复新增 7 条专项回归（`tests/test_r7_r2fixes.py`），**pytest 从 451 → 458**；R2 修复引入
**5 项代码行为变化**（P1-A 补偿重放 trap_hit_escalation_pending、P1-B 存量清洗 cleanup_duplicates.py、
P1-C 手工 check-hit 联动升级、P2-1 json_valid 门控、P3-1 幂等防重），其中 **3 项需同步文档**
（pytest 458、cleanup_duplicates 运维说明、check-hit 行为/事件 kind）；**存量清洗后数字与 R2 修复报告、线上 DB 完全一致**。

| 核实项 | 文档原声明 | 实测值（R3） | 结论 |
|---|---|---|---|
| pytest 全量 | README/docs-README/00/07 写 451（29 文件） | 远程实跑 **458 passed**（30 文件，59.99s，collect 458） | ❌ 过期 → 已改 5 处 + 07 表格补 test_r7_r2fixes 行 |
| OpenAPI | 94 paths / 103 ops / 46 schemas | 线上 94/103/46（R2 无新增路由） | ✅ 一致 |
| 表 / job / 工具 / 路由 | 22 表 / 9 job / 55 工具 / 24 路由 | 22 / 9 / 55 / 24（R2 未动 schema/调度/工具/路由） | ✅ 一致 |
| 验收报告序列 | …→451 | 451 → **458**（R2 修复新增 test_r7_r2fixes 7 条） | ⚠️ 序列追加 458 → 已改 07-测试 |
| 存量清洗数字 | R2 修复报告：cases 1695→80、evidence 738→103（冻结 86 保留） | 线上 DB 实测 **cases=80、evidence=103、frozen=86、重复组=0**；API stats cases=80；audit-verify `{valid:true, checked:1824}` | ✅ 完全一致（无幻觉） |
| cleanup_duplicates.py | docs 无该脚本任何说明 | scripts/cleanup_duplicates.py 存在（7425B，--dry-run/备份/审计/冻结保护） | ❌ 缺使用说明 → 已补 README §7/§8、00-项目概述、03-部署 §5.1 |
| 手工 check-hit 联动 | 02-API 写「命中即退役」；README/00 仅提 job 联动 | api/traps.py CheckHitRequest 含可选 `detection_id`，命中→finalize L5+trap_hit_escalated（source=manual_check_hit），escalation 字段 escalated/skipped/failed | ❌ 缺 P1-C 说明 → 已补 02-API/06-MCP/README/00 |
| 补偿路径 | 02-API events 未列 kind | scheduler `_escalate_pending` 重放 `trap_hit_escalation_pending`（at-least-once，recovered 标记） | ❌ 缺说明 → 已补 02-API events kind 清单 |
| R2 代码加载态 | — | **运行主控（15:24 启动）未加载 R3 磁盘代码（17:05）**：线上 openapi CheckHitRequest props=['text']（磁盘含 detection_id） | ⚠️ 运维动作（R3 宏观 P0-1 已记录）；文档描述以磁盘代码为准，运行态待重启后生效 |

---

## 1. 已修复（文档，共 7 个远程文件 + 1 个本地交接文档）

### 1.1 根 `README.md`
| 位置 | 原内容 | 改后 |
|---|---|---|
| §7 目录 | `tests/ # pytest（451 用例）` | `pytest（458 用例）`；`scripts/` 行补 `cleanup_duplicates.py（存量去重清洗）` |
| §8 测试 | **451 passed**（29 个测试文件，…test_r6_r1fixes…） | **458 passed**（30 个测试文件，…test_r6_r1fixes/test_r7_r2fixes…）；追加「存量数据维护：scripts/cleanup_duplicates.py（幂等，--dry-run 预演；冻结包永不删除；自动备份+审计；R2 已线上执行 cases 1695→80、evidence 738→103）」 |
| §5 蜜饵 R1 行 | `POST /traps/{id}/check-hit`（草稿 HITL 不发布） | 追加「（R2：命中即退役 + 联动检测升级 L5，可选 detection_id）」 |

### 1.2 `docs/README.md`（索引）
- 07-测试 行：「全量测试（451 passed）」→「（458 passed）」。

### 1.3 `docs/00-项目概述.md`
| 位置 | 原内容 | 改后 |
|---|---|---|
| §3 测试 | pytest（451 用例，29 个测试文件，…R1 修复回归） | pytest（458 用例，30 个测试文件，…R1/R2 修复回归） |
| §3 新增 | （无数据维护行） | 新增「数据维护：scripts/cleanup_duplicates.py（幂等清洗存量重复案例/冗余证据包，冻结包永不删除；--dry-run + 自动备份 + 审计；R2 线上执行 cases 1695→80、evidence 738→103）」 |
| §4 R1 | 命中即退役防反查并联动检测升级 L5（R1 修复：trap_hit_escalated） | 追加「job 与手工 check-hit 均联动（R1/R2 修复）」 |

### 1.4 `docs/02-API.md`
| 位置 | 原内容 | 改后 |
|---|---|---|
| traps check-hit | 「检查踩饵命中（P2-4：POST body `{text}`；命中即退役）」 | 追加「可选 `detection_id`；命中即退役 + **联动检测升级 L5**（R2 修复：按显式 detection_id 或 text 反查，escalation 字段 escalated/skipped/failed，失败不影响命中主结果）」 |
| events | 「事件流（分页/kind 过滤）」 | 补充 kind 清单：「trap_hit / trap_retired / trap_hit_escalated / trap_hit_escalation_pending（R2 补偿未决）/ detection_graded / alert_created / case_ready / data_cleanup 等」 |

### 1.5 `docs/03-部署.md`
- 新增 **§5.1 存量数据去重清洗（R2 新增运维工具）**：cleanup_duplicates.py 用法（--dry-run 预演 → 实际清洗）、清洗范围（cases 同 det 保留最早一条；evidence 完全冗余未冻结包）、**已冻结证据包永不自动删除**、json_valid 门控、审计（events data_cleanup + gate_logs data.cleanup_duplicates）、R2 线上执行结果（cases 1695→80、evidence 738→103）。

### 1.6 `docs/06-MCP集成.md`
- §3 工具表 `af_check_trap_hit` 行：追加「命中即退役 + 联动检测升级 L5（R2 修复；可选 detection_id 由 REST 直传，MCP 工具签名未暴露该参数）」。

### 1.7 `docs/07-测试.md`
| 位置 | 原内容 | 改后 |
|---|---|---|
| §1 命令注 | 451 passed（R2 复核，约 58s） | 458 passed（R3 复核实测，约 60s） |
| §1 结果 | **451 passed**（29 文件） | **458 passed**（30 文件） |
| §1 基线演进 | …→451（严格验收 R1 回归 +5，当前） | …→451（R1 回归 +5）→ **458（严格验收 R2 回归 test_r7_r2fixes +7，当前）** |
| §2 表头 | 29 文件，R2 复核 | 30 文件，R3 复核 |
| §2 表格 | 29 行合计 451 | 新增 `test_r7_r2fixes.py | 7 | 严格验收 R2 修复回归：P1-A 补偿重放 / P3-1 幂等 / P1-C 手工联动 / P2-1 json_valid 门控 / P1-B 存量清洗幂等`，合计 **30 文件 / 458** |
| §2 注 | 451 collected/passed（57.96s） | 458 collected/passed（59.99s） |

### 1.8 本地 `交接-金丝雀蜜罐.md`（本地交接文档，非远程）
- R2 复核注更新：pytest 451（29 文件）→ **458 passed（30 文件，含 test_r6_r1fixes/test_r7_r2fixes）**；追加存量清洗结果（cases 1695→80、evidence 738→103，冻结 86 保留）。

---

## 2. 核实通过（无幻觉）

| 项 | 声明 | 实测 |
|---|---|---|
| 线上 health / openapi / stats | health 200；94 paths / 103 ops / 46 schemas；stats cases=80 | ✅ R3 实测一致（stats：traps=1385、detections=3121、cases=80、alerts=1485、events=17014） |
| 存量清洗后数字 | R2 修复报告 cases=80、evidence=103、frozen=86、重复组=0、gate_logs=1824 | ✅ 线上 DB 实测完全一致；audit-verify `{valid:true, checked:1824}`；幂等 dry-run 记录在 R2 修复报告 |
| 22 表 / 9 job / 55 工具 / 24 路由 | R2 后文档 | ✅ 代码/线上核对一致（R2 未涉及 schema/调度/工具/路由变更） |
| R2 修复 7 条回归 | test_r7_r2fixes 7 条（P1-A×1、P3-1×1、P1-C×1、P2-1×1、P1-B×3） | ✅ 已纳入全量（451+7=458）；与 R2 修复报告一致 |
| e2e 63/63 | 端到端验收报告 | ✅ 历史记录一致（只读未重跑） |
| cleanup_duplicates.py 行为 | --dry-run / 备份 / 冻结保护 / 审计 | ✅ 脚本实测参数与 R2 修复报告一致 |

---

## 3. 待代码/运维（非文档，交 fix-engineer / 运维）

1. **R3 宏观 P0-1（最高优先）**：运行中主控（15:24 启动）未加载 R3 磁盘代码（17:05）——线上 CheckHitRequest 仍为 ['text']（无 detection_id），P1-A/P3-1/P1-C/json_valid 门控尚未生效。**动作：一键重启主控（start.bat）后复验 openapi 指纹**；重启后补一次生产受控踩饵终验。
2. **P2-8（R2 已记录，前置已满足）**：cases 已清洗 0 重复，可安全加 `UNIQUE(det_id)` 约束。
3. **P2-9 遗留**：evidence 冻结重复包（10 组 frozen=1）为存证语义设计保留；加全局唯一约束前需人工决策。
4. **P2-5/P2-6（沿用）**：代码注释「14 张表」「8 类 job」漂移（代码注释非文档，未改）。
5. **P3-5（R2 记录）**：start.bat `-match '9200'` 过宽 + START 后互杀窗口（脚本精度优化，后续轮次）。

---

## 4. 报告落盘

- 远程：`docs/严格验收R3_文档幻觉.md`（本文件）
- 本地：`E:\dsh3080工作区存放位置\dsh_gzq1\严格验收R3_文档幻觉.md`

> 全部修改均 UTF-8 无 BOM 写回；未修改任何代码；未重启主控（9200 全程在线，health 200）。
