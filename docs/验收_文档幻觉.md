# 验收报告 · 文档幻觉核实（final-acceptance · doc-audit3）

> 任务：t3 ｜ 角色：doc-audit3（文档幻觉核实员：核实 + 修复）
> 日期：2026-09-17 ｜ 项目：金丝雀蜜罐 CanaryGuard AntiFraud v1.0.0（远程 192.168.10.110，`C:\Users\Administrator\Desktop\知乎黑客松\金丝雀蜜罐`，主控 9200）
> 方法：读 docs + 代码 + 线上 API 实测（openapi/stats/health/ui-cover）+ 远程 DB 只读核查 + pytest collect-only；只改文档（UTF-8 无 BOM），不修代码、不重启主控。

---

## 0. 结论摘要

**核实通过 + 修复完成：全部核心数字已与当前状态（P19 反钓鱼伪装合入后）对齐，cover 反钓鱼伪装已补入 README 与 docs 全套；docs/openapi.json 已重新导出；历史验收报告一致性核对无冲突（R1–R5 为轮次快照，R5 已追加 P19 补充注）。**

| 核实项 | 任务目标 | 实测值（本次） | 结论 |
|---|---|---|---|
| pytest 全量 | 479 | 远程 `pytest --collect-only -q` = **479 tests collected**（32 文件）；队C 实跑 479 passed（76.69s）；test_p19_cover 17 条 | ✅ 一致 |
| OpenAPI | 95+（含 cover 7 端点） | 线上 `/openapi.json` **101 paths / 111 ops / 49 schemas**；cover = **7 paths / 8 ops**（94+7=101，103+8=111，46+3=49） | ✅ 一致 |
| 表数 | 24 | 线上 sqlite_master **24 张**（含 cover_identities/contact_events；models/db.py 注释已同步为 24） | ✅ 一致 |
| job 数 | 9 | scheduler.py `add_job` **9 个**（scan_dm/zhihu_sync/trap_recheck/account_scan/evidence/case_publish/collector_matrix/persona_sim/knowledge_decay） | ✅ 一致 |
| MCP 工具 | 55 | mcp/server.py `async def af_*` **55 个**；映射主控端点 **54 个**（af_register_agent 为本地自描述） | ✅ 55 一致（映射数 50→54 已修正） |
| 路由/视图 | 25（+cover） | webui router **25 个带组件路由**（含 /cover）+ / 重定向 + 通配兜底；views/ **25 个 .vue** | ✅ 一致 |
| cases / evidence | 80 / 103 | 线上 `cases=80`、`evidence_packages=103`（frozen 86，队C 复核） | ✅ 一致 |
| cover 反钓鱼伪装入文档 | 缺则补 | **修复前**：根 README + docs/00/02/05/07/索引 **0 处** cover 说明；**修复后**：全套已补（见 §2） | ⚠️→✅ 已补 |
| 核心文档残留旧数字 | 0 | 修复后复扫：README + docs 核心 10 篇 **0 命中**（462/94 paths/22 表/24 路由/31 文件/P0–P18 等全部清除）；仅轮次报告（R1–R5）历史正文保留 | ✅ 已清零 |
| 历史报告一致性 | 含 cover 验收 479 | `反钓鱼伪装验收_队C.md`（479 passed）与当前一致；`验收报告_端到端功能验收.md` 为 P10 时点快照（63/63、112 passed）属历史记录；R5 文档幻觉报告已追加 P19 补充注 | ✅ 一致 |

---

## 1. 数字核验明细（文档 vs 实际）

### 1.1 线上实测证据
- `GET /api/v1/system/health` → 200 `{status:ok, version:1.0.0, db:true, llm:not_configured, zhihu:idle}`
- `GET /openapi.json` → 200：**101 paths / 111 ops / 49 schemas**；`/api/v1/cover` 7 paths（identities GET/POST、{id}/toggle、{id} DELETE、{id}/expose、contact、contacts、map）
- `GET /api/v1/system/stats`（admin key）→ 真实计数：traps=1386、detections=3117、alerts=1481、cases=80、accounts=31、events=17087、**cover_identities=0、contact_events=5**（看板联动字段存在 ✅）
- `GET /ui/cover` → 200（SPA fallback，CoverView 已部署）
- DB 只读：24 表清单（含 cover_identities、contact_events）；cases=80、evidence_packages=103

### 1.2 修复前发现的文档幻觉（全部已修）
| 文件 | 旧（幻觉） | 新（实测） |
|---|---|---|
| docs/README.md L15 | 全量测试（462 passed） | 479 passed（含 P19 专项） |
| docs/README.md L17 | openapi 94 paths / 103 ops / 46 schemas | 101 / 111 / 49（含 cover 7 paths） |
| docs/README.md L13 | 24 个页面/路由 | 25 个（含 /cover） |
| docs/00-项目概述.md L31 | 22 张表 | 24 张表（+cover_identities/contact_events） |
| docs/00-项目概述.md L38 | 24 路由 / 24 视图 | 25 / 25 |
| docs/00-项目概述.md L40 | 462 用例，31 文件，P0–P18 | 479 用例，32 文件，P0–P19 |
| docs/01-快速开始.md L34 | 幂等建 22 表 | 24 表 |
| docs/02-API.md L3 | 94 paths / 103 ops / 46 schemas | 101 / 111 / 49（含 cover 7 paths） |
| docs/02-API.md L146 | 透传 94 个端点中已映射 50 个 | 101 个中已映射 **54** 个（重数核对） |
| docs/02-API.md（缺失） | 无 cover 端点章节 | 新增 §2.2 反钓鱼伪装端点（7 paths / 8 ops + 闸控说明） |
| docs/05-WebUI.md L18 | 24 路由 / 24 视图 | 25 / 25 |
| docs/05-WebUI.md（缺失） | 页面表无 /cover | 新增 /cover 伪装账号行 |
| docs/06-MCP集成.md L69 | 94 个端点中 50 个 | 101 个中 54 个 |
| docs/06-MCP集成.md L105 | 主控现有 94 个 REST 端点 | 101 个（cover 并入未映射清单） |
| docs/07-测试.md L9/L13/L14/L16/L51/L53/L60/L65 | 462 passed、31 文件、缺 P19 行 | 479、32 文件、历史基线追加 479、补 test_p19_cover 行、101/111/49 |
| README.md L65-66/L142/L165/L220/L226/L286/L290/L295/L304 | 94 paths、22 表、24 页面路由、50 个映射、462 用例 | 101 paths、24 表、25 页面、54 个映射、479 用例 + P19 说明 |
| docs/MCP端核对表.md 注 | 当前权威 openapi 94 paths | 101 paths（保留 R4 历史行 + 追加 P19 复核注） |
| webui/README-webui.md | 37 paths、11 路由/11 视图、stats 占位 | 101 paths、25 视图、stats 真实计数、补 /cover 行 |
| docs/openapi.json | 94 paths / 0 cover（交付物过期） | **已用 `scripts/export_openapi.py` 重新导出 = 101 / 111 / 49，含 cover 7 paths** |

### 1.3 修正的隐藏数字：MCP「已映射 50 个业务端点」
原文档口径「透传 94 个端点中已映射 50 个」经逐一核对 mcp/server.py 55 个工具：除 `af_register_agent`（本地自描述，不发主控请求）外，**54 个工具映射 54 个主控端点**（含 system/health|stats|embedded-dsh、zhihu 5 个、collector 7 个、account-bridges 7 个等）。已在根 README、02-API、06-MCP集成 三处统一改为 **54**。

---

## 2. cover 反钓鱼伪装文档补入清单（本次新增，纯文档）

1. **根 README.md**：§3 功能主线表新增 P19 行；§5 API 摘要表新增「反钓鱼伪装 P19」行（7 paths / 8 ops）；§2 架构图 94→101；§4.4 入口表 25 页面（含 /cover）；§7 目录 models 24 张表、webui 25 路由、tests 479 用例；§8 479 passed；§9 版本与变更追加 P19 交付说明。
2. **docs/README.md**：索引 00 行追加 P19；05 行 25 页面；07 行 479；openapi 行 101/111/49；核心速览新增「反钓鱼伪装（P19）」条目。
3. **docs/00-项目概述.md**：技术栈 24 表/25 路由/479 用例；功能主线表新增 P19 行（含合规边界）。
4. **docs/02-API.md**：新增 **§2.2 反钓鱼伪装端点**（7 paths / 8 ops，全部 require_admin，toggle/delete confirm+reason 审计，脱敏落库说明）；§4 MCP 对应关系补 cover 未映射说明。
5. **docs/05-WebUI.md**：页面清单 25 路由 + 新增 /cover 伪装账号行。
6. **docs/06-MCP集成.md**：101 端点口径 + cover 并入「未映射 MCP 工具」清单。
7. **docs/07-测试.md**：pytest 479 / 32 文件 / test_p19_cover 17 条行 / 历史基线追加 / openapi 101/111/49。
8. **docs/MCP端核对表.md**：当前权威口径 101 + P19 复核注。
9. **webui/README-webui.md**：101 paths、25 视图、/cover 行、stats 真实计数。
10. **docs/openapi.json**：重新导出（101/111/49，含 cover 7 paths，UTF-8 无 BOM）。
11. **docs/严格验收R5_文档幻觉.md**：追加 §5 P19 后补充注（历史快照标注当前权威数字）。

既有文档 `docs/反钓鱼伪装验收_队C.md`（2026-09-17，53/53 断言、专项 17、全量 479）**已覆盖 cover 后端/前端/回归验收，无需修改**，仅被上述文档引用。

---

## 3. 历史报告一致性

- `docs/反钓鱼伪装验收_队C.md`：当前状态（479）✅ 一致。
- `docs/验收报告_端到端功能验收.md`（P10，2026-09-12）：14 项 63/63、pytest 112 passed——P10 时点历史快照，与其验收日期自洽，保留。
- `docs/严格验收R1~R5_*.md`：各轮次历史快照（446→451→458→462→479 递增），数字均与当时实跑一致；R5 结论「文档已全部一致」已被 P19 合入超越，已追加 §5 补充注指向本报告。R1–R4 报告正文不改写（沿既定「历史快照 + 复核注」约定）。
- `tests/TEST_REPORT.md` / `FULL_TEST_REPORT.md`：测试报告快照，保留。
- CHANGELOG.md：最新条目为 [1.0.0] 2026-09-12，未记 P19；因版本号未提升（仍 v1.0.0），未在 CHANGELOG 虚构条目，建议（见 §4）。

---

## 4. 待修清单（非文档/建议项，记录不修改）

| # | 项 | 类型 | 说明 |
|---|---|---|---|
| 1 | CoverView toggle 开关 UI 无法生效（P1-01） | 代码（前端） | 队C 验收报告已列：`client.js coverToggle` 不发 confirm+reason body → 后端 403 `confirm_required`，UI 开关必然失败。**合入前建议修复**（开关走确认弹窗）。 |
| 2 | 接触记录「锁定」过滤参数（P2-01） | 代码（前端） | 前端发 `locked`，后端参数 `escalated`，过滤静默失效。 |
| 3 | 暴露清单解包路径（P2-02） | 代码（前端） | CoverView 未按 `res.exposure.checklist` 解包，回退客户端掩码兜底。 |
| 4 | CHANGELOG 未记录 P19 | 文档建议 | 版本未提升时可在 [1.0.0] 内追加「后续新增」小节或待下次 bump 一并记录（未擅改，避免虚构版本条目）。 |
| 5 | MCP 无 cover 工具 | 设计取舍 | cover 端点仅 REST/WebUI 可用（与 persona/knowledge 等一致），若需 agent 驱动可在后续轮次补 `af_cover_*`。 |
| 6 | R1–R4 严格验收报告正文 | 历史快照 | 保留原文（含当时 462/94/22 等数字），均已在 R5/R6 复核注中指向当前权威数字，不改写历史。 |

---

## 5. 复验方法（本次执行）

1. 远程 `pytest --collect-only -q` → **479 tests collected**（32 文件，2.49s；`.pytest_cache/v/cache/nodeids` 483 行一致）。
2. 线上 `/openapi.json` 101/111/49 + cover 7 paths/8 ops；与重新导出的 `docs/openapi.json` 完全一致。
3. 线上 `system/stats`（admin key）含 cover_identities/contact_events 字段；`/ui/cover` 200。
4. DB 只读：24 表、cases=80、evidence_packages=103。
5. 修复后复扫：核心文档旧数字 0 命中；新增章节/行已逐行核对渲染无 `n 残留。

## 6. 报告落盘

- 远程：`docs/验收_文档幻觉.md`（本文件）
- 本地：`E:\dsh3080工作区存放位置\dsh_gzq1\验收_文档幻觉.md`
- 全部修改 UTF-8 无 BOM；未修改代码；未重启主控（9200 全程 health 200）。

> 结论：**文档幻觉核验通过**——核心数字（479 / 101·111·49 / 24 表 / 9 job / 55 工具 / 25 路由 / 80 cases / 103 evidence）与线上完全一致，cover 反钓鱼伪装全套文档已补齐，交付物 docs/openapi.json 已同步，历史报告一致（轮次快照 + 复核注约定）。
