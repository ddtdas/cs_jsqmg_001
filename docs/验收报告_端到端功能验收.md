# 验收报告 · 端到端功能验收（P10）

> 项目：金丝雀蜜罐 CanaryGuard AntiFraud v1.0.0 ｜ 验收人：qa-tester
> 日期：2026-09-12 ｜ 验收方式：**真实运行**（启动真实主控，模拟用户/agent 全流程实测）
> 可重复脚本：`tests/e2e_acceptance.py`（`63 项断言，全绿`）；证据 JSON：`scripts/_e2e_results.json`

---

## 1. 验收环境与启动

| 项 | 值 |
|---|---|
| 主控启动命令 | `.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 9200`（等效 `start.bat` / `python -m app.run`） |
| 健康状态 | `GET /api/v1/system/health` → `{ok:true, data:{status:ok, version:1.0.0, db:true, llm:not_configured, zhihu:idle}}` |
| bootstrap key | `data/bootstrap_admin_key.txt`（`af_admin_*`，17 字符） |
| 测试基线 | t1–t11 全部完成；pytest 全量 **112 passed**（复跑确认仍绿） |

## 2. 验收结论（摘要）

- **14 项流程 + 合规检查：63/63 项断言全部通过，0 失败。**
- 未发现产品缺陷（唯一修复项为验收脚本自身断言的 5 处口径修正，见 §5，与产品实现无关）。
- 合规红线逐项检查通过（§6）。

## 3. 逐项验收明细

### 0. 启动与健康 ✅
【操作】venv 启动主控，GET /system/health。
【预期】200，status=ok。
【实测】200，`version=1.0.0, db=true, llm=not_configured（D5 降级提示）, zhihu=idle`。
【结论】**通过**。

### 1. 认证链路 ✅
【操作】POST /auth/bootstrap-info（公开）→ 带 key GET /system/stats → 无 key GET /system/stats。
【预期】bootstrap-info 返回 initialized；带 key 200；无 key 401 封包 `{ok:false,error:{code}}`。
【实测】`initialized=True`；带 key **200**；无 key **401 `missing_api_key`** 统一封包。
【结论】**通过**。
> 注：bootstrap-info 实现为 POST（P0 既定），任务书写 GET，已按实现口径验收。

### 2. 话术检测（R2）✅
【操作】POST /scan/text 输入 5 类真实诈骗话术（杀猪盘/刷单/冒充银行风控/投资理财/中奖缴费）+ 3 条正常语料；GET /scan/hits。
【预期】诈骗话术 verdict≠normal 且有 hits 与 grade；正常语料 verdict=normal；hits 有记录。
【实测】5 条诈骗话术全部 `verdict=suspicious`，grade L2/L3，hits 1–3 条，检测全部入链（detection_ids=5）；3 条正常语料全部 `verdict=normal`；/scan/hits 返回 20 条记录。
【结论】**通过**。

### 3. 五级分级（R4）✅
【操作】GET /grading/levels；GET /grading/explain/{detection_id}。
【预期】L1–L5 定义齐全；explain 返回结构化证据链（grade_reason）。
【实测】levels 返回 5 档（L1–L5，含 min_score/desc）；explain 对 L3 检测返回 4 条可解释证据链。
【结论】**通过**。

### 4. 蜜饵全生命周期（R1）✅
【操作】POST /traps/generate-draft → POST /traps/{id}/deploy → POST /traps/{id}/monitor → GET /traps/{id}/check-hit（蜜饵文本改写变体）→ 状态查询 → GET /traps。
【预期】草稿不自动发布（draft，含 hitl_hint）→ active → monitored → 改写变体踩饵命中 → 命中即退役（retired）。
【实测】`draft（disguise=100.0，fingerprint + hitl_hint 齐备）→ active → monitored → check-hit hit=True（overlap=0.605 命中）→ status=retired`；/traps 列表 7 条状态正确。
【结论】**通过**（HITL：生成草稿全程未产生任何自动发布动作）。

### 5. 账号速查（R3）✅
【操作】POST /accounts/check（公开信号含 reported_count/trap_hit_ids）→ GET /accounts/{url_name} → GET /accounts/{id}/timeline。
【预期】返回风险等级+证据；画像可查；时间线可查。
【实测】`risk=red`（信号评分触发）；画像 `url_name=e2e_suspect_001` 落库；timeline 200。
【结论】**通过**。

### 6. 证据包（R5）✅
【操作】POST /evidence/build（关联 2 条检测）→ GET /evidence/{pkg_id} → POST /freeze → GET /export（json+text）→ GET /report-template（96110/platform/pibyao 三类）。
【预期】构建成功；verify intact；冻结 frozen；导出可用；模板内容合规（个人署名、无冒充官方、无手机号明文）。
【实测】`pkg_id=7`；verify `{intact:True, nodes:2}`；freeze `frozen=True`；export json/text 均 200；三类模板 283/166/180 字节，均含"个人"署名、不含 138 手机号。
【结论】**通过**。

### 7. 案例库（R6）✅
【操作】POST /cases/publish（redacted_payload 含手机号）→ 脱敏后重发 → GET /cases → GET /cases/graph。
【预期】含敏感 → 422 拒绝；脱敏后 → 成功；列表/图谱可查。
【实测】含 `13812345678` → **422 `sensitive_data`**；正则脱敏（`138****5678`）后发布成功 case_id=6；列表 total=6；图谱 `by_tag=[(fake_investment,6),(pig_butchering,1)]`。
【结论】**通过**（脱敏强制红线实测生效）。

### 8. 告警（L3+ 闸控）✅
【操作】L3 检测触发后 GET /alerts?unread_only=true → POST /alerts/{id}/read → 复查。
【预期】出现未读告警；read 生效。
【实测】unread=19（L3+ 检测自动产生）；标记 alert_id=22 后 `unread=0`。
【结论】**通过**。

### 9. 知乎通道（P2）✅
【操作】GET /zhihu/channels → POST /zhihu/import（粘贴诈骗文本）→ POST /scan/inbox → GET /scan/inbox。
【预期】四通道健康结构；导入→pending；inbox 批量消费；结果可查。
【实测】channels=[ch-a,ch-b,ch-c,ch-d] 四通道；导入 `ingested=1, duplicates=0`；inbox `processed=1, verdict=suspicious, rule_score=22.0`；结果列表 4 条。
【结论】**通过**。

### 10. 事件流（P6.5）✅
【操作】GET /events（分页）→ GET /events/{id} → GET /events/999999。
【预期】事件列表/单查/404 封包。
【实测】total=51、单查 id=51 命中、不存在 id 返回 404 统一封包。
【结论】**通过**。

### 11. 配置（P6.5）✅
【操作】GET /config → PUT /config（无 confirm）→ PUT /config（confirm+reason）→ GET 复查 overrides。
【预期】合成配置且不泄露密钥；无 confirm 403；带 confirm+reason 成功并审计；overrides 生效。
【实测】GET 含 llm/degrade/zhihu/speech/trap/grading/overrides，`api_key_set` 布尔化不露明文；无 confirm → **403 `confirm_required`**；带 confirm+reason → 成功（gate_log_id=4，写 configs + gate_logs 审计）；复查 `overrides.grading.l3_min=9.5` 生效。
【结论】**通过**。

### 12. WebUI（P7）✅
【操作】GET /ui/ → GET / → GET /ui/some/deep/route → GET /ui/assets/not-exist-xyz.js。
【预期】/ui/ 200 SPA；/ 重定向 /ui/；SPA fallback；资源 404 不吞。
【实测】/ui/ **200**（text/html）；/ **307 → /ui/**；深层路由 SPA fallback 200；不存在资源 404（不吞）。
【结论】**通过**（webui/dist 已由主控静态托管）。

### 13. MCP（P8）✅
【操作】以 venv 加载 mcp/server.py，经真实 HTTP MasterProxy 调用 4 个工具：af_health / af_scan_text / af_list_traps / af_list_events。
【预期】真实主控链路可用。
【实测】`af_health` → status=ok version=1.0.0；`af_scan_text`（杀猪盘话术）→ verdict=suspicious grade=L3；`af_list_traps` → 7 条；`af_list_events` → total=52。
【结论】**通过**（另 P8 集成测试 test_mcp_mount.py 全绿佐证）。

### 14. 完整反诈工作流串联 ✅
【操作】导入诈骗文本 → 批量扫描 → 分级解释 → 告警 → 建证据包 → 冻结 → 举报模板 → 脱敏发布案例 → 图谱 → 事件流，一条龙走通。
【实测链】（本次运行）`import ingested=1 → scan/inbox processed=1 → detection_id=53 verdict=suspicious grade=L3 → explain 证据链 4 条 → 未读告警 5 条 → evidence pkg=8 → freeze → 96110 模板 OK → case 脱敏发布成功 → graph total=7 → events total=55`。
【结论】**通过**。

### 流程示意（全链路）

```
CH-D 手动导入 ──POST /zhihu/import──▶ detections(pending)
        │
        ▼
POST /scan/inbox ──批量消费──▶ 话术判定(verdict/rule_score) ──▶ GradingService.finalize
        │                                                          │
        ▼                                                          ▼
GET /grading/explain/{id}（证据链）                          L3+ 闸控 → alerts(unread)
                                                                 │
        ┌─────────────────────────────────────────────────────────┘
        ▼
POST /evidence/build → freeze → GET /report-template(96110/platform/辟谣)
        │
        ▼
POST /cases/publish（含敏感 422 拒绝 → 脱敏后入库）→ GET /cases → GET /cases/graph
        │
        ▼
GET /events（全部动作事件流落库，可追溯）
```

## 4. 14 项结果一览

| # | 验收项 | 结果 |
|---|---|---|
| 0 | 启动与健康 | ✅ 通过 |
| 1 | 认证链路 | ✅ 通过 |
| 2 | 话术检测（R2） | ✅ 通过 |
| 3 | 五级分级（R4） | ✅ 通过 |
| 4 | 蜜饵全生命周期（R1） | ✅ 通过 |
| 5 | 账号速查（R3） | ✅ 通过 |
| 6 | 证据包（R5） | ✅ 通过 |
| 7 | 案例库（R6） | ✅ 通过 |
| 8 | 告警（L3+ 闸控） | ✅ 通过 |
| 9 | 知乎通道（P2） | ✅ 通过 |
| 10 | 事件流（P6.5） | ✅ 通过 |
| 11 | 配置（P6.5） | ✅ 通过 |
| 12 | WebUI（P7） | ✅ 通过 |
| 13 | MCP（P8） | ✅ 通过 |
| 14 | 全链路串联 | ✅ 通过 |
| 附 | 合规红线逐项检查 | ✅ 通过（§6） |

**合计：14/14 项通过（脚本断言 63/63 全绿）。**

## 5. 验收期间处理的问题

| # | 问题 | 性质 | 处置 |
|---|---|---|---|
| 1 | 脚本对 `/grading/levels` 按字典断言，实际返回列表 | 脚本口径 | 修正断言为 L1–L5 集合校验（实现正确） |
| 2 | 脚本对 `/traps` 按 `{traps:[...]}` 断言，实际返回列表 | 脚本口径 | 修正断言（实现正确） |
| 3 | 脚本对 `/evidence/{id}` 的 `verified` 按布尔断言，实际为校验报告 dict（intact 字段） | 脚本口径 | 修正断言为 `verified.intact=True`（实现更细） |
| 4 | 脚本对告警 read 按 `read=True` 断言，实际返回行 `unread=0` | 脚本口径 | 修正断言（实现正确） |
| 5 | 脚本对 /zhihu/import 按 `written` 断言，实际键为 `ingested`；scan/inbox items 为分析结果无 status 字段 | 脚本口径 | 修正断言；导入文本加时间戳保证可重复运行（去重按 sha256，属设计行为） |
| 6 | `/auth/bootstrap-info` 实现为 POST（任务书写 GET） | 文档口径 | 按实现验收并在此注明 |

> 以上全部为**验收脚本/任务书口径**与实现不一致，**未发现产品功能缺陷**；修复均在 `tests/e2e_acceptance.py` 内，未改动任何产品代码。

## 6. 合规红线检查结论（D3/D7）

| 检查项 | 结果 | 证据 |
|---|---|---|
| 蜜饵只产草稿不自动发布（HITL） | ✅ | generate-draft 返回 draft + hitl_hint + fingerprint_note；生命周期 deploy 需显式调用 |
| 举报模板个人署名、不冒充官方 | ✅ | 96110/platform/pibyao 三类模板均含"个人"，不含"已受理/已冻结"等官方措辞，不含手机号明文 |
| 案例发布强制脱敏 | ✅ | 含手机号载荷发布 422 `sensitive_data` 拒绝入库；脱敏后成功 |
| 敏感操作双确认审计（D6） | ✅ | /config PUT 无 confirm 403；带 confirm+reason 写 gate_logs 哈希链 |
| cookie 注入受控（不代登录） | ✅ | /zhihu/channels/{id}/login 需 admin key（无 key 401）；cookie 加密存储明文不回显（P2 测试覆盖） |
| 认证闸控 | ✅ | 受保护端点无 key 401 统一封包 |
| 降级链（D5） | ✅ | LLM 未配置时主控正常运行（health not_configured），话术判定走纯规则（P3 测试覆盖） |

**合规检查结论：全部通过，无越界行为。**

## 7. 遗留问题清单

- **无产品缺陷遗留、无阻塞项**。
- 已知规划内说明（非缺陷）：后端暂未提供 `/ws/events` WebSocket 与 `/events` REST 的 WS 推送（WebUI 以 5s 轮询降级，P7 文档已注明）；LLM 复核/embedding 精排依赖用户配置 AF_LLM_*（未配置时 D5 降级纯规则，功能不瘫）。

## 8. 证据与复跑方法

```bash
# 复跑（主控须已启动）
.venv\Scripts\python.exe tests\e2e_acceptance.py          # 63 项断言，全绿
.venv\Scripts\python.exe -m pytest -q                     # 全量 112 passed
```

- 证据 JSON：`scripts/_e2e_results.json` ｜ 运行输出：`scripts/_e2e_run_output.txt` ｜ 测试报告：`tests/TEST_REPORT.md`