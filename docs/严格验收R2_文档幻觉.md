# 严格验收 R2 · 文档幻觉核实与修复报告（复验）

> 任务：t7 ｜ 角色：doc-verifier（文档幻觉核实员：核实+修复）
> 日期：2026-09-17 ｜ 项目：金丝雀蜜罐 CanaryGuard AntiFraud v1.0.0（远程 192.168.10.110，`C:\Users\Administrator\Desktop\知乎黑客松\金丝雀蜜罐`）
> 输入：R1 修复已合入（fix-engineer t4，451 passed）、R1 文档修复（t3，表22/job9/工具55/openapi94）已落盘
> 方法：读 docs/ + 读代码（R1 改动 7 文件）+ 调线上 API（9200，admin key）+ 远程实跑 pytest；只改文档（UTF-8 无 BOM），不修代码、不重启主控。

---

## 0. 结论摘要

R1 修复新增 5 条专项回归（`tests/test_r6_r1fixes.py`），**pytest 从 446 → 451**；R1 修复引入了
**4 项代码行为变化**（L5 自动升级 + trap_hit_escalated、evidence 重复建包 409、cases 幂等/409、
start 单实例互斥），其中 **2 项（evidence 409 / cases 409）直接改变 REST/MCP 错误语义**，必须同步文档；
另外发现 **2 处历史文档（全流程检查报告/下一轮修改方案）仍在描述已被 R1 修复的 P0**，需标注状态。

| 核实项 | 文档原声明 | 实测值（R2） | 结论 |
|---|---|---|---|
| pytest 全量 | README 446 / docs-README 446 / 00 446 / 07 446（R1 时点） | 远程实跑 **451 passed**（29 文件，57.96s，collect 451） | ❌ 过期 → 已改 4 处 + 07 表格补 test_r6_r1fixes |
| OpenAPI | 94 paths / 103 ops / 46 schemas | 线上 94/103/46（R1 无 API 变更，与 R2 实测一致） | ✅ 一致（无需改） |
| 表数 / job / 工具 / 路由 | 22 表 / 9 job / 55 工具 / 24 路由 | 22 / 9 / 55 / 24（R1 未动 schema/调度器/工具/路由） | ✅ 一致 |
| 验收报告序列 | 408→429→446 | 446 → **451**（R1 修复新增 test_r6_r1fixes 5 条） | ⚠️ 序列需追加 451 → 已改 07-测试 |
| evidence 重复建包 | docs/02-API 未描述 | 服务层/API/MCP 重复 build → **409 evidence_exists** | ❌ 缺功能说明 → 已补 02-API/06-MCP/README/00 |
| cases 幂等发布 | docs 未描述 | 同载荷幂等返回、异载荷 **409 case_exists** | ❌ 缺功能说明 → 已补 02-API/06-MCP/00 |
| 敏感工具闸控清单 | README §6.3 / 06-MCP §4.2 仅 3 个工具 | 代码实际 4 个（af_freeze_evidence 亦带 confirm+reason，evidence.freeze 审计） | ❌ 遗漏 → 已补 af_freeze_evidence |
| 错误码表 | 02-API 未列 409 | trap_retired/trap_disabled/already_frozen/evidence_exists/case_exists 均为 409 | ❌ 缺 409 → 已补错误码表 |
| 主动防御自动触发 | 全流程检查报告/下一轮修改方案：P0「grading 不自动 analyze」 | R1 已落地（grading `_auto_soc_analysis`，L3/L4/L5 自动触发+回写） | ❌ 历史文档未更新 → 已标注「已由 R1 修复」 |
| 重复案例提示 | 三轮回环/第1轮复验/第3轮计划：P2「同 det 重复案例发布无提示」 | R1 P1-4 已落地（幂等/409） | ❌ 历史文档未更新 → 已标注 |

---

## 1. 已修复（文档，共 11 个远程文件 + 1 个本地交接文档）

### 1.1 根 `README.md`
| 位置 | 原内容 | 改后 |
|---|---|---|
| §7 目录 | `tests/ # pytest（446 用例）` | `pytest（451 用例）` |
| §8 测试 | **446 passed**（28 个测试文件…） | **451 passed**（29 个测试文件，… + 严格验收 R1 修复回归 test_r6_r1fixes，含真实主控集成） |
| §3 R1 功能行 | 踩饵检测（…P3-1） | 追加「命中即退役并联动检测升级 L5（R1 修复：trap_hit_escalated 事件）」 |
| §4.3 start.bat | 三态描述 | 追加「R1 修复（P1-5）：单实例互斥——按 9200 监听 PID 检测 + 清理僵尸实例（保护监听父进程，CIM 不可用跳过）」 |
| §6.3 敏感操作表 | 3 个工具（retire/publish/config_update） | 补 `af_freeze_evidence`（POST /evidence/{id}/freeze，confirm+reason + GateLog）；并注明 cases 幂等/409、二次冻结 409 |

### 1.2 `docs/README.md`（索引）
- 07-测试 行：「全量测试（446 passed）」→「（451 passed）」。

### 1.3 `docs/00-项目概述.md`
| 位置 | 原内容 | 改后 |
|---|---|---|
| §3 测试 | pytest（446 用例，28 个测试文件…） | pytest（451 用例，29 个测试文件，… + 严格验收 R1 修复回归） |
| §4 R1 | 命中即退役防反查；改写 5-10 字仍召回 | 追加「命中即退役防反查并联动检测升级 L5（R1 修复：trap_hit_escalated）」 |
| §4 R4 | L3+ 才告警 | 追加「L3+ 自动触发社工库核查（R1 修复）」 |
| §4 R5 | 冻结/导出/举报模板 | 追加「同一检测重复建包 409 evidence_exists（R1 修复）」 |
| §4 R6 | 双层脱敏→入库→查询+图谱 | 追加「同一检测仅一个案例（同载荷幂等返回、异载荷 409 case_exists，R1 修复）」 |

### 1.4 `docs/02-API.md`
| 位置 | 原内容 | 改后 |
|---|---|---|
| §1 错误码 | 401/403/404/422/429/500（无 409） | 补 `409 conflict`（trap_retired/trap_disabled/evidence_exists/case_exists/already_frozen 等，R1 新增 evidence_exists/case_exists） |
| evidence build | 构建证据包 | 追加「同一检测重复建包 → 409 evidence_exists（R1 修复）」 |
| evidence freeze | 冻结（哈希链锁定） | 追加「confirm+reason 双确认；二次冻结 409 already_frozen」 |
| cases publish | 脱敏校验后入库（敏感操作） | 追加「同一检测仅一个案例：同载荷幂等返回、异载荷 409 case_exists（R1 修复）」 |

### 1.5 `docs/06-MCP集成.md`
- §4.2 敏感操作（D6 闸控）：工具清单 **3 → 4 个**，补 `af_freeze_evidence`（冻结证据包，R2 复核补录），
  gate_logs action 补 `evidence.freeze`；追加 R1 行为补充：af_build_evidence 重复建包 409 evidence_exists、
  af_publish_case 同载荷幂等/异载荷 409 case_exists、af_freeze_evidence 二次冻结 409 already_frozen。

### 1.6 `docs/07-测试.md`
| 位置 | 原内容 | 改后 |
|---|---|---|
| §1 命令注 | 446 passed（约 57s） | 451 passed（R2 复核实测，约 58s） |
| §1 结果 | **446 passed**（28 文件） | **451 passed**（29 文件） |
| §1 基线演进 | …→ **446（知识库 P18，当前）** | …→ 446（知识库 P18）→ **451（严格验收 R1 修复回归 test_r6_r1fixes +5，当前）** |
| §2 表头 | 28 文件，R1 复核 | 29 文件，R2 复核 |
| §2 表格 | 28 行合计 446 | 新增 `test_r6_r1fixes.py | 5 | 严格验收 R1 修复回归…`，合计 **29 文件 / 451** |
| §2 注 | 446 collected/passed（57.03s） | 451 collected/passed（57.96s） |

### 1.7 `docs/全流程检查报告.md`（历史 P0 状态标注）
- 汇总表第 2 行与 §2 标题：标注「该 P0 已由严格验收 R1 修复落地（grading `_auto_soc_analysis`，
  test_r6_r1fixes 5 条回归全绿），原文保留作历史对照」。

### 1.8 `docs/下一轮修改方案.md`
- 问题清单 P0 行与验收标准第 1 条：标注「✅ R1 已修复（grading `_auto_soc_analysis`，见 严格验收R1_修复.md §1.2）」。

### 1.9 `docs/三轮回环总报告.md`
- §3 遗留 P2 第 1 行「同 det 重复案例发布无提示」：标注「✅ 已由严格验收 R1 修复（P1-4）：同载荷幂等返回、异载荷 409 case_exists」。

### 1.10 `docs/第1轮_复验报告.md`
- 汇总「P2 未实现（2 项）：B9、C5」：标注「⚠️ R2 复核注：B9 已由严格验收 R1 修复落地；C5 仍为观察项」。

### 1.11 `docs/第3轮_复杂化验收计划.md`
- P2 观察项「重复案例提示未实现」：标注「✅ 已由严格验收 R1 修复：cases.publish 同载荷幂等/异载荷 409 case_exists」。

### 1.12 本地 `交接-金丝雀蜜罐.md`（本地交接文档，非远程）
- 顶部追加 R2 复核注：MCP 36→55、pytest 112→451、表 14→22、openapi 67 paths→94/103/46、job 9 类、WebUI 24 路由/24 视图（历史正文保留）。

---

## 2. 核实通过（无幻觉）

| 项 | 声明 | 实测 |
|---|---|---|
| 线上 health / openapi | health 200；94 paths / 103 ops / 46 schemas | ✅ R2 实测一致（`{status:ok, db:true, llm:not_configured, zhihu:idle}`） |
| 22 表 / 9 job / 55 工具 / 24 路由 | R1 修复后文档 | ✅ 代码/线上核对一致（R1 未涉及 schema/调度/工具/路由变更） |
| R1 修复 5 条回归 | test_r6_r1fixes 5 条 | ✅ 已纳入全量（446+5=451），专项断言（L5 升级/证据 409/cases 409）与 R1 修复报告一致 |
| e2e 63/63 | 端到端验收报告 | ✅ 与历史记录一致（未重跑，只读） |
| af_freeze_evidence 闸控 | （R2 补录后） | ✅ mcp/server.py 实测含 confirm+reason 参数；api/evidence.py `_require_confirm` 403/422 + gate_logs action=evidence.freeze |

---

## 3. 待代码修复（本任务范围外，交 fix-engineer / 运维）

1. **P2-8（R1 已记录）**：cases 表存量 1695 条重复案例清洗 + `UNIQUE(det_id)` 迁移（应用层已防新增，运维项）。
2. **P2-9（R1 已记录）**：evidence_packages 存量重复包清洗（[150]×9 等）。
3. **P2-5（R1 已记录）**：代码注释「14 张表」漂移（app/db.py:6、models/__init__.py）→ 实际 22 张（代码注释非文档，未改）。
4. **P2-6（R1 已记录）**：代码注释「8 类 job」漂移（app/main.py、scheduler.py）→ 实际 9 类。
5. 前端 BoardView 兜底字段 soc_hits 后端不存在（P2-7，防御式死分支，R1 已记录，非文档幻觉）。

---

## 4. 报告落盘

- 远程：`docs/严格验收R2_文档幻觉.md`（本文件）
- 本地：`E:\dsh3080工作区存放位置\dsh_gzq1\严格验收R2_文档幻觉.md`

> 全部修改均 UTF-8 无 BOM 写回；未修改任何代码；未重启主控（9200 全程在线，health 200）。
