# 金丝雀蜜罐 · 需求汇总与代码核对基准（PRD-CONFORMANCE）

> 版本：v1（2026-09-16）| 基线：pytest 330 collected / health 200
> 目的：汇总本窗口全部用户需求 → 作为**代码核对基准**（子代理只读核对：需求是否落地？
> 是否冲突？）→ 冲突项交修复队 → 我（调度）验收。**调度者不参与开发。**
> 项目根：C:\Users\Administrator\Desktop\知乎黑客松\金丝雀蜜罐

---

## 一、需求清单（R1-R13）

### R1 会话历史折叠
- 侧边栏"会话历史"只保留 **6 条检测记录**（检测 #id · 来源 · 分N），**不展开事件/蜜饵条目**
- 对应：App.vue recentSessions 逻辑（scanHits(6) 固定 6 条纯检测）

### R2 配置账号页面
- 新增"配置账号"页：调研哪些平台公共账号具备接入 AI 条件 + 搜索对应开源项目
- 确保账号**私信准确接入**系统，**展开反向侦察**；支持**微信/QQ**；支持**本地 agent 一键导入**
- 完成后向用户索要账号密码配置
- 对应：/ui/accounts-config + account-bridges API + ingest（detections pending→scan→grading→account_intel→supply_chain）

### R3 采集矩阵 + 爬虫优化
- 设计"矩阵"功能（平台×账号×策略 编排）
- 优化爬虫；**从 GitHub 寻找合适技术栈并嵌入集成**（非独立部署）
- 对应：/ui/collector-matrix + collector API + 平台适配器（zhihu/weibo/telegram/wechat/qq/rsshub）+ tech-stack 8 项 + APScheduler job

### R4 内嵌 DSH 干净独立（严格）
- **内嵌 DSH 必须是新的空白实例**（从 GitHub/npm 下载全新引擎），**不是连 3080**
- **隔离封装、换环境也能用**（别人下载打开即可，不依赖设备全局 3080/全局 home/全局插件）
- 了解其软件情况（引擎版本/profile 机制/端口/token 鉴权）
- 对应：dsh/ 目录（node_modules 0.1.5-rc.1 + 空白 dsh-home + 独立端口 3092 + start/stop + README）+ ConsoleView 默认 3092 + system/embedded-dsh

### R5 全面测试 + 社会工程学推理链
- 拉子代理**全面测试功能**；**完善推理链条**
- 搜索相关问题等教程；**分析案例**；**重构整个流程**；**保留交叉验证**；**双盲验证**
- 对应：se_analysis（24 因子：向量/技巧/SEADM/生命周期）+ DetailDrawer 社会工程学卡片 + 六队验证 + 双盲

### R6 其它方向深度验证
- 从**安全/性能/数据完整/MCP+WebUI/供应链采集/内嵌DSH**等其它方向验证
- 覆盖足够广；给出**修复方案和更新方案**；参考类似项目（ScamIntelli）+ 搭建可复用工作流
- 对应：六队验证报告 + 修复闭环（330 passed）+ docs/验证工作流指南.md + workflows/ 模板

### R7 找点事干交接（背景）
- 交接档案已备（交接-找点事干.md）；待办：实时平台抓取 + 版本统一 bump（金丝雀主线优先，未启动）

### R8 平台配置暂留空（账号凭据待用户提供）
- 用户选择"暂不提供，先演示/测试"；用模拟私信验证闭环后待真实凭据

### R9 模型路由规定
- 子代理/实现用 deepseek v4 flash（newapi）；调度用 glm5.3、用尽回退 flash（本会话路由）

### R10 主控与数据
- 金丝雀主控 9200（0.0.0.0）；内嵌 DSH 3092；全局 3080 不动；数据在项目内（db/af.db + dsh/dsh-home 空白）

### R11 一致性约束（贯穿全部）
- 统一封包 {ok,data}|{ok:false,error}；敏感操作 confirm+reason+GateLog；D1（WebUI/MCP 同内核）；
  D2（规则实锤不被 LLM 否定）；D5 降级链；D7 合规（只读自有/不自动发布/脱敏/加密）

### R12 合规红线
- HITL 不自动发布；不冒充官方；脱敏强制；cookie/凭据加密；限速；fence+降级

### R13 验收方式
- 至少 2 队双盲：队A 模拟人类操作（浏览器逐页点击，所有交互/UI 有意义且全显示）；
  队B 只读代码审计；结论互不可见交叉核对；修复后复验

---

## 二、代码核对任务书（交核对子代理）

对上述 R1-R13 逐条**只读核对**当前代码/配置/文档是否：
1. **已落地**（实现存在且可用，给出 文件:行为 证据）
2. **部分落地**（列出缺项）
3. **未落地**（缺失）
4. **冲突**（与其它需求/约定矛盾——重点！例如：端口冲突、鉴权口径不一、命名冲突、
   封包不一致、需求间语义矛盾、内嵌 DSH 与全局耦合、会话历史折叠与 DetailDrawer 冲突等）

核对范围（远程 192.168.10.110 项目根）：
- app/（api/services/models/main.py/db.py）
- webui/src/（views/components/router/App.vue/client.js/stores）
- mcp/server.py、dsh/（引擎/空白 home/启停/README）、docs/、tests/
- data/（表结构、迁移、gate_logs、加密落库抽查）

核对产出（双写）：docs/需求核对报告.md（远程）+ 本地 需求核对报告.md
格式：每条需求 → 【结论: 已落地/部分/未落地/冲突】+ 证据（文件:行）+ 差距描述 + 修复建议
最后给"冲突清单"（R1-R13 之间 + 与代码约定之间）按 P0/P1/P2。

## 三、冲突修复流程（我负责调度，不写码）
1. 核对报告 → 冲突清单落盘
2. 冲突修复任务交修复子代理（每冲突一条：文件:行 + 修复 + 专项验证 + 回归测试）
3. 全量 pytest 保持绿（≥330）→ 主控重启加载 → 浏览器快验
4. 最终验收报告

## 四、参考实现/工具
- 工作流模板：workflows/canary-full-verify.template.js
- 验证指南：docs/验证工作流指南.md
- 历史修复：R4/R5 修复报告（tests/test_r4_fix_repairs.py、test_r5_backend_fixes.py）