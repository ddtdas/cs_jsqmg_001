# 严格验收 R5 · 收尾文档幻觉终核报告

> 任务：t19 ｜ 角色：doc-verifier（文档幻觉核实员：核实+修复）
> 日期：2026-09-17 ｜ 项目：金丝雀蜜罐 CanaryGuard AntiFraud v1.0.0（远程 192.168.10.110，`C:\Users\Administrator\Desktop\知乎黑客松\金丝雀蜜罐`）
> 输入：R1–R4 四轮验收+修复全部合入、docs 四轮累计修复完成
> 方法：读 docs/（核心 10 篇 + R1–R4 严格验收报告全量）+ 读代码（scheduler/mcp/router/models）+ 调线上 API（openapi 指纹/stats/audit-verify，admin key）+ 远程实跑 pytest + 线上 DB 只读核查（PRAGMA/计数）；只改文档（UTF-8 无 BOM），不修代码、不重启主控。

---

## 0. 结论摘要

**文档终核通过：核心文档（README + docs/ 索引 9 篇）零残留旧数字，R1–R4 严格验收报告与当前状态一致（仅 1 处 R4 宏观推演细节需修正标注，已修）。**

| 核实项 | 目标值 | 实测值（R5） | 结论 |
|---|---|---|---|
| pytest 全量 | 462（若 t20 清理新增则更新） | **462 collected / 462 passed**（31 文件，60.98s） | ✅ 一致（t20 无新增用例） |
| OpenAPI | 94 paths / 103 ops / 46 schemas | 线上 94/103/46；CheckHitRequest props=['text','detection_id']（R3 代码激活） | ✅ 一致 |
| 表数 | 22 | 线上 sqlite_master 22 张 | ✅ 一致 |
| job 数 | 9 | scheduler.py add_job 9 个 | ✅ 一致 |
| MCP 工具 | 55 | mcp/server.py @server.tool 55 个 | ✅ 一致 |
| 路由/视图 | 24 | router 24 页面路由 / views 24 .vue | ✅ 一致 |
| cases | 80（UNIQUE 索引在线） | 线上 80 行、0 重复组、`idx_cases_det_unique` unique=1 | ✅ 一致 |
| evidence | 103（冻结 86 保留） | 线上 103 包、frozen=86 | ✅ 一致 |
| 核心文档残留旧数字 | 0 | 扫描 README+9 篇 docs：**0 命中**（446/451/458/80 paths/14 表/36 工具/25 路由等全部清除） | ✅ 无残留 |
| R1–R4 严格验收报告 | 与当前状态一致 | R1-R4 报告为各自轮次历史快照（446→451→458→462 递增），结论均已落盘 | ✅ 一致（1 处修正见 §1.2） |
| e2e 63/63 | 端到端验收 | 历史记录一致（只读未重跑） | ✅ 一致 |

---

## 1. 已修复（文档，共 2 处）

### 1.1 远程 `docs/严格验收R4_宏观推演.md`
- §3 第 70 行原写「cases 表当前**无 DB 级 UNIQUE**」（R4 推演时点结论）。
- **R5 终核修正**：R3 修复（t12）已加装 `idx_cases_det_unique` 并线上应用，R4/R5 两次 `PRAGMA index_list('cases')` 实测 unique=1 在线、cases 仍 80 行 0 重复；应用层 409 与 DB 层唯一索引为双保险。
- 处理方式：保留原文 + 追加 **R5 复核修正注**（历史快照不改写，标注当前事实）。

### 1.2 本地 `交接-金丝雀蜜罐.md`（本地交接文档，非远程）
- 顶部复核注追加 **🏁 五轮严格验收结论**：R1–R4 四轮闭环收敛（代码级 P0:0/P1:0、文档四轮修复 30+ 处、pytest 446→451→458→462、线上全部数字与文档一致、e2e 63/63、audit-verify valid）；遗留项均为 P2/P3 运维与设计取舍（见 R4 文档幻觉报告 §3）。

---

## 2. 核实通过（无幻觉，R5 终核）

| 项 | 声明 | 实测 |
|---|---|---|
| 线上 health / openapi / stats | health 200；94 paths / 103 ops / 46 schemas | ✅ R5 实测一致 |
| 核心文档零残留 | 无 446/451/458/80 paths/14 表/36 工具/25 路由/11 页 等旧数字 | ✅ grep 扫描 0 命中 |
| R1–R4 报告基线 | 446→451→458→462 递增 | ✅ 各轮报告内部一致，均为当时实跑数字 |
| R3 代码加载态 | R3 P0-1 已解决 | ✅ 线上 CheckHitRequest 含 detection_id |
| UNIQUE(det_id) | 在线 + 数据无损 | ✅ PRAGMA unique=1；cases 80 行 0 重复 |
| 存量清洗 | cases 1695→80、evidence 738→103（冻结 86） | ✅ 线上 DB 一致 |
| 22 表 / 9 job / 55 工具 / 24 路由 | — | ✅ 代码核对一致 |
| e2e 63/63 | 端到端验收 | ✅ 历史记录一致 |

---

## 3. 待代码/运维（非文档，供收尾交接）

最终遗留清单（均为 P2/P3 运维与设计取舍，不阻塞交付）：

1. **冻结重复证据包**（evidence 86 冻结中 10 组重复）：存证语义设计保留；全局唯一约束加装需人工决策（R2 P2-9 / R3 P1-A / R4 P1-A 延续）。
2. **代码注释漂移**：14 张表（实 22）、8 类 job（实 9）——R4 修复报告 §4 已列「修 6 处注释」项，若已执行请确认；若未执行属代码注释（非文档，不在本任务范围）。
3. **P2-13 运维**：scanned 滞留无归档、告警 unread 无自动已读、events/gate_logs 只增无归档。
4. **P2-14/P2-15 历史遗留**：IOC 提取噪声（QQ 正则命中银行卡）、knowledge import 并发 UNIQUE 竞态 500（建议捕获 IntegrityError 重试）。
5. **P2-11/P2-12 功能缺口**：cases 无更新/删除 API；evidence 无追加截图/补充材料机制。
6. **P3-6 理论竞态**：幂等判定非原子 check-then-act（跨进程，单实例互斥兜底）。
7. **沿用**：死代码 cfg_int/cfg_bool/strip_fence 等 7 处、4 处未引用 import。

---

## 4. 报告落盘

- 远程：`docs/严格验收R5_文档幻觉.md`（本文件）
- 本地：`E:\dsh3080工作区存放位置\dsh_gzq1\严格验收R5_文档幻觉.md`

> 结论：**文档已全部一致（五轮收尾达标）**——核心文档零残留旧数字，R1–R4 报告为一致的历史快照（1 处细节已修正标注），交接文档已补五轮验收结论。
> 全部修改 UTF-8 无 BOM；未修改代码；未重启主控（9200 全程 health 200）。

---

## 5. P19 反钓鱼伪装后补充注（2026-09-17 晚 · doc-audit3）

P19 反钓鱼伪装（cover）合入后，本节 §0 结论为 R5 时点快照；当前权威数字以 docs/验收_文档幻觉.md 为准：

- pytest **462 → 479**（+17 test_p19_cover）；OpenAPI 94 → **101 paths / 111 ops / 49 schemas**（+ /api/v1/cover 7 paths）
- 表 22 → **24**（+ cover_identities / contact_events）；WebUI 24 → **25 路由 / 25 视图**（+ /cover 伪装账号页）
- MCP 55 工具不变（cover 无 MCP 工具，仅 REST/WebUI）；cases 80 / evidence 103 不变。
