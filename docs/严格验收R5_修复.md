# 严格验收 R5 · 收尾修复报告（fix-engineer）

- 轮次：严格验收第 5 轮（收尾）· 修复（t20）
- 修复人：fix-engineer（修复工程师，据 t17/t18/t19 验收发现执行收尾清理）
- 修复对象：192.168.10.110 `C:\Users\Administrator\Desktop\知乎黑客松\金丝雀蜜罐`（9200 主控）
- 输入报告：docs/严格验收R5_代码调用.md（t17，18 项遗留清单精确到行）· 严格验收R5_宏观推演.md（t18）· 严格验收R5_文档幻觉.md（t19）
- 修复原则：最小改动、防御式、迁移幂等；不重启主控；不提交 git
- 范围：连续 4 轮遗留收尾——**7 处死代码 + 5 处未引用 import + 6 处注释漂移（18 项）**

---

## 0. 结论摘要

| 类别 | 清单项 | 处理 | 验证 |
|------|--------|------|------|
| 死代码（删除） | cfg_int / cfg_bool / strip_fence | ✅ 已删除（全库含测试 0 调用） | grep 0 残留 |
| 死代码（保留+注明） | make_reader / on_degraded / get_scheduler / job_snapshot | ✅ 保留并标注"测试专用"（被 test_r3/test_p3/test_p10/test_p2 引用） | 注释在盘 |
| 未引用 import | collector.ApiError / zhihu.ChannelUnavailableError / speech_engine Any+FENCE_OPEN/FENCE_CLOSE / desensitize.Any / trap_engine.Any | ✅ 已删除（5 处） | grep 0 残留 |
| 注释漂移 | db.py ×2、models ×2（14→22 张表）；main.py ×2、scheduler.py ×1（8→9 类 job） | ✅ 已修正（6 处位置） | grep 0 残留 |

**全量 pytest：462 passed，0 failed，60.79s（基线一致，零新增用例/零回归）；专项 121 passed；远程 py_compile 全绿；主控 health 200。**

---

## 1. 逐项清理明细

### 1.1 死代码 7 处

| # | 函数 | 位置 | 调用核查 | 处理 |
|---|------|------|----------|------|
| 1 | `cfg_int` | config_reader.py:39 | 全库（app+tests）0 调用（ripgrep 仅 1 命中=def 行） | ✅ 删除 |
| 2 | `cfg_bool` | config_reader.py:47 | 全库 0 调用 | ✅ 删除 |
| 3 | `make_reader` | config_reader.py:59 | 仅 tests/test_r3_backend_fixes.py:275/287 | ✅ 保留 + docstring 标注"测试专用（R5 收尾标注）：生产无调用方" |
| 4 | `on_degraded` | llm_provider.py:47 | 仅 tests/test_p3_speech.py:211 | ✅ 保留 + 标注"测试专用" |
| 5 | `strip_fence` | llm_provider.py:142 | 全库 0 调用（fence() 为实际路径） | ✅ 删除（含方法体，保留 fence()） |
| 6 | `get_scheduler` | scheduler.py:479 | 仅 tests/test_p10_collector.py:11/131 | ✅ 保留 + 标注"测试专用" |
| 7 | `job_snapshot` | scheduler.py:483 | 仅 tests/test_p2_engineering.py:199 | ✅ 保留 + 标注"测试专用（运维面板未接入 /system/stats）" |

### 1.2 未引用 import 5 处（全部仅 import 行，无其他引用）

| # | import | 位置 | 处理 |
|---|--------|------|------|
| 1 | `ApiError` | api/collector.py:18 `from ..utils import ApiError, ok` | ✅ 删除 ApiError，保留 ok |
| 2 | `ChannelUnavailableError` | api/zhihu.py:19 | ✅ 删除，保留 CHANNELS/ZhihuBridge |
| 3 | `Any` + `FENCE_OPEN/FENCE_CLOSE` | speech_engine.py:23/:30-31 | ✅ 删除（围栏由 llm_provider 内部处理） |
| 4 | `Any` | desensitize.py:17 | ✅ 删除 |
| 5 | `Any` | trap_engine.py:33 | ✅ 删除 |

### 1.3 注释漂移 6 处

| # | 位置 | 原 | 改 |
|---|------|----|----|
| 1 | app/db.py:6 | "全部 14 张表" | "全部 22 张表" |
| 2 | app/db.py:80 | 同上 | 同上 |
| 3 | app/models/__init__.py:1 | "全部 14 张表" | "全部 22 张表" |
| 4 | app/models/__init__.py:4 | "14 张表名清单" | "22 张表名清单" |
| 5 | app/main.py:5/:58 | "8 类 job" | "9 类 job" |
| 6 | app/services/scheduler.py:57 | "8 类 job" | "9 类 job" |

---

## 2. 验证证据

### 2.1 改动文件（11 个，推送远程并核字节）

| 文件 | 内容 | 远程字节 |
|------|------|----------|
| app/services/config_reader.py | 删 cfg_int/cfg_bool；make_reader 标注测试专用 | 2162 |
| app/services/llm_provider.py | 删 strip_fence；on_degraded 标注测试专用 | 18389 |
| app/services/scheduler.py | get_scheduler/job_snapshot 标注；注释 9 类 job | 21953 |
| app/api/collector.py | 删 ApiError import | 3775 |
| app/api/zhihu.py | 删 ChannelUnavailableError import | 5497 |
| app/services/speech_engine.py | 删 Any/FENCE_OPEN/FENCE_CLOSE import | 16096 |
| app/services/desensitize.py | 删 Any import | 16979 |
| app/services/trap_engine.py | 删 Any import | 23253 |
| app/db.py | 注释 22 张表 ×2 | 3756 |
| app/models/__init__.py | 注释 22 张表 ×2 | 25951 |
| app/main.py | 注释 9 类 job ×2 | 9082 |

### 2.2 残留归零实证（远程 grep）

- `cfg_int / cfg_bool / strip_fence`：**0 命中**（含 app + tests）
- `14 张表 / 8 类 job` 注释漂移：**0 命中**
- make_reader/on_degraded/get_scheduler/job_snapshot：仅剩 def 行 + 测试引用 + "测试专用"标注

### 2.3 专项 pytest（.venv）

```
test_r3_backend_fixes + test_p3_speech + test_p2_engineering + test_p10_collector +
  test_r6_r1fixes + test_r7_r2fixes + test_r8_r3fixes + test_p4_traps +
  test_p6_evidence + test_p2_zhihu + test_p5_grading
→ 121 passed in 15.03s（含被"测试专用"函数引用的 4 个测试文件全绿）
```

### 2.4 全量 pytest（.venv）

```
pytest -q → 462 passed, 0 failed, 1 warning in 60.79s
（基线 462 一致，零新增用例——收尾清理不改测试语义；无回归）
```

### 2.5 主控与约束

- 9200 health 200（`{"status":"ok","version":"1.0.0","db":true,...}`）；未重启主控；
  未提交 git；零数据改动；远程 py_compile（11 文件）通过。

---

## 3. 全量测试结果

**462 passed, 0 failed, 1 warning in 60.79s**（.venv Python 3.12.10 / pytest 9.1.1 / pytest-asyncio 1.4.0）
= R4 基线 462 一致（t20 为纯清理，未新增用例）；专项 121 passed；无回归。

---

## 4. 遗留交接（t18/t19 判定：全部非阻塞）

| 项 | 类别 | 说明 |
|----|------|------|
| 冻结重复证据包 10 组（86 冻结中） | P1 管理决策 | 存证语义设计保留，需团队人工决策（非脚本动作），不阻塞运行 |
| P2 运维清单 12 项 | P2 | 归档（scanned/告警/events）、cases 更新/删除 API、evidence 追加机制、IOC 噪声、knowledge import 竞态、hit_count 口径、LLM 背压告警等，供运行期排期 |
| #3132 证据包/case_ready | 观察窗 | job_evidence（~23:00 UTC）/job_case_publish（19:00 UTC）定时自动产出，候选集已实证 |
| events 非严格 append-only（历史删行 21） | P2 运维知情 | 建议明确 events 清理策略或保持只增 |

*报告完*
