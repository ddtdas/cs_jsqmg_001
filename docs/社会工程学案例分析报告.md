# 社会工程学案例分析报告（队3 · 案例分析工程师）

- **测试目标**：用真实诈骗案例跑通完整「社会工程学推理链」（se_analysis：攻击向量 / 心理技巧 / SEADM 阶段 / 诈骗生命周期 / 证据 / 置信度），产出可解释分析报告
- **测试对象**：远程主控 `192.168.10.110:9200`（项目根 `C:\Users\Administrator\Desktop\知乎黑客松\金丝雀蜜罐`）
- **测试时间**：2026-09-16
- **测试人员**：队3 案例分析工程师
- **样本集**：4 类典型诈骗 + 1 条扩展样本 + 2 条阴性对照 = 7 条

---

## 0. 重要前置发现（P1，需队长知悉）

> ⚠️ **9200 主控运行的是「未加载队2推理链」的旧代码进程**：
> - 9200 监听进程 PID 6608 / 2332 启动时间 **16:06:11~12**；磁盘推理链代码（`app/services/se_analysis.py`、`speech_engine.py`、`api/scan.py`、`api/detections.py`、`models/__init__.py`）更新时间为 **17:01:21~22** —— 代码更新晚于进程启动，且无 `--reload`。
> - 实测证据：`POST /scan/text` 响应**无 `se_analysis` 键**；`GET /detections/{id}/full` 的 detection 对象**无 `se_factors` 键**；生产库 `detections` 表 **PRAGMA 无 `se_factors` 列**。
> - 磁盘最新代码的 `ensure_migrations()` 会在启动时对老库 `ALTER TABLE detections ADD COLUMN se_factors`（在只读副本上已验证：迁移成功补列）。
> - 因此本报告分两轨呈现：**A 轨 = API 实测（运行主控实际行为）**；**B 轨 = 推理链预期输出（磁盘最新代码，只读加载 + 副本 DB 全链路验证）**。二者规则层（rule_match / rule_score）完全一致（9/16/31/0/0/0/0），差异仅在推理链层。
> - **建议动作**：由队长/主控方择机重启 9200（加载新代码 → 自动迁移补列 → 推理链上线）。队3 按约束未自行重启、未改代码。

其它结论速览：

| 项 | 结果 |
|---|---|
| 阳性识别 | case1~3 完整识别（suspicious，L2/L3）；case4、case5 规则层漏检（normal/L1） |
| 推理链预期 | case1~5 均有向量/技巧/阶段/生命周期输出（case4 向量为 unknown，见 P1-2） |
| 阴性对照 | neg1、neg2 均 `verdict=normal` / `L1`，主维度 unknown（neg1 心理技巧有轻微误报，见 P2） |
| 落库一致性 | 副本 DB + 新代码：`se_analysis` 响应 == `se_factors` 落库（consistent=true）✅ |
| LLM 状态 | `llm=not_configured`，`llm_verdict=null`，judge_confidence 为规则启发式 → rule-only 封顶 L3（设计如此，保守合规） |

---

## 1. 案例一：杀猪盘

### 【输入文本】
```
哥哥，我是做外汇的，最近有个稳赚不赔的内幕项目，你投 5 万试试，我带你，保证翻倍；现在名额就剩最后几个了，赶紧入金
```

### 【推理链输出】

**A 轨 · API 实测（运行主控）**：`detection_id=3060`，`verdict=suspicious`，`severity=medium`，`grade=L2`，`rule_score=9.0`，命中话术 1 条，无 `se_analysis`（旧进程）。

| 命中话术 | 分类 | 权重 |
|---|---|---|
| 稳赚不赔 | fake_investment | 9.0 |

**B 轨 · 推理链预期输出（最新代码，SEAnalyzer 确定性规则层）**：

```json
{
  "attack_vector": "baiting",
  "psych_techniques": ["urgency", "scarcity", "commitment"],
  "seadm_stage": "unknown",
  "scam_lifecycle": "groom",
  "evidence": [
    {"factor": "urgency",    "matched": "现在|最后",  "text": "现在名额就剩最后几个了，赶紧入金"},
    {"factor": "scarcity",   "matched": "名额|最后几个", "text": "现在名额就剩最后几个了，赶紧入金"},
    {"factor": "commitment", "matched": "稳赚|保证",  "text": "哥哥，我是做外汇的，最近有个稳赚不赔的内幕项目，你投 5 万试试，我带你，保证翻倍"},
    {"factor": "baiting",    "matched": "稳赚不赔",   "text": "哥哥，我是做外汇的，最近有个稳赚不赔的内幕项目，你投 5 万试试，我带你，保证翻倍"},
    {"factor": "persona",    "matched": "内幕",      "text": "哥哥，我是做外汇的，最近有个稳赚不赔的内幕项目，你投 5 万试试，我带你，保证翻倍"},
    {"factor": "groom",      "matched": "试试",      "text": "哥哥，我是做外汇的，最近有个稳赚不赔的内幕项目，你投 5 万试试，我带你，保证翻倍"}
  ],
  "confidence": 0.25
}
```

### 【推理链解读】
- **命中话术**：`稳赚不赔`（fake_investment 高权重 9.0）→ 规则层直接判定投资类诈骗话术。
- **攻击向量 = baiting（诱饵投放）**：以「内幕项目 + 稳赚不赔」为诱饵吸引入局，命中关键词 `稳赚不赔`（baiting 词表）。
- **心理技巧**：`urgency`（"现在…赶紧"限时施压）+ `scarcity`（"名额就剩最后几个"稀缺制造）+ `commitment`（"保证翻倍"承诺一致性诱导）——Cialdini 三原则叠加，符合杀猪盘收割话术特征。
- **SEADM 阶段 = unknown（缺陷）**：文本处于「利用/诱导投入」阶段（"投 5 万试试"），但 `哥哥` 未命中 relationship 词表（仅"哥哥姐姐/弟弟妹妹"），`试试` 只归入 groom 生命周期；exploitation 词表（垫付/操作/点链接/扫码/下载）未命中"投 5 万"。→ 阶段判定为空，**建议词表补充"投/入金/投资"类 exploitation 关键词**。
- **生命周期 = groom（培养信任/小试）**：`试试` 命中 groom——"先投 5 万试试"正是小额试水诱导动作。
- **置信度 0.25**（6 因子 / 24 全因子）：证据链可解释、覆盖合理，但 SEADM 缺失拉低覆盖度。
- **grade_reason 证据链**（A 轨）：`[{signal: rule_hit, weight: 9.0, note: "话术'稳赚不赔'命中（分类 fake_investment）"}]` → 规则分 9.0 < L3 阈值 10 → **L2（弱信号）**；无 LLM、无蜜饵、无账号信号，符合"单条低权重命中"定义。

### 【分级与建议】
- **判定**：诈骗（杀猪盘/投资类）——API 判定 suspicious / medium / L2；推理链预期向量 baiting + 三重心理技巧。
- **建议动作**：
  1. **告警**：L2 属弱信号，建议作为「观察/人工复核」而非直接阻断；若出现后续"入金/转账"话术则升级 L3+。
  2. **证据**：固定原文 + 命中话术（`稳赚不赔`）+ 推理链 evidence（稀缺/紧迫/承诺三因子）作为研判材料。
  3. **上报**：命中杀猪盘典型剧本（内幕项目→名额稀缺→保证翻倍），建议上报反诈线索，关联"外汇/内幕项目/名额"账号画像。

---

## 2. 案例二：刷单返利

### 【输入文本】
```
亲，兼职刷单了解一下，首单返 20 佣金，垫付 100 立返 120（骗术），先小单试试再大单，佣金翻倍
```

### 【推理链输出】

**A 轨 · API 实测**：`detection_id=3061`，`verdict=suspicious`，`severity=high`，`grade=L3`，`rule_score=16.0`，命中话术 2 条。

| 命中话术 | 分类 | 权重 |
|---|---|---|
| 刷单返利 | job_scam | 9.0 |
| 垫付 | job_scam | 7.0 |

grade_reason：`rule_hit(9.0) + rule_hit(7.0) + llm_confidence_high(2.0, judge 0.80)` = 18.0 → **L3**。

**B 轨 · 推理链预期输出**：

```json
{
  "attack_vector": "baiting",
  "psych_techniques": ["reciprocity"],
  "seadm_stage": "exploitation",
  "scam_lifecycle": "small_bait",
  "evidence": [
    {"factor": "reciprocity", "matched": "佣金|垫付", "text": "亲，兼职刷单了解一下，首单返 20 佣金，垫付 100 立返 120（骗术），先小单试试再大单，佣金翻倍"},
    {"factor": "baiting",     "matched": "兼职|刷单", "text": "亲，兼职刷单了解一下，首单返 20 佣金，垫付 100 立返 120（骗术），先小单试试再大单，佣金翻倍"},
    {"factor": "quid_pro_quo","matched": "佣金|垫付", "text": "亲，兼职刷单了解一下，首单返 20 佣金，垫付 100 立返 120（骗术），先小单试试再大单，佣金翻倍"},
    {"factor": "exploitation","matched": "垫付",     "text": "亲，兼职刷单了解一下，首单返 20 佣金，垫付 100 立返 120（骗术），先小单试试再大单，佣金翻倍"},
    {"factor": "groom",       "matched": "试试",     "text": "亲，兼职刷单了解一下，首单返 20 佣金，垫付 100 立返 120（骗术），先小单试试再大单，佣金翻倍"},
    {"factor": "small_bait",  "matched": "首单",     "text": "亲，兼职刷单了解一下，首单返 20 佣金，垫付 100 立返 120（骗术），先小单试试再大单，佣金翻倍"}
  ],
  "confidence": 0.25
}
```

### 【推理链解读】
- **命中话术**：`刷单返利`(9.0) + `垫付`(7.0)，均为 job_scam 分类 → 规则层双证据。
- **攻击向量 = baiting**（兼职/刷单诱饵）+ **quid_pro_quo**（垫付/佣金——"我给你垫付佣金、你返利"交易条件）双向量命中，baiting 为最终主向量。
- **心理技巧 = reciprocity（互惠）**："垫付 100 立返 120""首单返 20 佣金"——以小利换大额垫付，典型互惠式诱导。
- **SEADM = exploitation（利用）**：`垫付` 命中 exploitation，处于"要求目标执行动作"阶段。
- **生命周期 = small_bait（小额试水）**：`首单` 命中，+ `groom`（试试）——"先小单试试再大单"正是刷单诈骗从甜头到收割的标准推进。
- **置信度 0.25**；证据链 6 条全部可解释。
- **分级证据链**：双话术命中 16 分 + 规则启发式置信度 0.8（+2）→ 18 分 ≥ L3 阈值 → **L3（多信号/高权重话术命中）**。规则分足够，即使无 LLM 也已触发 L3（rule-only 封顶）。

### 【分级与建议】
- **判定**：诈骗（刷单返利）——suspicious / high / L3。
- **建议动作**：
  1. **告警**：L3 按系统设计进入正式告警闸控（当前主控已生成 alert，`/detections/3061/full` alerts=1）。
  2. **证据**：双话术命中 + 推理链（互惠/垫付/小额试水）证据链完整，可直接入告警正文。
  3. **上报**：刷单返利为高频高损骗局，建议上报并沉淀「垫付-返利-大单」剧本特征到话术词库/案例库。

---

## 3. 案例三：冒充公检法

### 【输入文本】
```
我是XX市公安局的，你涉嫌洗钱，账户将被冻结，请把钱转到安全账户配合调查，马上操作，否则冻结
```

### 【推理链输出】

**A 轨 · API 实测**：`detection_id=3062`，`verdict=suspicious`，`severity=high`，`grade=L3`，`rule_score=31.0`，命中话术 4 条。

| 命中话术 | 分类 | 权重 |
|---|---|---|
| 冒充公检法 | impersonation | 9.0 |
| 安全账户 | impersonation | 8.0 |
| 涉嫌洗钱 | impersonation | 8.0 |
| 配合调查 | impersonation | 6.0 |

grade_reason：4×rule_hit(9+8+8+6) + llm_confidence_high(2.0, judge 1.00) = 33.0 → **L3**。

**B 轨 · 推理链预期输出**：

```json
{
  "attack_vector": "pretexting",
  "psych_techniques": ["urgency", "authority"],
  "seadm_stage": "exploitation",
  "scam_lifecycle": "withdraw_block",
  "evidence": [
    {"factor": "urgency",     "matched": "马上",     "text": "我是XX市公安局的，你涉嫌洗钱，账户将被冻结，请把钱转到安全账户配合调查，马上操作，否则冻结"},
    {"factor": "authority",   "matched": "公安",     "text": "我是XX市公安局的，你涉嫌洗钱，账户将被冻结，请把钱转到安全账户配合调查，马上操作，否则冻结"},
    {"factor": "pretexting",  "matched": "公安|安全账户", "text": "我是XX市公安局的，你涉嫌洗钱，账户将被冻结，请把钱转到安全账户配合调查，马上操作，否则冻结"},
    {"factor": "exploitation","matched": "操作",     "text": "我是XX市公安局的，你涉嫌洗钱，账户将被冻结，请把钱转到安全账户配合调查，马上操作，否则冻结"},
    {"factor": "withdraw_block","matched": "冻结",   "text": "我是XX市公安局的，你涉嫌洗钱，账户将被冻结，请把钱转到安全账户配合调查，马上操作，否则冻结"}
  ],
  "confidence": 0.208
}
```

### 【推理链解读】
- **命中话术**：4 条 impersonation 话术全中，rule_score 31 分——规则层最强样本。
- **攻击向量 = pretexting（借口/冒充）**：以「公安 + 安全账户」编造权威身份与事由（涉嫌洗钱），命中 pretexting 词表 2 个关键词。
- **心理技巧**：`authority（权威）`——冒充国家机关制造威慑；`urgency（紧迫）`——"马上操作，否则冻结"制造时间压力，恐惧驱动立即服从。
- **SEADM = exploitation（利用）**：`操作` 命中——要求立即执行转账动作。
- **生命周期 = withdraw_block（提现受阻/账户冻结胁迫）**：`冻结` 命中——以"账户将被冻结"作为胁迫手段，属剧本中段「制造恐慌 → 引导转安全账户」。
- **置信度 0.208**（5 因子/24）；证据链完整、可解释。
- **分级证据链**：规则 31 分 + 置信度 1.0（+2）→ 33 分 → **L3**。无 LLM（llm_verdict=null），rule-only 封顶 L3，否则冒充公检法理应冲击 L4/L5。

### 【分级与建议】
- **判定**：诈骗（冒充公检法）——suspicious / high / L3，全样本最高危。
- **建议动作**：
  1. **告警**：L3 告警已生成（alerts=1）；此剧本应**直接人工干预**（冒充公检法属紧急高危类型）。
  2. **证据**：4 条话术 + 推理链（权威+紧迫+冻结胁迫）完整证据链，随告警固定。
  3. **上报**：紧急上报，建议联动反诈中心（此类通常伴随"安全账户"转账，需快速止付）；沉淀"安全账户/配合调查/冻结"组合特征到账户风险画像。

---

## 4. 案例四：投资理财

### 【输入文本】
```
我们平台收益 30% 以上，很多客户都赚了，今天最后一天活动，错过了就没了，扫码下载 APP 注册
```

### 【推理链输出】

**A 轨 · API 实测**：`detection_id=3063`，`verdict=normal`，`severity=none`，`grade=L1`，`rule_score=0.0`，命中话术 0 条 → **漏检（P1）**。

**B 轨 · 推理链预期输出**：

```json
{
  "attack_vector": "unknown",
  "psych_techniques": ["urgency"],
  "seadm_stage": "completion",
  "scam_lifecycle": "unknown",
  "evidence": [
    {"factor": "urgency",     "matched": "今天|最后", "text": "我们平台收益 30% 以上，很多客户都赚了，今天最后一天活动，错过了就没了，扫码下载 APP 注册"},
    {"factor": "exploitation","matched": "扫码|下载", "text": "我们平台收益 30% 以上，很多客户都赚了，今天最后一天活动，错过了就没了，扫码下载 APP 注册"},
    {"factor": "completion",  "matched": "收益",     "text": "我们平台收益 30% 以上，很多客户都赚了，今天最后一天活动，错过了就没了，扫码下载 APP 注册"}
  ],
  "confidence": 0.125
}
```

### 【推理链解读】
- **命中话术**：**0 条**（词库无"收益 30% / 最后一天活动 / 扫码下载"类投资理财话术）→ 规则层完全漏检，verdict=normal/L1。
- **攻击向量 = unknown（P1-2：未标 phishing）**：文本含"扫码下载 APP 注册"——典型 phishing 动作。但 VECTOR_KEYWORDS["phishing"] 词表仅有 `点链接/点击链接/二维码/下载app/下载APP/输入网址/链接`，**缺"扫码/下载"独立词**；`扫码|下载` 只存在于 SEADM exploitation 词表，不参与向量判定 → 向量落空。
- **心理技巧 = urgency**："今天最后一天活动，错过了就没了"——限时稀缺；`social_proof`（"很多客户都赚了"）**未命中**（词表为"很多人/大家都/已赚/成功案例"，"很多客户都赚了"无精确匹配）——**词表覆盖缺陷**。
- **SEADM = completion（完成）**：`收益` 命中 completion——但语义上"扫码下载"属 exploitation/执行阶段，completion 判定受"收益"一词误导，**阶段判定有偏差**。
- **生命周期 = unknown**：词表无"活动/注册/下载 APP"生命周期词。
- **置信度 0.125**（3 因子/24）：覆盖度低，印证规则层与向量层双重缺口。
- **分级证据链**：rule=0、无 LLM、无蜜饵 → L1（无信号）。**实际应为高危诈骗，属误判为正常**。

### 【分级与建议】
- **判定**：**应为诈骗（投资理财 + phishing 引流）但被判定 normal/L1 —— 规则层漏检，P1 级问题，反馈队长**（任务预告点）。
- **建议动作**：
  1. **补词库**（产品侧/队2确认）：`speech_patterns` 增加投资理财话术（收益 XX% / 最后一天活动 / 扫码下载 APP 注册 / 错过就没了）；`VECTOR_KEYWORDS.phishing` 增加 `扫码/下载APP/下载 app`；`PSYCH_KEYWORDS.social_proof` 增加 `很多客户/都赚了/客户都`；`SEADM` 补充下载/注册类 exploitation 词。
  2. **告警**：当前无告警（L1）；补词后应至少 L3 并生成告警。
  3. **证据**：推理链已有 urgency + exploitation（扫码|下载）证据可作研判辅助；补词后自动生成完整证据链。
  4. **上报**：此类"高收益+限时+扫码下载 APP"为典型非正规理财 App 引流，建议上报并标记"扫码下载"行为。

---

## 5. 案例五：冒充客服退款（扩展样本）

### 【输入文本】
```
您好，我是XX商城客服，您购买的商品有质量问题要退款，请加我QQ：12345678，点击我发的链接填写银行卡号验证码，双倍赔偿，马上操作
```

### 【推理链输出】

**A 轨 · API 实测**：`detection_id=3064`，`verdict=normal`，`severity=none`，`grade=L1`，`rule_score=0.0`，命中话术 0 条 → **规则层漏检（P2）**。

**B 轨 · 推理链预期输出**：

```json
{
  "attack_vector": "pretexting",
  "psych_techniques": ["urgency", "authority"],
  "seadm_stage": "disclosure",
  "scam_lifecycle": "unknown",
  "evidence": [
    {"factor": "urgency",     "matched": "马上",     "text": "您好，我是XX商城客服，您购买的商品有质量问题要退款，请加我QQ：12345678，点击我发的链接填写银行卡号验证码，双倍赔偿，马上操作"},
    {"factor": "authority",   "matched": "客服|银行", "text": "您好，我是XX商城客服，您购买的商品有质量问题要退款，请加我QQ：12345678，点击我发的链接填写银行卡号验证码，双倍赔偿，马上操作"},
    {"factor": "pretexting",  "matched": "客服",     "text": "您好，我是XX商城客服，您购买的商品有质量问题要退款，请加我QQ：12345678，点击我发的链接填写银行卡号验证码，双倍赔偿，马上操作"},
    {"factor": "phishing",    "matched": "链接",     "text": "您好，我是XX商城客服，您购买的商品有质量问题要退款，请加我QQ：12345678，点击我发的链接填写银行卡号验证码，双倍赔偿，马上操作"},
    {"factor": "exploitation","matched": "操作",     "text": "您好，我是XX商城客服，您购买的商品有质量问题要退款，请加我QQ：12345678，点击我发的链接填写银行卡号验证码，双倍赔偿，马上操作"},
    {"factor": "disclosure",  "matched": "银行卡|卡号|验证码", "text": "您好，我是XX商城客服，您购买的商品有质量问题要退款，请加我QQ：12345678，点击我发的链接填写银行卡号验证码，双倍赔偿，马上操作"}
  ],
  "confidence": 0.25
}
```

### 【推理链解读】
- **命中话术**：**0 条**（词库 refund_scam 有"客服联系退款"8.0 等，但文本中"客服"与"退款"被分隔、"双倍赔偿"无对应词 → 未精确匹配）→ 规则层漏检，verdict=normal/L1。
- **攻击向量 = pretexting（冒充客服借口）** + **phishing（链接诱导）** 双命中：`客服`（pretexting）+ `链接`（phishing），主向量取 pretexting。
- **心理技巧**：`authority`（"我是XX商城客服"）+ `urgency`（"马上操作"）——客服权威 + 限时压迫，诱导点击钓鱼链接。
- **SEADM = disclosure（披露）**：`银行卡|卡号|验证码` 命中——已进入**索要敏感凭证阶段**，诈骗链条已到最后一步前。
- **生命周期 = unknown**：词表无"退款/赔偿/加QQ"生命周期词——**覆盖缺口**。
- **置信度 0.25**；6 条证据，其中 disclosure 一条直接命中三敏感词，可解释性最强。
- **分级证据链**：rule=0 → L1。**但推理链显示已到 disclosure 阶段（索验证码），实际为高危诈骗，规则层需补词**。

### 【分级与建议】
- **判定**：**应为诈骗（冒充客服退款 + phishing）但被判 normal/L1 —— 规则层漏检，P2 级问题**。
- **建议动作**：
  1. **补词库**：refund_scam 增加 `质量问题退款/加QQ/双倍赔偿/点击链接/填写验证码`；生命周期增加 `退款/赔偿/加QQ` 词。
  2. **告警**：补词后按规则分进入 L3；推理链 disclosure 命中敏感词可作为强信号加权（建议产品侧评估：disclosure 因子命中时允许 rule-only 突破 L3 或加权重）。
  3. **证据**：推理链 evidence（客服冒充 + 链接 + 银行卡/验证码索要）完整，直接可作取证。
  4. **上报**：冒充客服索要验证码为高危即时转账型骗局，建议上报并联动 QQ 号 12345678 关联封禁。

---

## 6. 阴性对照（不误报验证）

### 6.1 阴性 1：日常闲聊
```
今天天气不错我去了图书馆看书
```

- **A 轨 API 实测**：`detection_id=3046`（近重复去重复用 09-14 旧行，content 为"…图书馆看书学习。"），`verdict=normal`，`severity=none`，`grade=L1`，`rule_score=0.0` ✅ **不误报**
- **B 轨推理链预期**：`attack_vector=unknown`，`seadm_stage=unknown`，`scam_lifecycle=unknown`，`evidence=[]`（除 urgency 外）——主维度全部 unknown ✅；但 `psych_techniques=["urgency"]`（误命中"今天"，见 P2）。

### 6.2 阴性 2：工作提醒
```
周五项目评审会记得带电脑和水杯
```

- **A 轨 API 实测**：`detection_id=3065`，`verdict=normal`，`severity=none`，`grade=L1`，`rule_score=0.0` ✅ **不误报**
- **B 轨推理链预期**：`attack_vector=unknown`，`psych_techniques=[]`，`seadm_stage=unknown`，`scam_lifecycle=unknown`，`evidence=[]`，`confidence=0.0` ✅ **零命中、零误报**

> 结论：阴性对照**均不误报**（verdict=normal/L1、主维度 unknown）。仅 neg1 因"今天"命中 urgency 词表产生一条心理技巧误命中（P2，不改变判定）。

---

## 7. 汇总表（案例 × 向量 × 技巧 × 阶段 × 生命周期 × 判定）

| # | 案例 | det_id | API verdict / severity / grade | rule_score | 攻击向量 | 心理技巧 | SEADM 阶段 | 生命周期 | 推理链置信度 | 判定 |
|---|---|---|---|---|---|---|---|---|---|---|
| 1 | 杀猪盘 | 3060 | suspicious / medium / **L2** | 9.0 | baiting | urgency+scarcity+commitment | unknown* | groom | 0.25 | 🚩 诈骗（弱信号） |
| 2 | 刷单返利 | 3061 | suspicious / high / **L3** | 16.0 | baiting(+quid_pro_quo) | reciprocity | exploitation | small_bait | 0.25 | 🚩 诈骗（强） |
| 3 | 冒充公检法 | 3062 | suspicious / high / **L3** | 31.0 | pretexting | urgency+authority | exploitation | withdraw_block | 0.208 | 🚩🚩 诈骗（最高危） |
| 4 | 投资理财 | 3063 | normal / none / **L1** ❌ | 0.0 | **unknown** ⚠️ | urgency | completion(偏差) | unknown | 0.125 | ❌ 漏检（P1） |
| 5 | 冒充客服退款 | 3064 | normal / none / **L1** ❌ | 0.0 | pretexting(+phishing) | urgency+authority | disclosure | unknown | 0.25 | ❌ 漏检（P2） |
| 6 | 阴性·日常 | 3046 | normal / none / L1 ✅ | 0.0 | unknown | [urgency] ⚠️ | unknown | unknown | 0.042 | ✅ 不误报 |
| 7 | 阴性·工作 | 3065 | normal / none / L1 ✅ | 0.0 | unknown | [] | unknown | unknown | 0.0 | ✅ 不误报 |

\* case1 的 SEADM 阶段实际应属 exploitation（诱导投入），词表缺"投/入金"类词导致 unknown（P2 词表优化点）。

---

## 8. 发现的问题汇总

### P1（需队长/主控方处理，队3 未擅自处置）
1. **主控运行旧代码，推理链未生效**：9200 进程（16:06）早于代码更新（17:01）。API 无 `se_analysis`/`se_factors`，生产库无 `se_factors` 列。→ 建议重启 9200 加载新代码（`ensure_migrations` 会自动补列；副本验证已证明迁移+落库链路正确）。
2. **案例4 未标 phishing（任务预告点确认）**：投资理财样本 verdict=normal/L1、规则 0 命中、推理链向量 unknown。根因：phishing 词表缺"扫码/下载"；social_proof 词表缺"很多客户都赚了"；speech_patterns 缺投资理财话术。→ 建议补词库（见 §4 建议）。

### P2（词表/规则覆盖优化，不阻塞上线）
3. **案例5 冒充客服退款规则层漏检**：verdict=normal/L1（推理链预期 pretexting+disclosure 强命中）。词库 refund_scam 词条与真实表达不匹配（分隔/变体）。→ 建议补"质量问题退款/加QQ/双倍赔偿/填写验证码"。
4. **neg1 心理技巧误命中**："今天"∈urgency 词表，日常文本误报 urgency（confidence 0.042）。→ 建议收紧 urgency 词表（如"今天"限"今天最后/今天截止"语境）或对单条心理技巧命中设置阈值。
5. **case1 SEADM=unknown**：exploitation 词表缺"投/入金/投资"类词。→ 建议补充。
6. **SEADM 判定偏差**（case4）：`收益` 命中 completion 造成阶段误判；建议 exploitation 优先于 completion（下载/注册属执行动作）。
7. **分级上限**：LLM 未配置时 rule-only 封顶 L3（设计如此）。若部署 LLM 复核，冒充公检法/杀猪盘可冲击 L4+，分级更精准。

---

## 9. 结论

- **跑通情况**：7 条样本全部执行完成（5 诈骗 + 2 阴性），每条均取得 API 判定 + 推理链预期输出 + 证据引用；阴性对照不误报。
- **推理链价值**：在规则层漏检（case4/5）或弱信号（case1）时，推理链仍能输出可解释的向量/技巧/阶段/生命周期证据链，是规则层的**有效降级与补充维度**；`se_analysis` 与 `se_factors` 落库一致（副本全链路验证 consistent=true）。
- **最大问题**：主控未重启导致推理链当前未在线（P1-1）；case4 未标 phishing（P1-2）。均已反馈，队3 按约束未改代码、未重启。
- **报告双写**：本报告已同步落盘远程 `docs/社会工程学案例分析报告.md` 与本地 `E:\dsh3080工作区存放位置\dsh_gzq1\案例分析_队3.md`。

---
*生成：队3 案例分析工程师 · 测试数据附原始 JSON 于 case_outputs/（scan 响应、full 详情、离线推理链、副本全链路验证）*
