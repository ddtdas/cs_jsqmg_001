# 严格验收 R4 · 文档幻觉核实与修复报告

> 任务：t15 ｜ 角色：doc-verifier（文档幻觉核实员：核实+修复）
> 日期：2026-09-17 ｜ 项目：金丝雀蜜罐 CanaryGuard AntiFraud v1.0.0（远程 192.168.10.110，`C:\Users\Administrator\Desktop\知乎黑客松\金丝雀蜜罐`）
> 输入：R3 修复已合入（fix-engineer t12，462 passed；含主控重启激活 R2/R3 代码）、R3 文档修复（t11）已落盘
> 方法：读 docs/ + 读代码（R3 改动 9 文件：grading/scheduler/traps/models/start.bat/start.ps1/CasesView/test_r8/test_r7）+ 调线上 API（openapi 指纹/stats/audit-verify，admin key）+ 远程实跑 pytest + 线上 DB 只读核查；只改文档（UTF-8 无 BOM），不修代码、不重启主控。

---

## 0. 结论摘要

R3 修复新增 4 条专项回归（`tests/test_r8_r3fixes.py`），**pytest 从 458 → 462**；R3 修复落地
**5 项行为变化**（主控重启激活 R2/R3 代码、`_escalate_pending` 终态收敛 resolved/terminal、
cases `UNIQUE(det_id)` 索引、`is_trap_escalated` 共享判定、start.bat 精确匹配），其中 **4 项需同步文档**。

| 核实项 | 文档原声明 | 实测值（R4） | 结论 |
|---|---|---|---|
| pytest 全量 | README/docs-README/00/07 写 458（30 文件） | 远程实跑 **462 passed**（31 文件，61.47s，collect 462） | ❌ 过期 → 已改 4 处 + 07 表格补 test_r8_r3fixes 行 |
| OpenAPI | 94 paths / 103 ops / 46 schemas | 线上 94/103/46（R3 无新增路由） | ✅ 一致 |
| 表 / job / 工具 / 路由 | 22 表 / 9 job / 55 工具 / 24 路由 | 22 / 9 / 55 / 24（R3 未动 schema 结构/调度/工具/路由） | ✅ 一致 |
| 验收报告序列 | …→458 | 458 → **462**（R3 修复新增 test_r8_r3fixes 4 条） | ⚠️ 序列追加 462 → 已改 07-测试 |
| R3 代码加载态 | R3 宏观 P0-1：主控未加载 R2/R3 代码 | **已重启激活**：线上 openapi CheckHitRequest props=['text','detection_id'] | ✅ 已解决（R3 修复 §1.1 实证） |
| cases UNIQUE(det_id) | 下一轮修改方案/验收B队：cases 无 UNIQUE | 线上 DB PRAGMA 实测 **idx_cases_det_unique（unique=1）在线**；cases=80、重复组=0；直插重复被 DB 拒绝（R3 修复 §1.3） | ❌ 历史文档未更新 → 已标注 |
| `_escalate_pending` 终态 | R3 代码调用：pending 行不清理/无界增长（P2-1 时点） | R3 修复：重放成功标 resolved=true、检测已删标 terminal=true（终态收敛）；线上 pending 行=0 | ✅ 已修复；02-API events kind 已补终态说明 |
| 存量清洗数字 | R2 修复报告：cases=80、evidence=103（冻结 86 保留） | 线上 DB 实测 cases=80、evidence frozen=86 不变 | ✅ 一致（R3 重启后保留） |

---

## 1. 已修复（文档，共 7 个远程文件 + 1 个本地交接文档）

### 1.1 根 `README.md`
| 位置 | 原内容 | 改后 |
|---|---|---|
| §7 目录 | `tests/ # pytest（458 用例）` | `pytest（462 用例）` |
| §8 测试 | **458 passed**（30 个测试文件，…test_r6_r1fixes/test_r7_r2fixes…） | **462 passed**（31 个测试文件，…test_r6_r1fixes/test_r7_r2fixes/test_r8_r3fixes…） |
| §4.3 start 互斥段 | R1 修复（P1-5）单实例互斥 | 追加「R3 修复（P3-5）：僵尸匹配精确为 `--port 9200`（start.bat 原 `-match '9200'` 过宽）；两脚本启动后等待 ~2s 复核端口归属，同秒双启动中落败进程被清理，收敛为单实例」 |

### 1.2 `docs/README.md`（索引）
- 07-测试 行：「全量测试（458 passed）」→「（462 passed）」。

### 1.3 `docs/00-项目概述.md`
| 位置 | 原内容 | 改后 |
|---|---|---|
| §3 测试 | pytest（458 用例，30 个测试文件，…R1/R2 修复回归） | pytest（462 用例，31 个测试文件，…R1/R2/R3 修复回归） |
| §4 R6 | 同一检测仅一个案例（同载荷幂等返回、异载荷 409 case_exists，R1 修复） | 追加「**DB 层 UNIQUE(det_id) 索引双保险（R3）**」 |

### 1.4 `docs/02-API.md`
| 位置 | 原内容 | 改后 |
|---|---|---|
| cases publish | 同一检测仅一个案例：同载荷幂等返回、异载荷 409 case_exists（R1 修复） | 追加「**DB 层 UNIQUE(det_id) 索引双保险（R3）**」 |
| events kind | trap_hit_escalation_pending（R2 补偿未决） | 追加「R3 起重放成功标记 resolved=true、检测已删标记 terminal=true 终态收敛」 |

### 1.5 `docs/03-部署.md`
- §1 一键启动：追加「单实例互斥（R1 P1-5 / R3 P3-5）」说明——匹配精确为 `--port 9200`、启动后 ~2s 复核端口归属、收敛同秒双启动为单实例、CIM 不可用跳过不阻塞。

### 1.6 `docs/07-测试.md`
| 位置 | 原内容 | 改后 |
|---|---|---|
| §1 命令注 | 458 passed（R3 复核，约 60s） | 462 passed（R4 复核实测，约 61s） |
| §1 结果 | **458 passed**（30 文件） | **462 passed**（31 文件） |
| §1 基线演进 | …→458（严格验收 R2 回归 +7，当前） | …→458（R2 回归 +7）→ **462（严格验收 R3 回归 test_r8_r3fixes +4，当前）** |
| §2 表头 | 30 文件，R3 复核 | 31 文件，R4 复核 |
| §2 表格 | 30 行合计 458 | 新增 `test_r8_r3fixes.py | 4 | 严格验收 R3 修复回归：P2-1 _escalate_pending 终态收敛（resolved/terminal）、P2-2 cases UNIQUE(det_id)、P3-1 is_trap_escalated 共享`，合计 **31 文件 / 462** |
| §2 注 | 458 collected/passed（59.99s） | 462 collected/passed（61.47s） |

### 1.7 `docs/下一轮修改方案.md`
- 问题清单 P3 批量行「cases 无 UNIQUE(det_id)」：标注「✅ 已由严格验收 R3 修复：idx_cases_det_unique 已线上应用」。

### 1.8 本地 `交接-金丝雀蜜罐.md`（本地交接文档，非远程）
- 复核注更新：pytest 451→458→**462 passed（31 文件，含 test_r6_r1fixes/test_r7_r2fixes/test_r8_r3fixes）**；追加「cases 已加 UNIQUE(det_id) 索引（R3）」。

---

## 2. 核实通过（无幻觉）

| 项 | 声明 | 实测 |
|---|---|---|
| 线上 health / openapi / stats | health 200；94 paths / 103 ops / 46 schemas；stats cases=80 | ✅ R4 实测一致；CheckHitRequest props=['text','detection_id']（R3 代码已激活） |
| cases UNIQUE(det_id) | R3 修复报告：索引在线 + 直插重复被拒 | ✅ PRAGMA index_list 实测 idx_cases_det_unique unique=1；cases=80、0 重复组 |
| `_escalate_pending` 终态 | R3 修复报告：resolved/terminal 收敛 | ✅ 线上 pending 行=0；02-API kind 说明已同步 |
| 存量清洗数字 | cases=80、evidence=103（冻结 86） | ✅ 线上 DB 实测一致（R3 重启后保留） |
| 22 表 / 9 job / 55 工具 / 24 路由 | R3 后文档 | ✅ 代码/线上核对一致 |
| R3 修复 4 条回归 | test_r8_r3fixes 4 条（P2-1×2、P2-2×1、P3-1×1） | ✅ 已纳入全量（458+4=462）；与 R3 修复报告一致 |
| e2e 63/63 | 端到端验收报告 | ✅ 历史记录一致（只读未重跑） |
| is_trap_escalated 共享 | P3-1 抽取 GradingService 静态方法 | ✅ 代码实测 scheduler/traps 复用，test_r8 断言 |

---

## 3. 待代码/运维（非文档，交 fix-engineer / 运维）

1. **沿用（连续 4 轮未处理，建议收尾轮清理）**：死代码 cfg_int/cfg_bool/strip_fence 等 7 处、4 处未引用 import、代码注释「14 表/8 job」漂移（代码注释非文档未改）。
2. **P2-9 遗留**：evidence 冻结重复包（10 组 frozen=1）为存证语义设计保留；加全局唯一约束前需人工决策。
3. **P2-11/P2-12 功能缺口**：cases 无更新/删除 API；evidence 无追加截图/补充材料机制（历史记录）。
4. **P2-13 运维**：scanned 滞留 638 条无归档；告警 1448 unread 无自动已读；events/gate_logs 只增无归档。
5. **P2-14/P2-15 历史遗留**：IOC 提取噪声（QQ 正则命中银行卡）；knowledge import 并发 UNIQUE 竞态 500。
6. **P3-6 理论竞态**：幂等判定非原子 check-then-act（跨进程），依赖单实例互斥兜底。

---

## 4. 报告落盘

- 远程：`docs/严格验收R4_文档幻觉.md`（本文件）
- 本地：`E:\dsh3080工作区存放位置\dsh_gzq1\严格验收R4_文档幻觉.md`

> 全部修改均 UTF-8 无 BOM 写回；未修改任何代码；未重启主控（9200 全程在线，health 200；
> R3 修复阶段已按 t10 P0-1 指示完成一次授权重启激活 R2/R3 代码，本任务未再重启）。
