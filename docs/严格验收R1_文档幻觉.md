# 严格验收 R1 · 文档幻觉核实与修复报告

> 任务：t3 ｜ 角色：doc-verifier（文档幻觉核实员：核实+修复）
> 日期：2026-09-17 ｜ 项目：金丝雀蜜罐 CanaryGuard AntiFraud v1.0.0（远程 192.168.10.110，`C:\Users\Administrator\Desktop\知乎黑客松\金丝雀蜜罐`）
> 方法：读 docs/ + 读代码 + 调线上 API（9200 admin key）实测关键数字 + 远程实跑 pytest；只改文档（UTF-8 无 BOM 写回），不修代码、不重启主控。

---

## 0. 结论摘要

文档存在 **9 处事实性过期/幻觉**，全部已直接修复（纯文档改动）；无需要代码修复的文档口径问题（有 1 处 MCP 覆盖描述在 02-API/06 中修正为「50/94 端点有工具」，属文档口径修正而非代码缺陷）。

| 核实项 | 文档原声明 | 实测值 | 结论 |
|---|---|---|---|
| OpenAPI 端点 | README/docs 均写 80 paths / 87 operations / 34 schemas；07-测试 写 37/40/12 | 线上 94 paths / 103 ops / 46 schemas | ❌ 过期 → 已重导 docs/openapi.json + 改文档 |
| MCP 工具数 | 00-项目概述 写 36 个；README/06 写 55 个 | mcp/server.py 实测 **55 个** @server.tool() | ⚠️ 00 过期 → 改；06 表只列 36 个 → 补全为 55 |
| 数据库表数 | README/00/01 写 14 张 | 线上 SQLite + models/__init__.py 均 **22 张** | ❌ 过期 → 已改 3 处 |
| 调度 job 数 | 00-项目概述 写 6 类 | scheduler.py 注册 **9 类**（test 断言 9） | ❌ 过期 → 已改 |
| pytest 全量 | README 写 332；07-测试 写 99/112；docs/README 写 112 | 远程实跑 **446 collected / 446 passed**（28 文件，57.03s） | ❌ 过期 → 已改 4 处 |
| WebUI 页面 | README/05 写 25 路由 / 21 视图；00 写 11 页 | router/index.js 实测 **24 路由 / 24 视图**（24 个 view 组件） | ❌ 过期 → 已改 3 处 |
| 验收报告数字 | 408/429/446 passed | 当前 446 与最新报告（代码审计验收队A / 知识库验收队C）**一致** | ✅ 一致（408→429→446 为真实增长序列） |
| e2e 断言 | 63/63 全绿 | e2e_acceptance.py 存在（63 断言口径，复验报告连跑两次通过） | ✅ 一致 |
| 版本 | v1.0.0 三者一致（VERSION/CHANGELOG/__init__） | VERSION=1.0.0、__version__="1.0.0"、CHANGELOG [1.0.0] | ✅ 一致 |

---

## 1. 已修复（文档，共 10 个文件）

### 1.1 根 `README.md`
| 位置 | 原内容 | 改后 |
|---|---|---|
| §2 架构图 | 「55 个 MCP 工具透传 80 个 REST 端点」 | 「55 个 MCP 工具透传 94 个 REST 端点（94 paths）」（与线上 openapi 对齐） |
| §4.3 | 「幂等建表（14 张表）」 | 「幂等建表（22 张表）」 |
| §4.4 | 「25 路由」 | 「24 页面路由」 |
| §5 末注 | 「docs/openapi.json（80 paths / 87 operations）」 | 「（94 paths / 103 operations）」 |
| §6 | 「55 个 MCP 工具透传 80 个 REST 端点」 | 「55 个 MCP 工具透传主控 94 个 REST 端点中已映射的 50 个」（MCP 覆盖 50/94 事实修正） |
| §6.3 认证清单 | 「POST /system/stats」 | 「GET /system/stats」（stats 为 GET，写错方法名） |
| §7 目录 | 「models/ # 14 张表」「25 路由 / 21 视图」「~55 工具」「pytest（332 用例）」 | 「22 张表」「24 路由 / 24 视图」「55 工具」「pytest（446 用例）」 |
| §8 测试 | 「332 passed」 | 「446 passed（28 个测试文件，P0–P18 全量 + …）」 |

### 1.2 `docs/README.md`（索引）
| 位置 | 原内容 | 改后 |
|---|---|---|
| 05-WebUI 行 | 「11 个页面」 | 「24 个页面/路由」 |
| 07-测试 行 | 「112 passed」 | 「446 passed」 |
| openapi.json 行 | 「80 paths / 87 operations」 | 「94 paths / 103 operations / 46 schemas」 |

### 1.3 `docs/00-项目概述.md`
| 位置 | 原内容 | 改后 |
|---|---|---|
| §2 双形态表 | 「36 个 af_* 工具」 | 「55 个 af_* 工具」 |
| §3 技术栈·数据库 | 「14 张表」 | 「22 张表」 |
| §3 技术栈·调度 | 「6 类 job」 | 「9 类 job」 |
| §3 技术栈·前端 | 「vue-router（11 页）」 | 「vue-router（24 路由 / 24 视图）」 |
| §3 技术栈·MCP | 未写工具数 | 「55 个 af_* 工具」 |
| §3 技术栈·测试 | 「pytest（112 用例，P0–P8 + P6.5 全覆盖）」 | 「pytest（446 用例，28 个测试文件，P0–P18 全覆盖）」 |

### 1.4 `docs/01-快速开始.md`
| 位置 | 原内容 | 改后 |
|---|---|---|
| §2 | 「幂等建 14 表」 | 「幂等建 22 表」 |

### 1.5 `docs/02-API.md`
| 位置 | 原内容 | 改后 |
|---|---|---|
| 首部 | 「80 paths / 87 operations / 34 schemas」 | 「94 paths / 103 operations / 46 schemas」 |
| §2.1 标题 | 「新功能端点（聊天导入 / 人设实战 / 社工库）」 | 追加「话术库 / 知识库」两组端点（P17/P18 新增的 14 个路径：/speech-patterns×7、/knowledge×9，均为线上已落地端点） |
| §4 MCP 对应关系 | 「每个 REST 端点都有对应 MCP 工具（共 55 个）」 | 改为「55 个工具透传已映射的 50 个端点；其余（auth 引导/persona/chat-import/soc-lib/knowledge/speech-patterns/collector-platforms 等）无工具，仅 REST/WebUI」 |

### 1.6 `docs/05-WebUI.md`
| 位置 | 原内容 | 改后 |
|---|---|---|
| §3 | 「25 路由 / 21 视图」且页面表仅 11 项 | 「24 路由 / 24 视图」，页面表补全为 24 项（新增 /assets /board /sessions /risk /collection /supply-chain /speech-patterns /knowledge /chat-import /settings /accounts-config /collector-matrix /console） |

### 1.7 `docs/06-MCP集成.md`
| 位置 | 原内容 | 改后 |
|---|---|---|
| §3 标题 | 「55 个 af_ 前缀工具，透传 80 个 REST 端点」 | 「55 个 af_ 前缀工具，透传主控 94 个 REST 端点中已映射的 50 个」 |
| §3 工具表 | 仅列约 36 个工具 | **补全为全部 55 个**（新增 检测 af_detection_full/platform_link、采集矩阵 7、账号桥接 7、供应链 2、运维 af_embedded_dsh） |
| §3 末注 | 无覆盖说明 | 补充「44 个端点无 MCP 工具（auth/persona/chat-import/soc-lib/knowledge/speech-patterns/collector-platforms 等），仅 REST/WebUI」 |

### 1.8 `docs/07-测试.md`
| 位置 | 原内容 | 改后 |
|---|---|---|
| §1 | 「99 passed（约 12s）」/「112 passed」 | 「446 passed（v1.0.0 实测，约 57s）」 |
| §2 | 9 个文件 / 112 用例 | **28 个文件 / 446 用例**（逐文件实测收集数；附历史基线演进 112→163→175→193→228→240→244→273→317→330/332→408→429→446） |
| §3 | 「37 paths / 40 operations / 12 schemas」 | 「94 paths / 103 operations / 46 schemas」 |
| §4 | openapi 冒烟数字 | 同步 94/103 |

### 1.9 `docs/MCP端核对表.md`（R4 时代产物）
- 原为「36 工具 × 37 paths」快照；新增 **§0 R1 复核更新表**：当前 55 工具 × 94 paths（覆盖 50/94）、44 个端点无工具、af_check_trap_hit 已改 POST、af_freeze_evidence 亦携带 confirm+reason。
- 原 §2–§5 保留为历史对照（明确标注「历史快照，权威清单见 06-MCP集成 §3 + openapi.json」）。

### 1.10 `docs/openapi.json`（机器可读文档）
- 旧文件为 80 paths / 87 ops / 34 schemas（**过期**，缺 knowledge/speech-patterns 等 14 个路径）。
- 已用项目自带 `scripts/export_openapi.py` 重新导出 → **94 paths / 103 ops / 46 schemas**，与线上 `/openapi.json` 完全一致。

---

## 2. 核实通过（无幻觉）

| 项 | 文档声明 | 实测 |
|---|---|---|
| 版本一致性 | VERSION / CHANGELOG / app/__init__::__version__ 三者 v1.0.0 | ✅ 一致 |
| MCP 55 工具数 | README/06/docs-README 均 55 | ✅ server.py 55 个工具，工具清单与端点逐一核对无缺失/无漂移 |
| 验收报告 408/429/446 | 各报告声明当时通过数 | ✅ 当前 446 = 代码审计验收队A / 知识库验收队C 的最新数字；408（P17 前）→429（话术库 P17 后）→446（知识库 P18 后）为真实递增序列，与测试文件新增一致 |
| e2e 63/63 | 端到端验收报告 | ✅ e2e_acceptance.py 存在、复验报告连跑两次 63/63 全绿（本任务只读不改，未重跑） |
| 健康/审计 | health 200、audit-verify valid | ✅ 线上实测 `{status:ok, db:true, llm:not_configured, zhihu:idle}`、audit-verify `{valid:true, checked:1823}` |
| 22 张表 | （修复后文档） | ✅ 线上 sqlite_master 22 张表与 models/__init__.py 的 22 个 CREATE TABLE 完全对应 |
| 9 类 job | （修复后文档） | ✅ scheduler.py `_register_jobs` 9 个 add_job；test_p2_engineering 断言 9 类（7 interval + 2 cron） |
| WebUI 24 路由/24 视图 | （修复后文档） | ✅ router/index.js 24 个带组件路由（另含 / 与通配重定向），views/ 24 个 .vue |

---

## 3. 待代码修复（本任务范围外，交 fix-engineer）

纯文档核实未发现「文档写了但代码完全没有」的功能幻觉；以下为文档与代码口径差异，属代码侧演进记录，供第 1 轮汇总参考（**非阻塞文档**）：

1. `docs/02-API.md` §2.1 原只记录 3 组新端点，P17 话术库 / P18 知识库端点已在线上但未入文档 —— 已由本任务文档侧补全；若后续加 MCP 工具（knowledge/speech-patterns 目前无工具），需在 mcp/server.py 增补工具并同步 06 文档（待定，非缺陷）。
2. `docs/MCP端核对表.md` 原 §2 表格中 `af_check_trap_hit` 标注 GET（R4 时点）——代码 P2-4 已改 POST，文档已标注；若 code-auditor 确认 mcp/server.py 该工具透传无误即闭环。
3. 各历史验收报告（队B/队E/队F 等）标注的「pytest 317 passed」为当时基线快照，数字随测试增长已过时，但作为历史文档保留原文（未改动）；如需统一口径可在最终验收报告中引用 446。

---

## 4. 报告落盘

- 远程：`docs/严格验收R1_文档幻觉.md`（本文件）
- 本地：`E:\dsh3080工作区存放位置\dsh_gzq1\严格验收R1_文档幻觉.md`

> 全部修改均 UTF-8 无 BOM 写回；未修改任何代码；未重启主控（9200 全程在线，health 200）。
