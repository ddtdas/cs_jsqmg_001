# 第 1 轮 · 复验报告（13 项问题逐条复验 + 回归）

> 项目：金丝雀蜜罐 CanaryGuard AntiFraud v1.0.0 ｜ 执行：qa-tester
> 依据：`docs/第1轮_实战测试报告.md` §3 问题清单 / §5 复验建议；开发修复（t3/t4/t5）已合入代码
> 生成时间：2026-09-13 06:24:21 ｜ 主控：http://127.0.0.1:9200（已重启加载修复代码；key 以 data/bootstrap_admin_key.txt 为准，当前 admin key 未再轮换）

## 汇总：通过 11/13（另 2 项 P2 未实现、观察保留）

- **P0/P1 全部通过（8/8）**：D1 / B3 / A6+C7 / D2 / D8 / A8 / A1 / A10 —— 无仍失败的 P0/P1
- **P2 通过（2/2 已实现项）**：D5 / D7
- **P2 未实现（2 项观察保留）**：B9（重复案例提示）、C5（disabled 禁 monitor）—— ⚠️ R2 复核注：B9 已由严格验收 R1 修复落地（cases.publish 同载荷幂等/异载荷 409 case_exists，见 `docs/严格验收R1_修复.md` §1.4）；C5 仍为观察项
- **回归**：`pytest -q` → **193 passed**（28.73s）；`npm run build` → **成功**（webui/dist 已更新，SessionsView 等新包生效）

## 逐条复验

### V1｜P0-D1 bootstrap-info 不再泄露 key 文件路径/文件名 → **PASS**
- 【复验操作】POST /auth/bootstrap-info 响应体扫描
  - 【修复方案确认】无 af_admin/bootstrap/.txt/盘符
  - 【实测结果】status=200 hits=无 → **通过**
  - 说明：{"ok":true,"data":{"initialized":true,"port":9200,"hint":"主控运行于 http://127.0.0.1:9200；admin key 已初始化（首次启动自动生成，见 data 目录下配置文件）。"}}

### V2｜P1-B3 空/空白/纯 emoji/纯符号 check-hit 不误命中不退役；真实文本仍命中 → **PASS**
- 【复验操作】check-hit 3空格
  - 【修复方案确认】hit=False
  - 【实测结果】status=200 hit=False matched=[] → **通过**
- 【复验操作】check-hit 纯emoji
  - 【修复方案确认】hit=False
  - 【实测结果】status=200 hit=False matched=[] → **通过**
- 【复验操作】check-hit 换行tab
  - 【修复方案确认】hit=False
  - 【实测结果】status=200 hit=False matched=[] → **通过**
- 【复验操作】check-hit 零宽
  - 【修复方案确认】hit=False
  - 【实测结果】status=200 hit=False matched=[] → **通过**
- 【复验操作】check-hit 纯符号
  - 【修复方案确认】hit=False
  - 【实测结果】status=200 hit=False matched=[] → **通过**
- 【复验操作】蜜饵未被误退役
  - 【修复方案确认】状态仍 monitored
  - 【实测结果】status=monitored → **通过**
- 【复验操作】真实改写文本仍命中
  - 【修复方案确认】hit=True 且 retired
  - 【实测结果】status=200 hit=True st=retired → **通过**

### V3｜P1-A6/C7 脱敏变体（全角/零宽/字母混淆/中文大写/拆词/+86）全部拒绝或抹除 → **PASS**
*注：绕过=无*
- 【复验操作】publish ① 明文手机号
  - 【修复方案确认】422 sensitive_data
  - 【实测结果】422 sensitive_data → **通过**
- 【复验操作】publish ② 嵌套 JSON
  - 【修复方案确认】422 sensitive_data
  - 【实测结果】422 sensitive_data → **通过**
- 【复验操作】publish ③ JSON 字符串转义
  - 【修复方案确认】422 sensitive_data
  - 【实测结果】422 sensitive_data → **通过**
- 【复验操作】publish ④a 全角数字
  - 【修复方案确认】422 sensitive_data
  - 【实测结果】422 sensitive_data → **通过**
- 【复验操作】publish ④b 零宽字符
  - 【修复方案确认】422 sensitive_data
  - 【实测结果】422 sensitive_data → **通过**
- 【复验操作】publish ④c 字母混淆
  - 【修复方案确认】422 sensitive_data
  - 【实测结果】422 sensitive_data → **通过**
- 【复验操作】publish ⑤ 中文大写数字+电话语义
  - 【修复方案确认】422 sensitive_data
  - 【实测结果】422 sensitive_data → **通过**
- 【复验操作】publish ⑥a 拆词连字符
  - 【修复方案确认】422 sensitive_data
  - 【实测结果】422 sensitive_data → **通过**
- 【复验操作】publish ⑥b 拆词空格
  - 【修复方案确认】422 sensitive_data
  - 【实测结果】422 sensitive_data → **通过**
- 【复验操作】publish ⑥c +86 前缀
  - 【修复方案确认】422 sensitive_data
  - 【实测结果】422 sensitive_data → **通过**
- 【复验操作】publish ⑦ 身份证
  - 【修复方案确认】422 sensitive_data
  - 【实测结果】422 sensitive_data → **通过**
- 【复验操作】publish ⑧a 微信ID wxid_abc123（设计边界，观察）
  - 【修复方案确认】记录通过/拒绝
  - 【实测结果】200 code=None → **通过**
- 【复验操作】publish ⑧b QQ 123456789（设计边界，观察）
  - 【修复方案确认】记录通过/拒绝
  - 【实测结果】200 code=None → **通过**

### V4｜P1-D2 伪造 XFF=127.0.0.1 不再放行 local-login/current-key/reset-key → **PASS**
- 【复验操作】非回环对端(127.0.0.2)+XFF=127.0.0.1 local-login
  - 【修复方案确认】403（XFF 无效）
  - 【实测结果】status=403 → **通过**
- 【复验操作】非回环对端+ XFF=127.0.0.1 current-key
  - 【修复方案确认】403
  - 【实测结果】status=403 → **通过**
- 【复验操作】非回环对端+ XFF=127.0.0.1 reset-key
  - 【修复方案确认】403（未重置）
  - 【实测结果】status=403 → **通过**
- 【复验操作】回环来源+ XFF=非回环 local-login
  - 【修复方案确认】200（头不再参与判定，来源即回环）
  - 【实测结果】status=200 → **通过**
- 【复验操作】回环来源+ XFF=127.0.0.1 local-login
  - 【修复方案确认】200（同无头一致；非回环对端已证 403）
  - 【实测结果】status=200 → **通过**

### V5｜P1-D8 XFF 换限流桶打不穿 429 → **PASS**
- 【复验操作】无 key 爆发 75 次
  - 【修复方案确认】出现 429
  - 【实测结果】200×61 429×14 → **通过**
- 【复验操作】桶枯竭时 plain/XFF(203)/XFF(127) 探测
  - 【修复方案确认】全部 429（XFF 不换桶）
  - 【实测结果】plain=429 xff203=429 xff127=429 → **通过**
- 【复验操作】admin key 豁免
  - 【修复方案确认】200
  - 【实测结果】200 → **通过**

### V6｜P1-A8 MCP 错误码透传可见 → **PASS**
- 【复验操作】MCP af_publish_case
  - 【修复方案确认】isError 且含 confirm_required
  - 【实测结果】is_error=True text=Error executing tool af_publish_case: [confirm_required] 敏感操作必须 confirm=true（双确认） → **通过**
- 【复验操作】MCP af_config_update
  - 【修复方案确认】isError 且含 reason_too_short
  - 【实测结果】is_error=True text=Error executing tool af_config_update: [reason_too_short] reason 至少 20 字，当前 1 字 → **通过**
- 【复验操作】MCP af_get_evidence
  - 【修复方案确认】isError 且含 evidence_not_found
  - 【实测结果】is_error=True text=Error executing tool af_get_evidence: [evidence_not_found] 证据包不存在: id=999999 → **通过**
- 【复验操作】MCP af_scan_text
  - 【修复方案确认】isError 且含 validation_error
  - 【实测结果】is_error=True text=Error executing tool af_scan_text: [validation_error] [{'type': 'string_too_short', 'loc': ('body', 'text'), 'msg': 'Str → **通过**

### V7｜P1-A1 事件流含 detection_scanned/detection_graded/alert_created/alert_read → **PASS**
- 【复验操作】scan/text 诈骗文本
  - 【修复方案确认】200 且产生检测
  - 【实测结果】status=200 det=145 → **通过**
- 【复验操作】检测/分级/告警事件
  - 【修复方案确认】含 ['alert_created', 'detection_graded', 'detection_scanned']
  - 【实测结果】kinds=['alert_created', 'detection_graded', 'detection_scanned'] → **通过**
- 【复验操作】alert_read 事件
  - 【修复方案确认】read 后出现 alert_read
  - 【实测结果】alert_id=60 found=True → **通过**

### V8｜P1-A10 事件历史分页可用（REST 侧：page_size=50 OK / 200 422） → **PASS**
*注：浏览器事件 tab 渲染单独验证（V8b）*
- 【复验操作】GET /events?page_size=50
  - 【修复方案确认】200 正常
  - 【实测结果】status=200 total=517 → **通过**
- 【复验操作】GET /events?page_size=200
  - 【修复方案确认】422（API 上限 100；UI 已改为 50 分页不触发）
  - 【实测结果】status=422 → **通过**

### V8b｜P1-A10 事件历史 tab 浏览器渲染 → **PASS**
- 【复验操作】浏览器打开 /ui/sessions（事件历史 tab）
  - 【修复方案确认】事件列表真实渲染、无原始校验错误
  - 【实测结果】事件列表正常渲染（529 进入监控/528 蜜饵停用/527 蜜饵部署…），无 validation_error 文本、无『暂无数据』 → **通过**
  - 说明：SessionsView 已改为 EVENT_PAGE_SIZE=50 服务端分页

### V9｜P2-B9 重复案例发布有提示（若实现） → **NOT-IMPLEMENTED**
*注：未实现（P2 观察项保留，不阻塞复验）*
- 【复验操作】同 det 二次 publish
  - 【修复方案确认】第二次 409 或重复提示
  - 【实测结果】r1=200 r2=200 code=None → **未通过**
  - 说明：未实现去重/提示（app/api/cases.py 无重复检测），两次均 200 —— 记录为未实现观察项

### V10｜P2-C5 disabled 后不可 monitor（若实现） → **NOT-IMPLEMENTED**
*注：未实现（P2 观察项保留，不阻塞复验）*
- 【复验操作】disabled 蜜饵 monitor
  - 【修复方案确认】400（修复后）
  - 【实测结果】status=200 state=monitored → **未通过**
  - 说明：未实现 enabled 校验（trap_engine.monitor 仅校验 TRANSITIONS），返回 200 状态漂移到 monitored —— 记录为未实现观察项

### V11｜P2-D5 伪造/过期 key 打开页面自动跳登录 → **PASS**
*注：同一页面 URL 不触发重挂载，需全量刷新验证（已用 location.reload/导航验证）*
- 【复验操作】sessionStorage 写入垃圾 key 后打开 /ui/dashboard
  - 【修复方案确认】挂载探活 401 → 清凭据跳 /login
  - 【实测结果】重定向到 /ui/login?redirect=/dashboard，sessionStorage 被清空（auth.logout） → **通过**
  - 说明：main.js auth.probeSession() + client.js 401 handler 生效

### V12｜P2-D7 带空格 key 登录后存储为 trim 值 → **PASS**
- 【复验操作】登录表单输入『  af_admin_6fea238a  』（前后各 2 空格）
  - 【修复方案确认】存储值无首尾空格
  - 【实测结果】sessionStorage.af_api_key='af_admin_6fea238a'（17 字符，无空格） → **通过**
  - 说明：client.js setApiKey 统一 trim

## 回归

| 项 | 命令 | 结果 |
|---|---|---|
| pytest 全量 | `.venv\Scripts\python -m pytest -q` | **193 passed**（1 warning，28.73s）|
| 前端构建 | `npm run build`（webui/） | **成功**（✓ built in 29.48s；SessionsView 等新 chunk 已生成）|

## 仍失败项清单

- **V9（P2-B9）未实现**：同 det 二次 `cases/publish` 仍两次 200、无重复提示（app/api/cases.py 未加去重）→ 观察项保留，不阻塞第 2 轮。
- **V10（P2-C5）未实现**：disabled 蜜饵 `monitor` 仍返回 200（trap_engine.monitor 未校验 enabled）→ 观察项保留，不阻塞第 2 轮。
- 无 P0/P1 仍失败项。
