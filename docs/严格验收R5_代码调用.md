# 严格验收 R5 · 收尾代码级验收（清理确认）报告

- 轮次：严格验收第 5 轮（收尾）· 代码级验收 + 连续 4 轮遗留清理确认
- 审计对象：192.168.10.110 `C:\Users\Administrator\Desktop\知乎黑客松\金丝雀蜜罐`（只读审计，未改任何文件，未重启）
- 审计员：code-auditor（调用关系检查）
- 基线：pytest 462 passed（t15/t19 实测）；9200 health 200
- 方法：R5 磁盘代码全量拉取（13 关键文件 + R4 补齐 99 py）→ **R4↔R5 全文件 SHA256 比对** → 遗留 18 项逐一 grep 复核 → 全树 AST 调用图复跑 → 线上 openapi/health 实测

---

## 0. 结论摘要

| 级别 | 数量 | 一句话说明 |
|------|------|-----------|
| P0   | 0    | 无阻断级缺陷 |
| P1   | 0    | R4 判定延续，功能面无新问题 |
| P2   | 1    | **收尾清理（t20）尚未落地**：R5 磁盘代码与 R4 **逐字节一致**（0 文件变更），18 项遗留全部仍在——本报告作为清理前基线，供 t20 执行后对比复验 |

**收尾结论：清理未完成（t20 pending，未开始执行）。** 代码级验收功能面 P0=0/P1=0；遗留清理清单已逐项确认现状（见 §1），t20 可按此清单执行，执行后由 R5 收尾复验闭环。

---

## 1. 遗留清理清单逐项确认（R5 现状）

> 前提证据：R4↔R5 全文件 SHA256 比对 → **0 文件变更**（含所有 app/、scripts/、webui/、docs/、tests/）；stat 时间戳亦与 R4 一致 → 磁盘代码 = R4 状态，t20 尚未写入。

### 1.1 死代码 7 处 —— 全部仍在（0 调用状态与 R4 相同）

| # | 函数 | 位置 | R5 确认 |
|---|------|------|---------|
| 1 | `cfg_int` | config_reader.py:39 | 仍在；全库（含测试）0 调用 |
| 2 | `cfg_bool` | config_reader.py:47 | 仍在；全库 0 调用 |
| 3 | `make_reader` | config_reader.py:59 | 仍在；仅测试引用 |
| 4 | `on_degraded` | llm_provider.py:47 | 仍在；仅测试引用 |
| 5 | `strip_fence` | llm_provider.py:142 | 仍在；全库 0 调用 |
| 6 | `get_scheduler` | scheduler.py:479 | 仍在；仅测试引用 |
| 7 | `job_snapshot` | scheduler.py:483 | 仍在；仅测试引用 |

### 1.2 未引用 import 5 处 —— 全部仍在

| # | import | 位置 | R5 确认 |
|---|--------|------|---------|
| 1 | `ApiError` | api/collector.py:18 | 仍在（全文件仅 1 处=import 行） |
| 2 | `ChannelUnavailableError` | api/zhihu.py:19 | 仍在（仅 1 处=import 行） |
| 3 | `Any` + `FENCE_OPEN/FENCE_CLOSE` | speech_engine.py:23/:30-31 | 仍在（仅 import 行） |
| 4 | `Any` | desensitize.py:17 | 仍在（仅 import 行） |
| 5 | `Any` | trap_engine.py:33 | 仍在（仅 import 行） |

### 1.3 注释漂移 6 处 —— 全部仍在

| # | 位置 | R5 确认 |
|---|------|---------|
| 1 | db.py:6 "全部 14 张表" | 仍在（实际 22 张） |
| 2 | db.py:80 "全部 14 张表" | 仍在 |
| 3 | models/__init__.py:1 "全部 14 张表" | 仍在 |
| 4 | models/__init__.py:4 "14 张表名清单" | 仍在 |
| 5 | main.py:5 + :58 "8 类 job" | 仍在（实际 9 类） |
| 6 | scheduler.py:57 "8 类 job" | 仍在 |

**确认结论：18 项遗留全部保持 R4 状态，t20 清理尚未开始。** 本清单即 t20 执行基准（位置/内容精确），执行后应全部消失。

---

## 2. R4 修复后调用关系（最终图复核）

- 全树 AST 分析（mirror_r5 99 py）：调用图与 R4 完全一致——
  - 21 服务 × 21 路由；API→服务单向；服务→服务仅 scheduler→10 服务 + se_analysis→soc_lib（函数内延迟 import）；
  - **循环依赖 0、幽灵调用 0**（4 个常量误报 MATRIX_PLATFORMS/GRADE_LEVELS/ALLOWED_CATEGORIES/CHANNELS + 1 个别名 _SocLibService，沿用排除）；
  - **新断点 0**；traps→grading、scheduler→grading 的 is_trap_escalated 共享调用合法；
  - 死代码候选仅剩 §1.1 的 7 项（= 清理清单），**无新增**；
  - cleanup_duplicates.py 独立于 app 调用图（scripts/），import app.services.audit_log 合法。

---

## 3. 前端契约（client.js ↔ openapi）R5 复查

- client.js 与 docs/openapi.json 哈希 R4==R5（零漂移）；**线上 /openapi.json = 94 paths** 实测；
- client.js 91 个调用逐一命中（contract_check4 无 MISS）；反向 15 个未调用端点不变（测试覆盖，设计内）；
- R4 的 case_exists 前端文案（CasesView.vue）在库，无契约影响。

---

## 4. 静态健康与终验前置

- **py_compile**：mirror_r5 99 个 .py **全部通过**（0 语法错误）；
- **终验数字实测**：
  - openapi：94（线上）✓ 一致
  - 表数：22（models.TABLES 实测）✓ 一致（注释仍写 14 → 属 §1.3 漂移项）
  - job 数：9（scheduler._register_jobs 实测 sched.add_job×9）✓ 一致（注释仍写 8 → 属 §1.3 漂移项）
  - pytest：462 passed（t15/t19 实测基线，本轮未重复跑——由 doc-verifier t19 终核）
  - health：200 ✓（本次实测 `{ok:true, data:{status:ok, db:true}}`）
- **重复函数**：0（zhihu_bridge 多类同名 to_dict 误报排除）。

---

## 5. 问题清单（R5 结论）

| # | 级别 | 类型 | 结论 |
|---|------|------|------|
| P2-1 | P2 | 清理未落地（任务定向） | t20 尚未执行，18 项遗留全部在位（§1 清单精确到行）；**非代码缺陷**，为收尾清理项。t20 执行后应按 §1 逐项复验为 0 |
| P3-1 | P3 | 语义说明 | 死代码/未引用 import 均无运行时影响（0 调用/仅 import 行）；注释漂移无功能影响——清理为代码卫生与文档一致性 |

---

## 6. 修复建议（供 fix-engineer t20 执行）

按 §1 清单 18 项执行（建议一次性提交，避免半清理态）：
1. 删除 config_reader.cfg_int/cfg_bool（0 调用）；make_reader 若保留请标注测试专用（或删除并同步 test_r3_backend_fixes.py:275/287）
2. 删除 llm_provider.on_degraded（同步 test_p3_speech.py:211）与 strip_fence（0 调用）
3. 删除 scheduler.get_scheduler/job_snapshot（同步 test_p10_collector.py:131、test_p2_engineering.py:199）；若运维面板需要 job_snapshot 建议改为接入 /system/stats（非必须）
4. 删除 5 处未引用 import（collector ApiError、zhihu ChannelUnavailableError、speech_engine Any+FENCE、desensitize Any、trap_engine Any）
5. 修 6 处注释漂移（14→22 张表 ×4、8→9 类 job ×3 处位置）
6. 清理后同步受影响测试 + 全量 pytest ≥462 复跑 + py_compile 全绿
7. 完成后通知 code-auditor 复验（对照本报告 §1 逐项归零 + R5 报告 §4 数字不回归）

*报告完*