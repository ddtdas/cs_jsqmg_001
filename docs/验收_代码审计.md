# 验收_代码审计（含调用关系）

- **审计对象**：金丝雀蜜罐 CanaryGuard AntiFraud（远程 192.168.10.110，根 `C:\Users\Administrator\Desktop\知乎黑客松\金丝雀蜜罐`）
- **审计方式**：只读。远程文件经 ssh + Unicode-Base64 读取到本地镜像逐一核验；线上服务仅 GET/无副作用探测；**未改动任何代码/数据、未重启服务、未执行 pytest**（静态 py_compile + 远程既有测试日志佐证）。
- **审计日期**：2026-09-17
- **审计员**：code-audit3（代码审计员，含调用关系）
- **基线**：9200 主控 health 200（`/api/v1/system/health` 实测 200，db=true）；`/cover` 已激活（实测 401/200 鉴权正确）；pytest 479 passed 基线（远程最新全量日志 `var/pytest_full_p10.log` 446 passed，p18 时点；`test_p19_cover.py` 17 条用例已就位，p19 纳入后达到任务书 479 口径）

---

## 〇、结论摘要

| 等级 | 数量 | 说明 |
|---|---|---|
| **P0** | 0 | 无阻断性缺陷 |
| **P1** | 0 | 无高优先级缺陷（反钓鱼伪装模块、调用关系、前端契约、静态健康四维全部通过） |
| **P2** | 2 | ① mcp/server.py 存在 1 处未使用 import（json）；② 11 个 openapi 操作（10 条路径）服务端实现完整但 client.js/WebUI/MCP/测试均无消费方 |
| 观察项 | 2 | app/middleware/__init__.py 包级转发无消费方；根路径 `/health` 返回 404（规范路径为 `/api/v1/system/health`，非缺陷） |

---

## 一、反钓鱼伪装模块审计（app/services/cover.py + app/api/cover.py + models）

### 1.1 与既有约定一致性 —— 全部通过

| 约定 | 结论 | 证据 |
|---|---|---|
| `{ok,data}/{ok:false,error}` 统一封包 | ✅ | 所有端点 `return ok(...)`；ApiError 经 utils.register_error_handlers 统一封包 |
| require_admin（D6） | ✅ | `/cover` 全部 7 个端点均带 `dependencies=[Depends(require_admin)]`；实测无 key → 401，有 key → 200 |
| 敏感操作 confirm+reason≥20 + gate_logs | ✅ | toggle/delete 均走 `_require_confirm`（403 confirm_required / 422 reason_too_short）+ `write_gate_log` 哈希链审计（before/after/payload 齐全）；实测 `/api/v1/system/audit-verify` → `{valid:true, checked:1838}` |
| SQL 参数化 | ✅ | 全部查询使用 `?` 占位符（create/get/list/toggle/delete/expose/register_contact/contacts/cover_map/_link_trap 逐一核验），无字符串拼接注入面；contacts 动态 WHERE 亦以参数列表拼接 |
| 脱敏（明文不落库，D7） | ✅ | `contact_value` 经 `DesensitizeService.regex_redact` 脱敏后落库（实测线上 `139****2222`）；IOC 仅掩码写 events；`_mask_fake`/`_mask_ioc` 首尾保留规则与社工库口径一致 |
| 防御式 | ✅ | `_link_trap` 全 try/except，任何失败返回 `{linked:false, reason}` 不中断接触登记主流程 |
| 迁移幂等 | ✅ | `CREATE TABLE IF NOT EXISTS` + `CREATE INDEX IF NOT EXISTS`；ensure_schema 可重入；实测线上 25 表（24 真实 + sqlite_sequence），cover_identities 14 列 / contact_events 9 列与 DDL 完全一致 |
| 嵌套 layer 计算 | ✅ | `layer = parent.layer + 1`（无 parent=1）；delete 时下层 parent_id 置 NULL 防孤儿；cover_map tree 按 parent 嵌套、children 按 layer 排序、孤儿作为根节点兜底；测试断言三层嵌套 |

### 1.2 关键链路：cover contact → IOC → 锁定

```
POST /api/v1/cover/contact
  → CoverIdentityService.register_contact
     1) DesensitizeService.regex_redact(原文) → contact_events.contact_value（脱敏落库）
     2) SocLibService.extract_iocs(原文) → events(kind='cover_contact') 仅存掩码
     3) 同 contact_value（脱敏后）count>=2 → 该值全部行 escalated=1 + events('cover_locked')
     4) _link_trap → events('cover_trap_link') 反哺供应链图谱（best-effort）
```
- ✅ 实测线上 contact_events 6 行，contact_value 均为掩码；stats 联动正常
- ✅ test_p19_cover.py 专项覆盖：脱敏落库 / IOC 事件掩码 / 二次接触锁定 / 过滤 / 嵌套地图 / 401 / 422

### 1.3 单测覆盖

`tests/test_p19_cover.py` 17 条用例：create（默认/子层/重名 409/parent 404/persona_type 422）、list、toggle 双确认+gate_logs、delete 双确认+gate_logs+二次 404、expose 脱敏清单+事件、contact 脱敏落库+layer_at、IOC 事件、二次锁定、contacts 过滤、cover_map 嵌套、401、contact_type 422、表数 24。

---

## 二、调用关系（全项目，含 cover）

### 2.1 循环依赖 —— 无

AST 静态 DFS（59 个模块）**未发现任何环**。`grading→alerting`、`grading→soc_lib`、`speech_engine→trap_engine` 等均为函数体内延迟 import，方向单一，无回边。

### 2.2 未解析 import —— 无（项目内）

237 条项目内 from-import 全部解析到真实名字/子模块。`mcp/server.py` 的 `from mcp.server.mcpserver import MCPServer` 指向 site-packages 官方 mcp SDK（mcp/ 目录无 `__init__.py` 命名空间隔离，文件头有明确警示注释，属设计，非缺陷）。

### 2.3 幽灵调用 / 死代码

- ❌ **未发现**：不存在"调用不存在函数/端点"的幽灵调用；openapi 111 个操作对应的后端 handler 全部存在且已挂载。
- ✅ 12 条关键链路逐一核验：

| # | 链路 | 结论 |
|---|---|---|
| 1 | scan→grading→alert：`POST /scan/text` → SpeechEngine.scan_text → GradingService.finalize → AlertService.evaluate（仅 L3+ 告警、幂等去重、单事务三写） | ✅ |
| 2 | scan inbox 批量：`POST /scan/inbox` → 逐条 analyze_detection + finalize | ✅ |
| 3 | cover contact→IOC→锁定（见 1.2） | ✅ |
| 4 | grading→soc_lib 自动分析：L3/L4/L5 分级后 post-commit 自动 `SocLibService.analyze` 回写 se_factors.soc_analysis（防御式降级） | ✅ |
| 5 | 蜜饵实锤→L5：`POST /traps/{id}/check-hit` → TrapEngine.check_hit → GradingService.finalize(trap_hit_count=1) → L5 + events(trap_hit_escalated)（幂等：is_trap_escalated 防重） | ✅ |
| 6 | 调度器→服务：9 类 job（scan_dm 10m / zhihu_sync 5m / trap_recheck 6h / account_scan 2h / evidence 12h / case_publish 3:00 / collector 1m / persona 1m / knowledge_decay 3:30）全部指向既有服务方法 | ✅ |
| 7 | knowledge 蒸馏：import/import-text → distill（DesensitizeService + extract_iocs 掩码）→ to-pattern → speech_patterns + gate_logs | ✅ |
| 8 | soc_lib 查询：analyze → HIBP/本地泄露库（sha256，明文不落库）→ events('soc_lib_hit')，全降级不抛 | ✅ |
| 9 | persona：respond/drill → extract_iocs + 拟真回复；schedule/publish_due 由调度器驱动 | ✅ |
| 10 | evidence/cases：build → DesensitizeService；freeze/publish 敏感操作 confirm+reason + gate_logs | ✅ |
| 11 | chat_import：scan/import → SpeechEngine + GradingService + desensitize + audit_log | ✅ |
| 12 | auth：local-login/current-key/reset-key 仅回环可调（proxy_headers=False 防伪造），reset 走 gate_logs 链 | ✅ |

### 2.4 前端路由挂接

`webui/src/router/index.js` 注册 `/cover → CoverView.vue`；`App.vue` 导航含「伪装账号」入口；`webui/dist` 已构建（build_after_fix.log：built in 23.57s）。

---

## 三、前端契约（client.js ↔ 线上 openapi）

### 3.1 端点统计

- 线上 openapi（:9200/openapi.json 实测）：**118 路径 / 111 操作**（≥95 要求满足）
- client.js：84 条唯一请求路径，经 Node 实际执行捕获 140 次调用/100 个唯一 METHOD+PATH 操作 → **客户端命中的操作数 100/111**

### 3.2 cover 8 方法 —— 逐一命中 ✅

| client.js 方法 | 路径 | 线上 openapi | 视图使用 |
|---|---|---|---|
| coverIdentities | GET /cover/identities | ✅ | CoverView.vue:162 |
| coverCreate | POST /cover/identities | ✅ | CoverView.vue:246 |
| coverToggle | POST /cover/identities/{id}/toggle | ✅ | CoverView.vue:278 |
| coverDelete | DELETE /cover/identities/{id} | ✅ | CoverView.vue:469 |
| coverExpose | POST /cover/identities/{id}/expose | ✅ | CoverView.vue:303 |
| coverContact | POST /cover/contact | ✅ | CoverView.vue:435 |
| coverContacts | GET /cover/contacts | ✅ | CoverView.vue:504 |
| coverMap | GET /cover/map | ✅ | CoverView.vue:561 |

请求体/参数与后端 Pydantic 模型一致（IdentityCreateRequest、ConfirmReasonRequest、ContactRegisterRequest）；CoverView 本地 layer 预计算与后端一致（仅 UX 提示，后端为准）。

### 3.3 未命中（P2）：11 个操作 / 10 条路径有端点无客户端

服务端实现完整、openapi 在、require_admin 在，但 client.js / WebUI 视图 / MCP server / 测试均无消费方：

`POST /persona/generate`、`GET /persona/posts`、`GET/POST /persona/replies`、`DELETE /persona/replies/{reply_id}`、`POST /persona/replies/{reply_id}/toggle`、`POST /persona/schedules/{id}/pause`、`POST /persona/schedules/{id}/resume`、`GET /collector/platforms`、`POST /soc-lib/query`、`GET /system/audit-verify`

> 建议：若为保留的 REST 管理面/外部集成端点，在 docs/API.md 注明用途；否则补齐 client.js 方法或在后续版本下线，避免"接口无人消费"面扩大。

---

## 四、静态健康

- **py_compile**：远程全量 108 个 .py 文件 `python -m py_compile` **0 失败**（Python 3.12）
- **未引用 import**：AST 扫描 59 模块，55 个候选均为 `from __future__ import annotations` 误报；**唯一真实未用 import：`mcp/server.py:31 import json`**（全文无 `json.` 调用，P2）
- **循环依赖**：无（见 2.1）
- **未解析引用**：无（见 2.2）
- **统一错误处理**：ApiError / validation_error（剥离 input/ctx）/ OverflowError / sqlite locked→503 / 500 通用文案，均不泄露类名与敏感输入（P0-E3/P1-B7/P2-E3 复核通过）

---

## 五、线上实测证据（只读）

| 探测 | 结果 |
|---|---|
| GET /api/v1/system/health | 200 `{status:ok, version:1.0.0, db:true}` |
| GET /api/v1/cover/identities（无 key） | 401（鉴权生效） |
| GET /api/v1/cover/identities（有 key） | 200，data.identities 数组 |
| GET /api/v1/cover/map、/cover/contacts | 200；contact_value 已脱敏（`139****2222`） |
| GET /api/v1/system/stats | 200，cover_identities/contact_events 联动 |
| GET /api/v1/system/audit-verify | 200 `{valid:true, checked:1838}`（gate_logs 哈希链完整） |
| data/af.db 只读查询 | 25 表（24 真实 + sqlite_sequence）；cover_identities 14 列、contact_events 9 列与 DDL 完全一致 |
| webui/dist | 已构建（38 文件，index.html 602B），CoverView 路由挂接 |

---

## 六、缺陷清单

### P0（无）

### P1（无）

### P2

1. **mcp/server.py:31 `import json` 未使用** —— 纯静态清理项，无行为影响。
   - 位置：`mcp/server.py` 第 31 行
   - 建议：删除该 import。

2. **11 个 openapi 操作无任何客户端消费方**（persona/generate、persona/posts、persona/replies×4、persona/schedules pause/resume、collector/platforms、soc-lib/query、system/audit-verify）
   - 性质：服务端功能完整、可直达 REST 调用，但 WebUI/MCP/测试均未挂接，属"接口无人消费"面。
   - 建议：文档标注用途（REST 管理面）或补齐前端方法 / 下线。

### 观察项（不计缺陷）

- `app/middleware/__init__.py` 包级转发（RateLimitMiddleware / reset_rate_limiters）无消费方（main.py 直接 `from .middleware.rate_limit import`），可清理或保留为包 API。
- 根路径 `/health` 返回 404 —— 项目规范健康检查路径为 `/api/v1/system/health`（任务书"9200 主控 health 200"即指此路径，实测 200），非缺陷。

---

*本报告由 code-audit3 依据远程只读源码镜像（audit-code-a/mirror，59+ 文件）与线上只读探测出具；审计过程未修改远程任何文件、未重启服务。*
