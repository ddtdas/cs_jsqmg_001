# 验收修复报告 t5 —— 金丝雀蜜罐（P0 白屏 + Console/表格 P1 + 精选 P2）

修复工程师：fixer-final（验收修复工程师，final-acceptance 团队）
日期：2026-09-17
目标：192.168.10.110 · `C:\Users\Administrator\Desktop\知乎黑客松\金丝雀蜜罐`（WebUI http://192.168.10.110:9200/ui/）
基线：9200 主控 health 200；`webui/` 由主控静态服务 `webui/dist`（app/main.py `_WEBUI_DIST`），重建即生效，**未重启主控**；**未提交 git**（远程无 git 命令）。

---

## 一、修复项与 diff（文件:行）

### P0-1 EvidenceView 白屏（原白屏根因）
- 文件：`webui/src/views/EvidenceView.vue`
- 问题：第 3 行 `import { reactive, ref, onMounted } from 'vue'` 缺 `computed`，第 93 行 `const canFreeze = computed(...)` 触发 setup 阶段 ReferenceError → 整页白屏。
- 修复：第 3 行改为 `import { reactive, ref, computed, onMounted } from 'vue'`（1 行 diff）。

### P1-1 /ui/console 布局塌陷（UI 碰撞 P1-1）
- 文件：`webui/src/views/ConsoleView.vue` `<style scoped>`（约 136-139 行）
- 问题：`.af-console{height:100%}` 硬约束 + `.af-console-frame{flex:1;min-height:620px}` → 卡片被 flex 压缩至 3px（overflow hidden 裁剪），离线告警条压盖卡片头/主体，地址输入与按钮不可见。
- 修复：
  ```css
  .af-console { display: flex; flex-direction: column; height: auto; min-height: 100%; }
  .af-console > div:not(.af-console-frame) { flex-shrink: 0; }
  .af-console-frame { flex: 1; min-height: 620px; ... }
  ```
  页面随内容增长、由 el-main 滚动；卡片/告警条永不压缩。

### P1-2 表格固定列遮挡（UI 碰撞 P1-2 + P2-6）
- 文件：`webui/src/views/ScanView.vue`（最近检测记录表，约 146-172 行）、`BoardView.vue`（检测速览表约 526-581 / 拟真账号计划表约 624-673 同源加固）、`TrapsView.vue`（蜜饵表约 224-253 行）
- 问题：`fixed="right"` 操作列在窄容器下绝对定位覆盖相邻列（1280px scan 时间列被盖 68px、board 104px，900px 命中/创建时间列 91-100% 被盖），且 `bodyWrapper sw==cw` 无可横滚。
- 修复（统一方案）：移除 `fixed="right"`（操作列保留宽 70/150/300/290），表格外层包 `<div style="overflow-x:auto">` + 表格 `style="min-width:700/780/800/900px"`；900px 下横向滚动查看全部列，零重叠。

### P1-4 看板「应对骗子」toast 谎报（交互 P1）
- 文件：`webui/src/views/BoardView.vue` `submitRespond()`（约 139-150 行）
- 问题：LLM 未配置时后端 reply 为空，仍 toast「应对回复已生成」。
- 修复：reply 非空 → 原成功 toast；reply 为空 → `ElMessage.warning('LLM 未配置，未生成应对（可用模板池）')`。

### P2-1 mcp/server.py 未使用 import json（代码审计 P2）
- 文件：`mcp/server.py` 第 31 行，删除 `import json`（全文件无 `json.` 用法）。

### P2-5 CoverView 操作列越界 ~120px（UI 碰撞 P2-5）
- 文件：`webui/src/views/CoverView.vue`（伪装档案表 674/687/715 行附近）
- 修复：操作列 `min-width:280` → `width:230`；假联系信息 `min-width:220` → `180`；档案列 `min-width:260` → `220`。列宽合计 226+184+90+90+150+230=970 ≤ 表宽 970。

### P2-7 cover 删除文案与实际行为一致（交互 P2）
- 文件：`webui/src/views/CoverView.vue` 删除弹窗 placeholder（约 983 行）
- 修复：「其下所有接触记录将由后端级联处理」→「接触审计记录保留（设计），不级联删除」。

### 其余 P2（按要求记录不修）
卡片头 900px 标题/副标题重叠、登录页侧边栏信息泄露、11 端点未消费、蒸馏分词、cover map 空——本任务按指示**记录在案但不修改**。

---

## 二、构建输出

远程 `webui` 目录 `npm run build`：**EXIT=0，built in 19.23s**。
产物（节选）：`EvidenceView-DTAXKsox.js / ConsoleView / ScanView-BKJJoJaf.js / BoardView-BN_rsOWx.js / TrapsView-CZS2RGT5.js / CoverView-DB--7KC6.js` 等全部生成；仅 chunk>900kB 提示（既有，非本次引入）。上传文件与远程 SHA256 一致（7/7 校验通过）。

## 三、专项验证（浏览器实测 + API）

浏览器：ego 浏览器直连 192.168.10.110:9200/ui（登录 admin key 取自远程 data/bootstrap_admin_key.txt）。

| 项 | 结果 |
|---|---|
| P0 /ui/evidence 白屏 | ✅ 不再白屏：卡片/检测表/按钮正常渲染；构建证据包成功（#742，哈希链1节点 intact:true）；冻结弹窗打开、勾选+原因≥20字后「确认冻结」按钮由 disabled→可用（computed 生效）；导出 JSON（200，verified.intact=true）、96110 模板（200） |
| P1-1 /ui/console | ✅ 卡片高 182px（原 3px）；地址输入 h30 可见；加载/新窗口按钮 32px；离线告警条在卡片下方（t270，重叠 -16px 无交集）；iframe 区 620px |
| P1-2 scan（1280px） | ✅ 时间列 l1151-r1311 / 操作列 l1311-r1381 无缝衔接，重叠 0px（原 68px）；wrapper overflow-x:auto |
| P1-2 scan / board / traps（900px） | ✅ scan sw700>cw292 可横滚、零重叠；board 检测速览 sw800>cw390、拟真账号 sw900>cw658 均零重叠；traps sw900>cw658 零重叠（原 创建时间 100% 被盖）；实测 scrollLeft 408/408 可达最右 |
| P1-4 应对骗子 | ✅ API 复验：LLM 未配置（health llm=not_configured）时 `/persona/respond` 返回 `reply:""`；浏览器点「生成应对」toast 实测为「LLM 未配置，未生成应对（可用模板池）」，不再谎报 |
| P2-5 CoverView 操作列 | ✅ 操作列右缘 1227 == 表格右缘 1227，越界 0px（原 +120px） |
| P2-7 删除文案 | ✅ 源码与构建产物均含新文案；旧文案「级联处理」在构建产物中已消失 |
| P2-1 mcp | ✅ py_compile OK；venv python `mcp/server.py --help` 正常（import 链 OK）；`^import json` 已移除 |

截图：`E:\dsh3080工作区存放位置\dsh_gzq1\_t5_fix\shots\console-fixed.png`、`scan-900-scrolled.png`。

## 四、全量 pytest

- **venv python（项目标准环境）：`479 passed, 1 warning in 70.72s`（PYTEST_EXIT=0）** ← 与基线 479 一致，前端/后端修复均未破坏全量。
- 说明：系统 PATH 的 python（Python312，site-packages mcp 为旧版、无 `mcp.server.mcpserver`）下 `tests/test_mcp_mount.py`、`tests/test_r5_backend_fixes.py` 收集报 ModuleNotFoundError（`from mcp.server.mcpserver import MCPServer`，server.py:39）。该错误与本次 json import 删除无关（删除的是未使用 import，不影响模块解析），属**测试环境 python 版本问题**；项目 venv 中 mcp SDK 为 2.x、全量 479 绿。

## 五、约束遵守

- ✅ 未重启主控（webui/dist 静态重建即生效；9200 全程 200）
- ✅ 未提交 git（远程无 git，未做版本操作）
- ✅ 未造脏数据：浏览器构建了 1 个证据包（#742，正常业务产物），冻结流程未实际提交（取消）