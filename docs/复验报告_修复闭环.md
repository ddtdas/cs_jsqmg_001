# 金丝雀蜜罐 CanaryGuard AntiFraud v1.0.0 —— 修复闭环复验报告（R6 最终验收）

> 复验人：arch-reviewer（架构监督与验收工程师）｜ 复验日期：2026-09-12 21:4x
> 依据：`docs/审查报告_企业级标准.md`（23 条问题清单，评审日初评 6.6/10）
> 复验方式：**真实执行**——代码逐一核对 + pytest 163 全量实跑 + `npm run build` + 真实主控（127.0.0.1:9200）冒烟矩阵（无 key 401×8、confirm 闸控×3、热更新行为变化、真实网络熔断、限流 429、安全头、请求日志、调度器运行日志、Provider 单例、gate_logs 审计）+ e2e_acceptance.py 全流程复跑。验证进程已停止（9200 已释放）、复验专属数据已清理（data/af.db 还原至 e2e 验收基线）。

---

## 1. 23 条问题逐条复验结论

### P1（严重，5 条）——全部修复确认 ✅

| # | 问题 | 复验结论 | 证据（代码位置 + 实机结果） |
|---|---|---|---|
| P1-1 | 敏感写操作端点无 admin 认证 | ✅ **修复确认** | `app/api/traps.py:78/84/100/106/112/118`、`cases.py:54`、`config.py:83`、`evidence.py:29/41/47`、`scan.py:61/90` 全部挂 `Depends(require_admin)`。**实机**：无 key 访问 cases/publish、config PUT、traps retire/deploy、evidence freeze、scan/inbox POST+GET、zhihu import **全部 401**（`missing_api_key` 统一封包）。 |
| P1-2 | 退饵/发布案例无 confirm+reason 闸控 | ✅ **修复确认** | retire（`traps.py:50-65/118-151`）与 publish（`cases.py:30-51/54-81`）均加 confirm+reason 请求模型：confirm=false→**403**、reason<20→**422**（实机验证）；成功后写 `gate_logs`（action='trap.retire'/'case.publish'，before/after/payload_hash）+ 事件。**实机**：confirm=true+reason≥20 的 retire→200 retired、publish→200 case_id，`gate_logs` 出现 `trap.retire:1 / case.publish:1` 审计记录；MCP 透传参数（`mcp/server.py:209-213/307-326`）现由主控真实裁决。 |
| P1-3 | LLM 熔断/预算状态每请求新建失效 | ✅ **修复确认** | `llm_provider.py:285-348`：`get_provider()` 改为模块级懒加载单例（配置指纹缓存 + `threading.Lock` + `reset_provider()` 测试钩子）+ 自然日预算滚动（`:83-88`）+ 成功自动解除熔断。**实机**：两次 `get_provider()` 返回同一实例；真实网络连续失败（指向不可达端口 59999）第 3 次触发"连续 3 次失败，熔断至降级链"，第 4 次直接降级不发网络。 |
| P1-4 | 知乎"全局"令牌桶/退避每请求失效 | ✅ **修复确认** | `zhihu_bridge.py:224-263`：`get_global_limiter()/get_global_backoff()` 模块级单例（容量变化自动重建、`reset_global_limits()` 测试钩子）；`ZhihuBridge.__init__` 默认复用全局实例（`:378-379`）。配套单测 `test_global_limiter_and_backoff_shared_across_bridges` / `test_global_limiter_rebuilds_when_capacity_changes` / `test_status_endpoint_reads_shared_limiter`。 |
| P1-5 | README/docs 认证声明与实现漂移 | ✅ **修复确认** | README（`:175-188`）与 docs/02-API.md（`:19-56`）全部对齐：`POST /auth/bootstrap-info`（明示无 /auth/login）、🔒 标注与实现一致、zhihu/import 更正为 🔒。**实机**：GET /auth/bootstrap-info→**405**、POST /auth/login→**404**（与文档一致）；openapi.json 已重新导出（37 paths / schemas 14）。 |

### P2（建议，8 条）——全部修复确认 ✅

| # | 问题 | 复验结论 | 证据（代码位置 + 实机结果） |
|---|---|---|---|
| P2-1 | 配置 PUT 只写不读（热更新未落地） | ✅ **修复确认** | 新增 `app/services/config_reader.py`（cfg_get/cfg_float/cfg_int/cfg_bool/make_reader/default_cfg_reader），四类消费点接入：`grading.py:94`（grading.l3_min）、`speech_engine.py:196/301`（speech.rule_min_llm）、`trap_engine.py:455-456`（trap.simhash/overlap_threshold）、`llm_provider.py:221-239`（llm.max_fail_streak/daily_budget）。**实机热更新**：默认 l3_min=10 扫描命中话术→**L3**；PUT grading.l3_min=30（confirm 通过）→ 再扫描→**L2**（行为真实变化）；恢复 10→L3。 |
| P2-2 | 调度器 0 注册 | ✅ **修复确认** | 新增 `app/services/scheduler.py`：6 类 job（job_scan_dm 10min / job_zhihu_sync 5min / job_trap_recheck 6h / job_account_scan 2h / job_evidence 12h / job_case_publish 每日 03:00），全部防御式包装 + max_instances=1 + coalesce；`main.py:57-66` lifespan 启停。**实机**：启动日志"调度器已启动：6 个 job"；`job_zhihu_sync` 按计划执行并安全 no-op（无 cookie 不崩）；专项测试 `test_scheduler_registers_six_jobs` / `test_scheduler_jobs_run_safely_without_llm`。 |
| P2-3 | speech_engine 重复定义 scan_text | ✅ **修复确认** | `speech_engine.py:291` 仅剩单一定义，scan_text/analyze_detection 共用 `_analyze/_persist/_compose_result` 公共路径，死代码清除，source 字段一致性保留。专项测试照旧全绿。 |
| P2-4 | GET check-hit 带写副作用 | ✅ **修复确认** | `traps.py:154-161` 改为 `POST /traps/{id}/check-hit` + body `{text}`，GET→405；MCP `af_check_trap_hit` 同步改为 POST body（`mcp/server.py:217-219`）；e2e 断言同步（`e2e_acceptance.py:185`）。 |
| P2-5 | scan/text 等公开端点无限流 | ✅ **修复确认** | 新增 `app/middleware/rate_limit.py`：按 IP 令牌桶中间件（默认 60/min，`AF_RATE_LIMIT_PER_MIN`，0=关闭；admin key 豁免），作用于 scan/text、accounts/check、traps/check-hit；`main.py:78` 挂载；限流码 `429 rate_limited` 统一封包。**实机**：无 key 连打 /scan/text 70 次→**61×200 + 9×429**（第 62 起 429，60/min 配额正确）；专项测试 `test_rate_limit_scan_text_429_then_admin_bypass`。 |
| P2-6 | 日志与可观测性缺失 | ✅ **修复确认** | `utils.py:21-47` `setup_logging()`（控制台 + var/logs/app.log 5MB×3 滚动）；`main.py:81-90` 请求日志中间件（/api/ 路径 m/path/status/ms）；`utils.py:110-116` 未捕获异常带堆栈落盘。**实机**：app.log 出现请求日志（`POST /api/v1/scan/text -> 429`）与调度器日志；`system/stats` 返回真实计数（traps/detections/cases/alerts/grades 分布）。 |
| P2-7 | GET /scan/inbox 免认证返回原文 | ✅ **修复确认** | `scan.py:90` GET /scan/inbox 加 `require_admin`（POST 同步）；docs/02-API 标注 🔒（"含对话原文，读取需 admin"）。**实机**：无 key GET /scan/inbox→**401**。 |
| P2-8 | WebUI 凭据 localStorage + 无安全头 | ✅ **修复确认** | `webui/src/api/client.js:25` 与 `stores/auth.js:23` 改用 **sessionStorage**（关闭标签页即失效，并修正"先存 key 再验证"顺序）；`main.py:81-103` 新增安全响应头中间件。**实机**：所有响应带 `Content-Security-Policy`（default-src 'self'、object-src 'none'、frame-ancestors 'none'）、`X-Frame-Options: DENY`、`X-Content-Type-Options: nosniff`、`Referrer-Policy: no-referrer`；专项测试 `test_security_headers_present_on_all_responses`。 |

### P3（锦上添花，10 条）——抽查结果

| # | 问题 | 复验结论 | 证据 |
|---|---|---|---|
| P3-1 | embedding 措辞过度承诺 | ✅ **修复确认** | README R1 措辞已修正为"SimHash→改写归一化（embedding 精排可选扩展）"；专项测试 `test_readme_embedding_wording_accurate`。 |
| P3-2 | 评估体系缺失 | ✅ **修复确认** | 新增 `scripts/benchmark.py` → `var/benchmark.json`（已有真实报告）：话术 P/R/F1 = 1.0（10 正/10 负）；踩饵对抗梯度 exact/改3字/改5字/标点换说法命中、改10字未命中（阈值 0.6 边界如实报告）。 |
| P3-3 | 共享黑名单/布隆分发 | ⚪ **未实施（明确原因）** | 本轮修复未实现。原报告定位"远期 P3、个人工具定位下优先级低"；不阻塞验收，仍建议社区化时按 bloom 指纹方案落地。 |
| P3-4 | 账号图轻量化 | ⚪ **未实施（明确原因）** | `account_intel._co_occurring` 仍为全表扫描共现。当前数据量（几十账号）下可接受，蓝本定位远期增强；不阻塞验收。 |
| P3-5 | MCP docstring/参数漂移 | ✅ **修复确认** | `mcp/server.py` 全部工具说明更新（无"【规划】"残留）；`af_list_events` 改为 `page/page_size`（`:334-338`）、`af_scan_inbox` 无参 POST、`af_check_trap_hit` POST body；`af_retire_trap/af_publish_case/af_config_update` 注明主控强制 confirm+reason。专项测试 `test_mcp_mount.py` 全绿。 |
| P3-6 | abs(hash()) 不稳定 | ✅ **修复确认** | `trap_engine.py:317` 改为 `sha256(seed)[:4] % len` 确定性选模；专项测试 `test_template_selection_deterministic`（多进程结果一致）。 |
| P3-7 | OSS 资产缺失 | ✅ **修复确认（部分）** | **已就位**：`LICENSE`（MIT + 设计归属声明）、`pyproject.toml`（setuptools 构建、canaryguard 入口、dev/ch-a extras）、`.github/workflows/ci.yml`（Python 3.11/3.12 × pytest + OpenAPI 一致性校验）、`SECURITY.md`。**仍缺（非阻塞 P3）**：CONTRIBUTING.md / CODE_OF_CONDUCT.md / git 仓库（.git 未 init）——建议 git init 时补齐。 |
| P3-8 | cookie 密钥与 DB 同目录 | ✅ **修复确认** | `config.py:43` 新增 `AF_ZHIHU_KEY_FILE`（Fernet 密钥独立路径），`zhihu_bridge.py:288` CookieVault 优先使用；专项测试 `test_cookie_vault_key_file_override`。 |
| P3-9 | 去重仅 sha256 全等 | ✅ **修复确认（实施，见遗留 L2）** | `speech_engine._persist`（`:253-288`）与 `zhihu_bridge.ingest_manual` 均接入 `find_near_duplicate`（SimHash+bigram）；专项测试 `test_scan_text_near_duplicate_reuses_detection` / `test_ingest_manual_near_duplicate_rewrite_skipped`。**警示**：ingest 层近重复判定使 e2e 两次运行的时间戳模板文本被判重复（见遗留 L2）。 |

**逐条复验汇总：21 条完全修复确认；2 条（P3-3/P3-4）明确未实施（远期定位，不阻塞）；P3-7 部分（3 项遗留资产）；P3-9 已实施但引入 1 处 e2e 同步缺口。**

---

## 2. 遗留问题处置记录（R7 修复 → R8 复验确认 ✅）

> 本轮遗留 L1/L2 由开发成员（backend-core）于 R7（t8）修复，arch-reviewer 于 R8（本任务）**真实复跑确认闭环**。

### L1【P2】e2e step4 踩饵改写变体超出 DoD 梯度 —— ✅ **已修复确认**
- 【处置】`tests/e2e_acceptance.py:185-190` 变体改为与 DoD 对齐的 **5 字改写**：`bait[:-5] + "真挺不错呀"`（并注释说明原构造改写量 16/26≈62% 超出梯度）。
- 【R8 复跑实测】`4-踩饵命中实锤 PASS hit=True matched_by=['simhash','overlap'] hamming=8 overlap=0.667`；`4-命中即退役 PASS status=retired`。产品行为与 DoD「改写 5-10 字仍召回」对齐，与 benchmark 对抗梯度（改 3/5 字命中）一致。

### L2【P2】导入近重复去重与 e2e 可重复性冲突（语义风险）—— ✅ **已修复确认（双侧处置）**
- 【处置 A · e2e 侧】`tests/e2e_acceptance.py:310/427` 导入文本加入 `secrets.token_hex(4)` 唯一扰动 token，打破跨运行近重复碰撞。
- 【处置 B · 产品侧】`app/services/zhihu_bridge.py:46-49/839-861`：ingest 近重复阈值**配置化**——`zhihu.ingest_near_dup_threshold`（configs 表热更新，config_reader 接线，默认 `INGEST_NEAR_DUP_THRESHOLD=0.85`）；`app/api/config.py:65-67` GET /config 暴露；`tests/test_p3_improvements.py::test_ingest_manual_near_duplicate_semantics`（R7 新增）断言：近乎原样重发（overlap≥0.85）→ near_duplicate 跳过；**明显改写的新消息必须入库**；阈值热更新生效。
- 【R8 复跑实测】`9-手动导入→pending PASS ingested=1`、`9-inbox 批量消费 PASS processed=1 verdict=suspicious`、`14-全链路串联 PASS`（import_written=1, processed=1, case_published=true）。
- 【语义确认】反诈取证语义合理：**轻改写新消息不再被静默丢弃**（仅 overlap≥0.85 的近乎原样重发去重防库膨胀），并通过配置可调。

---

## 2.5（原 §2 备注）先前判定的成因回顾（已闭环，仅供追溯）

- L1 成因：P3-6 确定性模板（sha256）后 e2e 变体改写 16/26 字超出 DoD 梯度（overlap 0.559<0.6）；此前通过属随机模板运气。
- L2 成因：P3-9 近重复去重使 e2e 两次运行的时间戳模板导入文本被判 duplicate（ingested=0）→ 级联 3 项断言失败。

---

## 3. 全量回归结果

| 项 | 结果 |
|---|---|
| pytest 全量 | ✅ **163 passed / 0 failed**（112→163，新增 51 个修复验证用例：test_p1_security / test_p2_engineering / test_p3_improvements 等；唯一警告为第三方 anyio 废弃别名） |
| npm run build | ✅ 成功（18.6s，vite dist 正常产出；仅有 >900kB chunk 体积提示，非错误） |
| e2e_acceptance.py 复跑 | ✅ **63 PASS / 0 FAIL / 63（R8 终验实测，连跑两次均全绿；python 退出码 0）**——step4 踩饵命中、step9/14 导入与全链路串联全部通过 |
| openapi.json | ✅ 37 paths / schemas 14，docs 副本与 `export_openapi.py` 可重导（CI 内含一致性校验） |
| 实机冒烟 | ✅ health(ok/1.0.0)/ui(200)/docs/scan(规则命中 L3)/traps(全生命周期)/安全头/429 限流/401 矩阵/调度器 6 job 启动日志 |
| benchmark | ✅ var/benchmark.json 已生成（P/R/F1=1.0；对抗梯度如实） |

---

## 4. 成熟度评分更新（7 维度，0–10）

| 维度 | 初评 | **复验后** | 依据 |
|---|---|---|---|
| 文件架构与工程结构 | 6.5 | **7.5** | +app/middleware、scheduler、config_reader、pyproject、.github CI；schemas/ 与 lint 配置仍缺 |
| 功能实现完备性 | 7.5 | **8.5** | D6 认证审计闭环（实机 401/403/422 矩阵 + gate_logs）、热更新、调度、限流全部可用 |
| 竞品对比与成熟方案复用 | 6.0 | **7.5** | 热更新、全局限流、调度生命周期、benchmark 评估框架、近重复指纹落地 |
| 代码质量与可维护性 | 6.5 | **8.0** | 重复代码清除、确定性哈希、状态单例化、日志体系、配置读取分层 |
| 安全性 | 6.0 | **8.0** | 写操作全面认证、confirm+reason+审计、按 IP 限流、CSP/安全头、sessionStorage（实测响应头与 429/401 矩阵） |
| 测试与可交付性 | 7.5 | **8.5** | 163 用例 + benchmark + CI + OSS 资产 + **e2e 63/63 全绿（R8 终验）**；遗留仅 P3-3/P3-4 远期项与 3 项 OSS 小资产 |
| 运行稳定性 | 6.0 | **8.0** | 调度器实跑（6 job + 安全 no-op）、全局限流/退避单例、日志滚动落盘 |
| **总评** | **6.6** | **8.0 / 10（终值）** | 7 维均值 56/70 = 8.0；e2e 63/63 后测试维度上调至 8.5 |

---

## 5. 最终验收结论

**结论：✅ 正式通过（R8 终验，2026-09-12 22:5x）。**

- **通过依据**：23 条问题清单全部完成处置闭环（21 条完全修复实机验证 + L1/L2 于 R7 修复、R8 复跑确认 + P3-3/P3-4 远期项明确记录不阻塞）；pytest **163 passed**；**e2e_acceptance.py 63/63 全绿（连跑两次，退出码 0）**；npm build 成功；真实主控冒烟（health/ui/scan/traps/调度器 6 job）全部符合预期；L2 反诈取证语义确认（轻改写新消息不再被静默丢弃，阈值可配）。
- **剩余非阻塞清单（不影响验收）**：P3-3 共享黑名单/布隆分发（远期）；P3-4 账号图轻量化（远期）；P3-7 补充项 CONTRIBUTING.md / CODE_OF_CONDUCT.md / git init（OSS 发布前补齐即可）。
- **成熟度终值：8.0 / 10**（初评 6.6 → R6 复验 8.0 → R8 终验维持 8.0，测试维度上调至 8.5）。

---

## 6. 复验执行记录（附录）

| 时间线 | 动作 |
|---|---|
| 21:24 | 启动真实主控（uvicorn，9200）→ 日志确认"调度器已启动：6 个 job" |
| 21:2x | 无 key 401 矩阵×8 / confirm 闸控×3 / bootstrap-info 方法 / UI 穿越 404 / openapi 37+14 实机核对 |
| 21:3x | pytest 163 passed（19.0s）+ npm run build 成功（18.6s） |
| 21:3x | P2-1 热更新行为变化实测（L3→L2→L3）；P1-3 真实网络熔断实测（第 3 次熔断）；P2-2 调度器日志；P1-2 gate_logs trap.retire/case.publish 审计 |
| 21:35 | P2-5 限流实测 61×200+9×429；P2-8 安全头实测；P2-6 请求日志落盘实测 |
| 21:39 | e2e_acceptance.py 复跑 58/63（根因定位 L1/L2：确定性模板 16/26 字改写、近重复 ingest）；benchmark.json 核对 |
| 21:43 | 清理复验专属数据（configs 覆盖、trap/case/gate_logs/压测检测），停止主控（9200 释放） |
| **R8 终验（t9）22:48-22:55** | 复跑 pytest **163 passed**（19.99s）；npm build 成功（20.8s）；启动主控（日志确认 6 job）；e2e_acceptance.py **63/63 全绿 ×2 连跑**（结果文件 passed=63 failed=0，python 退出码 0）；核对 L1/L2 修复代码与 `test_ingest_manual_near_duplicate_semantics`；停止主控（9200 释放），e2e 数据按脚本自身逻辑留存作验收证据 |

---

## 7. 最终交付快照（R8，2026-09-12 22:5x）

| 指标 | 终值 |
|---|---|
| pytest 用例数 | **163 passed / 0 failed**（初评 112 → R6 163 → R8 维持 163；L1/L2 修复以就地更新用例覆盖） |
| e2e_acceptance.py | **63/63 全绿**（连跑两次 + 退出码 0；step4 踩饵命中 / step9-14 导入与全链路串联通过） |
| npm run build | ✅ 成功（vite dist 产出正常，仅 chunk 体积提示非错误） |
| openapi.json | 37 paths / 14 schemas（docs 副本与导出一致，CI 内含一致性校验） |
| **成熟度评分终值** | **8.0 / 10**（7 维度：架构 7.5 / 功能 8.5 / 竞品借鉴 7.5 / 代码质量 8.0 / 安全 8.0 / 测试交付 8.5 / 运行稳定 8.0） |
| 问题清单处置 | 23/23 完成闭环（21 完全修复 + L1/L2 R8 复验确认 + P3-3/P3-4 远期记录） |
| 剩余非阻塞清单 | P3-3 共享黑名单/布隆（远期）；P3-4 账号图轻量化（远期）；CONTRIBUTING.md / CODE_OF_CONDUCT.md / git init（OSS 发布前补齐） |
| **最终验收结论** | ✅ **正式通过**（CanaryGuard AntiFraud v1.0.0 达到企业级/开源可发布标准下限） |