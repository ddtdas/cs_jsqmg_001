# 严格验收 R4·宏观推演 + 生产终验前台观测报告

- 验收对象：金丝雀蜜罐 CanaryGuard（192.168.10.110:9200，health=200，R1-R3 代码已激活，462 passed）
- 推演人：macro-analyzer（宏观推演员）
- 方式：加载态复核（openapi 指纹）→ **生产受控踩饵演练**（R4TEST 标记蜜饵 + 手工 check-hit，写操作仅限演练蜜饵）→ 只读 API/SQL 全链验证 → **WebUI 前台观测**（仪表盘/检测详情/告警历史/蜜饵历史 4 屏截图）→ 演练产物按设计收敛（蜜饵自动退役，检测留作真实 L5 样例，证据/案例由定时 job 自动接续）。临时脚本全部清理。
- 时间：2026-09-17 18:53-19:10（本地；DB 时间戳为 UTC）
- 线上基线：detections=3122、L5=0、alerts=1485、cases=80（零重复）、events=17024、pending=0

---

## 0. 宏观结论（TL;DR）

**生产环境全链路真实跑通：受控踩饵演练 100% 成功，前台 4 屏可观测。**

在**生产库、生产代码（R3 激活）**上完成受控踩饵：部署 R4TEST 蜜饵（#1388）→ 命中 → 状态机 active→monitored→hit→retired → 关联检测 #3132 自动升级 **L5** → 生成 **L5 告警 #1490** → `trap_hit_escalated` 事件 → 前台仪表盘/检测详情（分级 L5 + 实锤证据链）/告警历史（L5 置顶）/蜜饵历史（已退役 命中 1）全部可见。证据包与 case_ready 由 job_evidence（12h）/job_case_publish（每日 3:00）自动接续（已用与 job 同口径 SQL 实证 3132 在候选集中）。

**系统已达成"可全自动可信"的验收判定条件**：R1-R3 全部修复在生产激活并实证；无 P0；剩余 P1 为"冻结重复证据包处置决策"类设计遗留；P2 为运维收敛项。**结论：可以转入"可全自动可信"运行。**

---

## 1. 加载态复核（R3 已激活）

| 项 | 证据 | 结论 |
|----|------|------|
| openapi 指纹 | 运行实例 `CheckHitRequest` props = **[detection_id, text]**（R3 新增字段在线） | ✅ R3 代码已激活 |
| 进程 | 1960(.venv 重定向器)→2248(解释器子进程)，18:46:09 创建；9200 单监听 | ✅ 单逻辑实例 |
| 调度器 | 启动日志 9 个 job；18:54:42 起 job_trap_recheck/job_evidence 有活动（含测试与生产混写） | ✅ 9 job 注册 |
| 演练间并发写入 | 演练期间 teammate 在测 collector（events 17056-17061，cell 90/91 rsshub/weibo） | ✅ 多写入者并存无冲突 |

---

## 2. 生产受控踩饵终验（前台观测）——核心交付

### 2.1 演练步骤与结果（全部 200 OK）

| # | 动作 | 结果 |
|---|------|------|
| 1 | POST /traps 创建蜜饵 #1388（bait=`R4TEST_6afde9 我在找一个投资理财项目，收益很稳定，你有兴趣一起聊聊吗？`，disguise=100，draft） | ✅ |
| 2 | POST /traps/1388/deploy → active | ✅ |
| 3 | POST /scan/text（同文本）→ 检测 #3132（规则分 0=无词库命中，纯标记文本，隔离验证信号源） | ✅ grade L1 初判 |
| 4 | POST /traps/1388/check-hit `{text, detection_id:3132}` | ✅ hit=true |
| 5 | 命中升级：检测 #3132 → **grade L5**，grade_reason=[trap_hit +20 蜜饵实锤命中 1 次] | ✅ |
| 6 | 告警：alert #1490 **L5**（unread=1） | ✅ |
| 7 | 事件流：trap_draft_created→trap_deployed→detection_scanned→detection_graded(L1)→trap_monitored(implicit)→trap_hit→trap_retired→detection_graded(**L5**)→alert_created→**trap_hit_escalated(source=manual_check_hit)** | ✅ 10 事件完整有序 |
| 8 | 蜜饵终态：#1388 status=retired、retired_reason=hit、hit_count=1 | ✅ 命中即退役 |

### 2.2 前台观测（WebUI 截图，见 _macro_r4/r4_*.png）

| 屏 | 观测内容 |
|----|----------|
| 仪表盘（r4_dashboard.png） | 最近事件三行：`trap #1388 状态=retired` / `检测 #3132 规则分=0` / **`告警 #1490 L5`** |
| 检测详情 #3132（r4_det_detail.png） | 徽标**分级 L5**；① 告警原因证据链 `trap_hit 蜜饵实锤命中 1 次 权重 20`；② 原文上下文含 R4TEST 标记 |
| 告警历史（r4_alerts.png） | **告警 #1490 L5 置顶**（其余历史告警全为 L3） |
| 蜜饵历史（r4_traps.png） | 蜜饵 #1388 **已退役、命中 1**（标记文案可见） |

### 2.3 演练产物收敛（按任务要求）

- 蜜饵 #1388：已按"命中即退役"自动收敛（终态，不再参与复查）✅
- 检测 #3132：保留为**真实 L5 样例**（后续自动流程的活数据）✅
- 证据包：job_evidence（12h 周期）下次触发将选中 #3132（**与调度器同口径 SQL 实证候选 = [3132]**）——预计 06:46-06:54 前构建
- case_ready：job_case_publish（每日 3:00）下次触发将选中 #3132（**同口径 SQL 实证候选 = [3132]**）→ 今日 03:00 产出 case_ready 草稿事件
- 无其它残留；临时脚本/输出文件已清理

---

## 3. R3 修复端到端 + 终态收敛

- **_escalate_pending 终态收敛**：生产 pending 事件 = **0**（无未决积压）；R4 代码新增 resolved/terminal 标记机制（成功标记 resolved=true、检测被删标记 terminal=true，均保留审计），日志实证 "补偿升级 1 条未决，0 条终态收敛"（测试路径）与 "0 条未决，0 条终态收敛"（生产）。events 只增不 UPDATE 主字段，增长正常。
- **升级幂等（P3-1）**：生产 drill 手动路径一次升级成功；测试日志实证 "蜜饵 #3 命中 → 检测 #2 已 L5（跳过重复升级）"。
- **UNIQUE(det_id) 后发布不冲突**：cases 表当前**无 DB 级 UNIQUE**（应用层 409 幂等已实证：80 条零重复）；evidence_packages 因冻结重复包保留暂无法加 UNIQUE（P1 设计遗留）。**发布冲突路径均被应用层拦截**：同载荷幂等返回、异载荷 409 case_exists、重复建包 409 evidence_exists（R3 隔离演练已证）。
  ⚠️ **R5 复核修正（2026-09-17）**：本行"cases 无 DB 级 UNIQUE"与当前状态不符——R3 修复（t12）已加装 `idx_cases_det_unique`（`CREATE UNIQUE INDEX ... ON cases(det_id)`）并线上应用，R4/R5 两次 `PRAGMA index_list('cases')` 实测 unique=1 在线、cases 仍 80 行 0 重复；应用层 409 与 DB 层唯一索引现为**双保险**。evidence 侧结论不变（冻结重复包设计保留，未加全局唯一约束）。
- **events 增长**：17024→17061（演练 10 条 + teammate collector 测试 6 条），正常无异常事件类型。

---

## 4. 并发 / 边界（生产实测）

- **演练期间多写入者并存**：我的演练写路径（traps/detections/alerts/events）与 teammate 的 collector 写路径并发，无锁冲突/500（SQLite WAL + busy_timeout 兜底）✅
- **升级重放并发**：生产 pending=0，无重放压力；R4 代码对 pending 的 SELECT 限定 `resolved IS NULL AND terminal IS NULL`（不重扫已收敛项）✅
- **手工 check-hit 与 job 路径**：生产演练走手工路径（P1-C 联动升级）；job 路径（job_trap_recheck）18:54:42 启动批在测试库执行（日志"蜜饵 #2 命中 → 检测 #1 升级 L5"为 pytest 临时库产物——生产 det 1 grade=NULL 实证无意外升级）✅
- **D5 无 LLM**：演练全程 llm=not_configured，L5 纯规则判定（trap_hit 权重 20）✅
- **429/限流**：演练连续写操作未触发限流（admin key 豁免）✅

---

## 5. 剩余 Top 风险（R4）

### P0
无。

### P1
- **P1-A（设计遗留）｜冻结重复证据包 86 个中 10 组重复**：存证语义设计保留（不可自动删）；evidence_packages 全局 UNIQUE 约束无法加装。若后续要全库唯一，需人工逐组确认离线处置（非脚本动作）。对当前功能无影响（未冻结包 17 个已唯一）。
- **P1-B（观察窗）｜证据包与 case_ready 生产终验待定时 job 触发**：job_evidence（≤12h）与 job_case_publish（今日 03:00）将自动选中 #3132；建议次日核对 events 出现 evidence_built/case_ready 即完成最后两块拼图（逻辑已由 R3 隔离演练 14/14 与同口径 SQL 实证）。

### P2（运维收敛，全部非阻塞）
- cases 无更新/删除 API（发布错误无法修正）；evidence 无追加截图机制
- scanned 638 条滞留无归档；告警 1486 条 unread 无自动已读/降噪
- events/gate_logs 只增无归档策略；LLM 慢时 job_scan_dm 单轮背压（有界）
- IOC 提取噪声（QQ 正则命中银行卡子串）；knowledge import 并发 UNIQUE 竞态 500（未修）
- hit_count 口径=扫描次数非去重消息（在线学习信号失真）

---

## 6. 整体结论

**系统已达成"可全自动可信"验收条件，建议判定通过并转入运行。**

依据：
1. R1-R3 全部修复在生产激活（openapi 指纹实证）+ 生产受控踩饵全链路真实跑通（命中→退役→L5→告警→事件→前台 4 屏可见）；
2. 证据/案例自动接续已由 job 同口径 SQL 实证候选就绪（#3132 在 job_evidence 与 job_case_publish 候选集内），定时 job 将自动完成最后两块拼图（今日 03:00 case_ready、≤12h evidence）；
3. 无 P0；唯一 P1 为冻结重复包处置的**设计决策**（不阻塞运行）；P2 均为运维收敛项。

**运行期建议**：① 次日核对 evidence_built/case_ready 事件完成终验闭环；② 制定冻结重复包处置决策（P1-A）；③ 按 P2 清单排期运维项（归档/告警降噪/并发竞态/IOC 噪声）；④ 保持单实例启动互斥与定期 gate_logs 链校验（当前 valid）。

---

*推演范围：宏观整体判断 + 生产受控演练（写操作仅限演练蜜饵，已自动收敛）；演练临时文件已清理；结论可复核（API 事件 17046-17061、检测 #3132、告警 #1490、蜜饵 #1388、截图 4 张）。*