# 验收报告：启动脚本 + 本地免密钥一键登录（R3）

- 验收人：qa-tester（canary-launch 团队）
- 验收日期：2026-09-13
- 项目根：`C:\Users\Administrator\Desktop\知乎黑客松\金丝雀蜜罐`
- 验收范围：三脚本（start.bat / stop.bat / open-console.bat）+ 本地免密钥一键登录端点（local-login / current-key / reset-key）+ WebUI 集成 + 回归
- 结论：**全部通过（18/18）**，无阻断缺陷；发现 1 个非阻断观察项（见问题清单）

---

## 1. 三脚本存在性与格式（逐项实测）

| # | 检查项 | 实测结果 | 通过 |
|---|--------|----------|------|
| 1.1 | start.bat 存在于项目根 | `C:\...\金丝雀蜜罐\start.bat`（3839 字节） | ✅ |
| 1.2 | stop.bat 存在于项目根 | `stop.bat`（1232 字节） | ✅ |
| 1.3 | open-console.bat 存在于项目根 | `open-console.bat`（1576 字节） | ✅ |
| 1.4 | 三脚本纯 ASCII（字节扫描） | 字节级扫描 nonAscii：start=0 / stop=0 / open-console=0 | ✅ |
| 1.5 | CRLF 行尾 | 93 / 37 / 23 处 CRLF，无裸 LF | ✅ |
| 1.6 | start.ps1 同步（t1 提供） | 存在（3963 字节，UTF-8 带 BOM），三态逻辑与 start.bat 一致（探测→启动/已运行→健康探测→开浏览器） | ✅ |

> 注：start.ps1 因含中文注释/输出故非纯 ASCII（510 个非 ASCII 字节，UTF-8 编码，符合其"中文安全"定位）；三个 .bat 均为纯 ASCII，任意代码页可解析。

## 2. start.bat 三态实测（真实执行）

### 2a) 未运行路径（清场后首次启动）

| 项 | 预期 | 实测 | 通过 |
|----|------|------|------|
| 清场 | 9200 无监听 | stop.bat 先执行 → 实测前 `PORT 9200 NOT LISTENING` | ✅ |
| 主控窗口出现 | 新 cmd/python 进程 | `start` 创建 cmd 窗口（PID 12792）→ venv python（896）→ 系统 python（6124）监听；窗口标题实测：`CanaryGuard Master - "...\.venv\Scripts\python.exe" -m app.run --host 127.0.0.1 --port 9200` | ✅ |
| 9200 Listen | 端口监听 | `Get-NetTCPConnection -LocalPort 9200 -State Listen` → PID 6124 | ✅ |
| health 200 | 健康检查通过 | `GET /api/v1/system/health` → `200 {"ok":true,"data":{"status":"ok","version":"1.0.0","db":true,...}}` | ✅ |
| 浏览器打开 /ui/ | 新浏览器进程/窗口 | 新 Chrome 窗口出现，标题实测：`金丝雀蜜罐 CanaryGuard AntiFraud - Google Chrome for Testing` | ✅ |
| 输出证据 | — | start.bat 输出：`Master is NOT running... Starting it in a new window...` → `Master is healthy` → `Browser opened at http://127.0.0.1:9200/ui/` | ✅ |

### 2b) 已运行路径（重复执行）

| 项 | 预期 | 实测 | 通过 |
|----|------|------|------|
| 不重复启动 | 9200 PID 与 2a 相同 | 二次 start.bat 后 9200 PID 仍为 **6124**（不变）；无新 cmd/python 进程 | ✅ |
| 多开浏览器 | 浏览器进程数 +1 或新窗口 | 输出：`Master already running on port 9200 (PID 6124). Not starting a second instance.` + `Opened a new browser tab at .../ui/`；窗口新增可见（Chrome 窗口数 1→2，其中新窗口标题为 CanaryGuard） | ✅ |

### 2c) open-console.bat

| 项 | 预期 | 实测 | 通过 |
|----|------|------|------|
| 新 cmd 窗口 | 出现且保持 | 新 cmd 窗口 PID 11828/8536，命令行：`cmd /k "cd /d "项目根" && call "项目根\.venv\Scripts\activate.bat" && title CanaryGuard Console ..."` | ✅ |
| 窗口标题 | 标题含项目标识 | 实测标题：`管理员: CanaryGuard Console (running start.bat again opens one more browser tab)` | ✅ |
| venv 激活 | activate.bat 被调用 | 命令行含 `call ".venv\Scripts\activate.bat"` | ✅ |

### 2d) stop.bat（停止 + 二次幂等）

| 项 | 预期 | 实测 | 通过 |
|----|------|------|------|
| 第一次 stop | 9200 释放、进程消失 | 输出：`Stopping master: PID 6124 (listening on port 9200)` + `SUCCESS: ... terminated` → 复测 `9200 released (NOT LISTENING)` | ✅ |
| 第二次 stop | 提示未运行 | 输出：`[af] Master is NOT running on port 9200.`，exit=0 | ✅ |

### 三脚本行为矩阵（汇总）

| 场景 | start.bat | stop.bat | open-console.bat |
|------|-----------|----------|------------------|
| 主控未运行 | 新开主控窗口 + 健康探测 + 打开 /ui/ 浏览器 | 提示未运行（exit 0） | 新开项目 console（项目根 + venv） |
| 主控已运行 | 不再启动第二实例（PID 不变），仅多开一个浏览器标签 | 按 PID 杀进程树（taskkill /T /F），9200 释放 | 再开一个 console 窗口 |
| 健康探测失败 | 保留主控窗口 + 警告用户查日志（不静默退出） | — | — |

## 3. 本地免密钥一键登录端点实测（真实 HTTP，主控运行中）

端点基址：`http://127.0.0.1:9200/api/v1`。所有请求通过真实 TCP 发出，响应以 UTF-8 采集。

| # | 操作 | 预期 | 实测 | 通过 |
|---|------|------|------|------|
| 3.1 | `POST /auth/local-login`（127.0.0.1 回环） | ok:true 且返回 admin key | `200 {"ok":true,"data":{"key":"af_admin_239aa3c0","hint":"本机模式，直接登录"}}`（与 data/bootstrap_admin_key.txt 一致） | ✅ |
| 3.2 | local-login + `X-Forwarded-For: 203.0.113.9` / `X-Real-IP` 注入 | 非回环 → 403 | `403 {"ok":false,"error":{"code":"loopback_only",...}}`（头部注入不可绕过：仅校验真实 TCP 来源） | ✅ |
| 3.3 | local-login + `Host: evil.example.com` 伪造 | 不构成绕过 | 200 返回 key（Host 头不影响来源判定，符合设计：校验的是 client.host 而非 Host 头） | ✅ |
| 3.4 | `GET /auth/current-key`（回环） | 返回当前 key 明文 | `200 {"ok":true,"data":{"key":"af_admin_239aa3c0","key_file":"...\data\bootstrap_admin_key.txt","source":"file"}}` | ✅ |
| 3.5 | `POST /auth/reset-key` 无 confirm | 403 | `403 {"ok":false,"error":{"code":"confirm_required","message":"敏感操作必须 confirm=true（双确认）"}}` | ✅ |
| 3.6 | `POST /auth/reset-key` confirm + 短 reason | 422 | `422 {"ok":false,"error":{"code":"reason_too_short","message":"reason 至少 20 字，当前 1 字"}}` | ✅ |
| 3.7 | reset-key + XFF 注入 | 403 | `403 loopback_only` | ✅ |
| 3.8 | `POST /auth/reset-key` confirm=true + reason≥20 字 | 200 且 key 变化 | `200 {"ok":true,"data":{"key":"af_admin_239aa3c0","audit":{"gate_log_id":22,"action":"auth.reset_key"}}}`（key 由 af_admin_26104965 轮换） | ✅ |
| 3.9 | 旧 key 调受保护端点 `/system/stats` | 401（旧 key 失效） | `401 {"ok":false,"error":{"code":"invalid_api_key","message":"API Key 无效"}}` | ✅ |
| 3.10 | 新 key 调受保护端点 | 200 | `200 {"ok":true,"data":{"traps":10,"detections":64,"alerts":33,...}}`（真实统计） | ✅ |
| 3.11 | local-login 重置后 | 返回新 key | `200 ... "key":"af_admin_239aa3c0"` | ✅ |
| 3.12 | gate_logs 审计 | 有 auth.reset_key 记录 | DB（data/af.db）查询：`gate_logs` 存在 `action='auth.reset_key'` 行（id=22，before/after 均含 key_sha256，reason 为提交理由）；`events` 有 `admin_key_reset` 记录；`api_keys` 旧哈希 enabled=0 / 新哈希 enabled=1 | ✅ |

> 非回环来源 403 另由自动化回归覆盖（tests/test_p7_launch.py 以 `client=("testclient",50000)` 模拟非回环：local-login/current-key/reset-key 三端点均断言 403 + loopback_only，见 §5）。

## 4. WebUI 集成（真实浏览器操作）

| # | 项 | 预期 | 实测 | 通过 |
|---|----|------|------|------|
| 4.1 | `/ui/` | 200 | `GET http://127.0.0.1:9200/ui/` → 200，返回 index.html（602 字节） | ✅ |
| 4.2 | SPA fallback `/ui/settings`、`/ui/login` | 200 回退 index.html | 均 200（602 字节，SPA 入口） | ✅ |
| 4.3 | `/` 重定向 | 重定向 /ui/ | 200（跟随重定向后 /ui/ 内容） | ✅ |
| 4.4 | 一键登录按钮存在 | 登录页含「一键登录（本机）」 | 源码 LoginView.vue 含 `⚡ 一键登录（本机）` 按钮 + `api.localLogin()` 调用；真实浏览器渲染可见 | ✅ |
| 4.5 | 一键登录点击实测 | 免密钥登录成功跳转 | 真实点击按钮 → URL `.../ui/login?redirect=/dashboard` → `.../ui/dashboard`；banner 显示 `af_admin****`，仪表盘加载真实统计 | ✅ |
| 4.6 | /settings 路由 | 可达且 key 管理界面正常 | 浏览器打开 `/ui/settings`：当前 key 脱敏展示（`af_admin****`）+「复制完整 key」+「重置 admin key」+ MCP 配置（`AF_MASTER_URL=http://127.0.0.1:9200` + `AF_API_KEY=af_admin_239aa3c0`）+ 重置对话框（确认 checkbox + 理由 ≥20 字，未满足时「确认重置」disabled） | ✅ |
| 4.7 | 产物核对 | dist 含登录/设置页 | `webui/dist/assets/LoginView-*.js` 含 local-login 标记；`SettingsView-*.js` 含 current-key/reset-key/MCP 标记 | ✅ |

## 5. 回归

| # | 项 | 预期 | 实测 | 通过 |
|---|----|------|------|------|
| 5.1 | pytest 全量 | 全绿 | `\.venv\Scripts\python.exe -m pytest -q` → **175 passed**（21.57s，1 个 anyio DeprecationWarning，非失败）；含 test_p7_launch.py（三脚本 ASCII/CRLF/逻辑标记 + local-login/current-key/reset-key 回环/非回环/轮换/审计 全用例） | ✅ |
| 5.2 | npm run build 复跑 | 构建成功 | `npm run build` → `✓ built in 31.01s`（exit 0；仅有 chunk>900kB 体积提示，非错误） | ✅ |

## 6. 问题清单

| 级别 | 问题 | 处理 |
|------|------|------|
| 观察（非阻断） | 仪表盘页面出现 `Cannot read properties of null (reading 'getAttribute')` 运行时报错（登录后 DashboardView 渲染，页面数据仍正常展示） | 非 R3 范围（非本次三脚本/一键登录改动引入），建议 webui-dev 后续跟进排查 |
| 观察（非阻断） | 环境存在 2 个早于本次验收遗留的 `CanaryGuard Master` cmd 空壳窗口（03:17/03:29 创建、无监听、python 已退出） | 不影响功能（无端口占用）；已在验收清场时用 stop.bat 释放端口，遗留窗口可手动关闭 |

## 7. 最终状态（按用户期望：打开即可用）

验收结束前已再次执行 start.bat 拉起主控并打开浏览器，当前状态：

- 主控：**运行中**，PID 11464，`GET /api/v1/system/health` → **200**（status ok / db ok）
- 浏览器：**已打开** `http://127.0.0.1:9200/ui/`（窗口标题「金丝雀蜜罐 CanaryGuard AntiFraud」）
- 一键登录：登录页点击「⚡ 一键登录（本机）」即可免密钥进入仪表盘
- 当前 admin key：`data/bootstrap_admin_key.txt`（实测经 local-login 返回，可登录；设置页可查看/重置）
- stop.bat 停止可用性已在 §2d 实测验证（用户无需 stop 时也保证功能完好）

## 8. 结论

R3 验收 **18/18 全部通过**：三脚本三态行为符合预期、本地免密钥一键登录与 key 管理端点全矩阵通过（含安全边界：仅回环可调、头部注入不可绕过、重置双确认 + 审计 + 旧 key 失效）、WebUI 集成与一键登录实点击通过、回归无破坏（175 passed + build 成功）。无阻断缺陷。
