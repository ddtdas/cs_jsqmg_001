# 严格验收 R5·收尾宏观终验报告（五轮总结 + 最终判定）

- 验收对象：金丝雀蜜罐 CanaryGuard（192.168.10.110:9200，health=200，R1-R4 全部落地，462 passed）
- 推演人：macro-analyzer（宏观推演员，五轮全程）
- 方式：只读 API/SQL 探针（含 #3132 闭环核对、events 完整性、审计链复验）+ 日志/进程复核；全程只读不改、不重启；临时文件已清理。
- 时间：2026-09-17 19:2x（本地）；DB 时间戳为 UTC
- 线上终态基线：detections=3123、L5=1（#3132 演练样例）、alerts=1486、cases=80（零重复）、evidence=103（未冻结 17 全唯一、冻结 86 含 10 组设计保留）、events=17046（max id 17067，历史删行 21）、pending=0、pending 升级=0、gate_logs=1825（**链 valid=true**）

---

## 0. 最终结论（TL;DR）

**正式判定：通过验收，系统可以转入运行（"可全自动可信"状态成立）。**

- **P0 = 0，P1（缺陷类）= 0**：五轮发现的所有 P0/P1 缺陷（await 空转、L5 不可达、evidence LIKE 误判、cases 重复、升级非原子、手工不联动等）均已在 R1-R4 修复并在生产/隔离环境实证闭环。
- 剩余 1 项 **P1（管理决策类，非缺陷）**：86 个冻结证据包中含 10 组重复（存证语义设计保留，不可自动删）→ 需团队人工决策是否处置，**不阻塞运行**。
- 剩余 **P2 运维清单**（12 项，全部非阻塞）：详 §4。
- **R4 P1-B 观察窗尚未到期**：#3132 证据包与 case_ready 的定时 job 将在 **今日 19:00 UTC（case_ready，03:00 本地）与 ~23:00 UTC（evidence，06:00-07:00 本地）** 自动产出（候选集已实证包含 #3132），建议届时复核 events 完成最终闭环。

---

## 1. R4 P1-B 观察窗补核（#3132 自动闭环）

| 项 | 当前状态 | 说明 |
|----|----------|------|
| 检测 #3132 | grade=L5、status=processed ✅ | R4 演练升级保持 |
| 告警 #1490 | L5、unread=1 ✅ | 保持 |
| trap_hit_escalated 事件 17055 | 存在 ✅ | source=manual_check_hit |
| 证据包 | **尚未构建**（预期内） | job_evidence 为 12h 周期，自调度器 10:54 UTC 启动起算下次 ≈ **22:54 UTC**；候选 SQL 实证 `[3132]` 在列 |
| case_ready | **尚未产出**（预期内） | job_case_publish 为 CronTrigger 03:00（本地）→ 下次 **19:00 UTC（今日 03:00 本地）**；候选 SQL 实证 `[3132]` 在列 |
| 自动接续链路 | 逻辑已证 | R3 隔离演练 14/14（含 job_evidence 建包 + job_case_publish 就绪）；R4 生产同口径 SQL 实证；只差定时触发时间窗 |

**结论**：P1-B 为"定时窗口未到"，非缺陷。**请在 19:00 UTC 之后复核 `events(kind='case_ready')`、22:54 UTC 之后复核 `evidence_built` 含 3132**，即完成"可全自动"最后一块拼图（预计今日内自动出现）。

---

## 2. 五轮总结性宏观推演（是否还有未覆盖的整体问题）

对五轮已覆盖域做最终扫描（数据流/并发/边界/状态一致性），**未发现新的 P0/P1 级整体问题**。逐域结论：

1. **数据流（私信→检测→分级→告警→社工库→证据→案例→知识库→话术库）**：全链在生产激活并实证（R4 演练 = 私信/检测→分级→告警→实锤→事件；社工库/证据/案例/知识库/话术库 = R1-R3 隔离+SQL 实证）。无断点。
2. **并发/时序**：单实例（1960→2248）+ WAL + busy_timeout + max_instances=1 + 进程内锁（cases/evidence 串行化）→ 无死锁；R4 演练与 teammate 写并发无冲突。残余 check-then-act 竞态均为极低概率 P2。
3. **边界**：D5 无 LLM（全程 not_configured 下 L5 纯规则成立）、HIBP 无 key 降级、IOC 缺失、超长文本截断、空库幂等建表——全部实证健康。
4. **状态一致性**：detections（L5=1 样例 + pending=0 + scanned 滞留为运维项）、traps（退役收敛）、evidence/cases 唯一性（0 重复）、pending 升级终态收敛（0 积压）、告警去重（json_extract）、审计链 valid(1825)——一致。

**R5 新增观察（P2 级，运维知情）**：
- **events 表非严格 append-only**：count=17046 vs max(id)=17067，历史删行 21（演练事件 17046-17055 完整保留，删行源自更早的测试数据清理）；gate_logs 链不受影响（独立表）。建议运维明确 events 清理策略或保持只增。
- **pending 终态收敛采用 UPDATE events.payload 标记（resolved/terminal）**：语义可接受（避免重扫），但属"事件行载荷原地修改"——如需严格审计纯度，可改为追加 resolution 事件（P2 可选优化）。
- **job_trap_recheck 候选 LIMIT 10（最近 10 条 scanned）**：6h 周期下旧消息的踩饵召回受限（设计取舍，P2）。
- **手工 check-hit 无 detection_id 时按 content 精确反查**：文本有细微差异（空白等）会 skipped（P2，响应已明示 status）。

---

## 3. 剩余风险收敛度（五轮汇总）

### P0：0（已清）
| 轮次 | 问题 | 状态 |
|------|------|------|
| R1 | job_trap_recheck 未 await → 踩饵检测空转 | ✅ R1 修复，R3/R4 实证 |

### P1（缺陷类）：0（已清）
| 轮次 | 问题 | 状态 |
|------|------|------|
| R1 | L5 自动不可达（实锤不进流水线） | ✅ R1 修复，R4 生产实证 |
| R1 | evidence LIKE 子串误判 + 无唯一约束 | ✅ json_each 双层去重 + 409 |
| R1 | cases 无去重（1125 条重复） | ✅ 串行锁内查重 + 存量清洗至 80 |
| R1 | 双实例（后修正为 venv 父子架构） | ✅ 互斥守护（保护父进程） |
| R2 | 升级路径非原子（实锤丢失窗口） | ✅ P1-A 补偿重放 + 终态收敛，R3 实证 |
| R2 | 存量重复数据（cases/evidence） | ✅ 清洗脚本执行 + 审计 |
| R2 | 手工 check-hit 不联动升级 | ✅ P1-C 联动，R4 生产实证 |
| R3 | 主控未加载新代码（运维态） | ✅ 已重启激活（R4 指纹实证） |

### P1（管理决策类）：1 项，不阻塞
- 冻结重复证据包 10 组（frozen=1 存证语义保留；evidence 全局 UNIQUE 加装需人工决策）。**建议**：维持现状（防误删存证）或离线逐组人工确认后处置；运行期无功能影响。

### P2 运维清单：12 项（全部非阻塞，供排期）
1. cases 无更新/删除 API（发布错误无法修正）
2. evidence 无追加截图/补充材料机制
3. scanned 滞留 638 条（>1 天）无归档策略
4. 告警 1449/1486 unread 无自动已读/降噪
5. events/gate_logs 只增无归档策略（events 已有历史删行，需定策略）
6. IOC 提取噪声（QQ 正则命中银行卡子串）
7. knowledge import 并发 UNIQUE 竞态（IntegrityError 未捕获）
8. persona publish_due/publish_now 竞态（窗口极小）
9. hit_count 口径=扫描次数非去重消息（在线学习信号失真）
10. LLM 慢时 job_scan_dm 单轮背压（有界，无告警）
11. job_trap_recheck 候选 LIMIT 10（旧消息召回受限）
12. detections.text_hash 非唯一（跨源去重口径不一）

---

## 4. 最终判定与运行期建议

**判定：✅ 通过验收，同意转入运行（可全自动可信）。**

依据：
1. R1-R4 全部修复在生产激活并实证（openapi 指纹、生产受控踩饵 100% 链路、前台 4 屏可观测、审计链 valid）；
2. P0/P1 缺陷归零；剩余 P1 为管理决策项（不阻塞）；P2 全为非阻塞运维项；
3. 唯一未到期项为 #3132 证据/case_ready 的定时 job 触发（19:00/23:00 UTC 前后自动产出，候选已实证）。

**运行期建议（按优先级）**：
1. **今日 19:00 UTC 后**复核 `case_ready`、**23:00 UTC 后**复核 `evidence_built`（#3132）→ 完成"可全自动"最终闭环确认；
2. 冻结重复证据包处置决策（P1 管理项，建议 1 周内定）；
3. P2 清单排期（优先：scanned 归档、告警降噪、events 策略、IOC 噪声、knowledge 竞态）；
4. 保持：单实例启动互斥、定期 `GET /system/audit-verify`（当前 valid）、LLM 配置后复测 D5 熔断/预算链；
5. 若启用知乎真实通道/账号桥接：先行小流量试运行并观察 job_zhihu_sync/collector 写路径。

---

*推演范围：五轮宏观整体判断汇总 + 只读终验；演练仅 R4 一轮受控写（已收敛）；本报告全部结论可复核（API/SQL/日志/截图）。*